# Pin-hard policy for this track: @deepseek-ai/dsh is a breaking-change developer preview
# (see packages/README.md, "Pinned dsh version"). Every new dependency must be added with an
# exact version, never a range — a caret range on a preview package is not a safety net, it's
# a promise the upstream repo has already broken once (see the stale dsh-headless@0.0.1-rc.1
# incident documented in packages/README.md).
save-exact=true
