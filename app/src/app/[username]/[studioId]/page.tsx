'use client'

import { SpacesLayout } from '@/components/shell/spaces-layout'

/**
 * Studio overview page - shows spaces list (formerly WorkspacesLayout).
 */
export default function StudioOverviewPage() {
  return <SpacesLayout />
}
