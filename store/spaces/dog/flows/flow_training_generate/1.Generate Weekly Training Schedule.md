---
description: "Generates a personalized weekly training schedule based on breed, age, and skill level"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate a customized weekly training schedule for the user's dog. Consider the dog's breed type (working, sporting, herding, toy, etc.), age, current obedience level, and the user's training goals. Assign specific skills to each day of the week with session duration, repetition counts, and progression criteria. Include rest days and socialization time. Balance new skill introduction with review of previously learned commands. Adapt intensity to the dog's energy level and attention span.

<output target="weeklySchedule">
{
  "type": "object",
  "properties": {
    "trainingGoal": {
      "type": "string",
      "description": "Primary training goal (e.g., 'Basic Obedience', 'Advanced Commands')"
    },
    "dailyPlan": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": { "type": "string" },
          "focus": { "type": "string", "description": "Primary skill focus for the day" },
          "exercises": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "duration": { "type": "string" },
                "repetitions": { "type": "number" },
                "instructions": { "type": "string" }
              }
            }
          },
          "sessionDuration": { "type": "string" }
        }
      }
    },
    "progressionCriteria": {
      "type": "string",
      "description": "When to advance to the next difficulty level"
    },
    "weeklyTips": {
      "type": "array",
      "items": { "type": "string" },
      "description": "General tips for the training week"
    }
  },
  "required": ["trainingGoal", "dailyPlan", "progressionCriteria"]
}
</output>