---
description: "Generates complete ingredient list with quantities, authentic sourcing notes, and substitutions"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate the complete ingredient list for the selected dish. Prioritize authentic Greek products (koroneiki olive oil, P.D.O. feta, fresh rigani). Group ingredients into categories: pantry staples, fresh produce, aromatics, and proteins. Include exact quantities scaled to the serving count. Provide substitution options for hard-to-find Greek ingredients, noting quality impact. Consider the user's selected season for produce availability.

<output target="ingredientList">
{
  "type": "object",
  "properties": {
    "ingredients": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "quantity": { "type": "string" },
          "category": { "type": "string", "enum": ["pantry", "produce", "aromatics", "protein", "dairy"] },
          "notes": { "type": "string" }
        }
      }
    },
    "substitutions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "original": { "type": "string" },
          "substitute": { "type": "string" },
          "qualityImpact": { "type": "string" }
        }
      }
    },
    "servingCount": {
      "type": "number",
      "description": "Number of servings these quantities produce"
    }
  },
  "required": ["ingredients", "servingCount"]
}
</output>