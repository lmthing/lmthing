---
all:
  hosts:
    node1:
      ansible_host: 135.225.105.98
      ansible_user: azureuser
      ansible_become: true
      ansible_ssh_private_key_file: "{{ lookup('env', 'HOME') }}/.ssh/lmthing-vm_key.pem"
      ip: 10.0.0.4
      access_ip: 10.0.0.4
      etcd_member_name: etcd1
  children:
    kube_control_plane:
      hosts:
        node1:
    etcd:
      hosts:
        node1:
    kube_node:
      hosts:
        node1:
    calico_rr:
      hosts: {}
    bastion:
      hosts: {}
    _kubespray_needs_etcd:
      hosts: {}
    k8s_cluster:
      children:
        kube_control_plane:
        kube_node:
