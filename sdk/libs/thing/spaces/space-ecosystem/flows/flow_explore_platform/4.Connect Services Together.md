---
description: "Explains how lmthing services connect and data flows between them for the user's workflow"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate a personalized explanation of how the user's recommended services connect and share data. Focus on the integration points relevant to their specific workflow, drawing from these core platform connections:

- **Workspace sync** — All workspace data (agents, flows, knowledge) lives in a private GitHub repo. Studio, Computer, and Space all sync from this repo. Changes in one place propagate after push/pull.
- **Authentication** — All services authenticate through com/ via SSO. One login works everywhere. API keys (lmt_) provide programmatic access.
- **LLM proxy** — All AI requests route through generate-ai edge function to Stripe LLM proxy to provider. Usage is metered automatically regardless of originating service.
- **Deployment pipeline** — Build in Studio, deploy via Space, list on Store. Or build in Studio, run on Computer for personal use.

Only describe the connections relevant to the user's services. Frame the platform as an integrated system, not separate tools. Highlight any data flow the user should be aware of to avoid surprises (e.g., Computer tier cancellation destroys the K8s pod).

<output target="serviceConnections">
{
  "type": "object",
  "properties": {
    "dataFlows": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "from": { "type": "string", "description": "Source service" },
          "to": { "type": "string", "description": "Destination service" },
          "mechanism": { "type": "string", "description": "How data moves (e.g., GitHub sync, SSO, Stripe metering)" },
          "description": { "type": "string", "description": "What flows and why it matters" }
        },
        "required": ["from", "to", "mechanism", "description"]
      },
      "description": "Relevant data flow connections between services"
    },
    "warnings": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Important caveats or gotchas the user should know"
    },
    "summary": { "type": "string", "description": "One-paragraph overview of how the user's services work together" }
  },
  "required": ["dataFlows", "warnings", "summary"]
}
</output>
