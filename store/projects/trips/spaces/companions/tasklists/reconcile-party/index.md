---
input:
  tripId: string
---

Fold one trip's traveler preferences into a single cited `knowledge_notes` row the planner and
packer read: scan the party's preferences first, then write (or refresh) the party-preferences
note.
