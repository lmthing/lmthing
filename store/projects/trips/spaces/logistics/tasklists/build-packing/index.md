---
input:
  tripId: string
---

Build a packing list for the trip from its destinations, dates, planned activities, and the
forecast around those dates: assess what's needed first, then write the `packing_items` rows.
