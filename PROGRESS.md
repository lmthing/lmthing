# Reliability Rethink: System Spaces + Harness — PROGRESS

Single source of truth for this change set. Plan: `~/.claude/plans/hashed-waddling-plum.md` (approved 2026-07-01).
Previous effort (SPACE-SPEC, all WPs done 2026-06-25) archived in git history of this file.

Status legend: `pending` · `in-progress` · `blocked` · `done`

| Phase | Scope | Status | Notes |
|----|-------------|--------|-------|
| P0 | Hygiene + bundle fix — cli dist copy of system-spaces, delete cruft, fix `system-deep-research` naming drift, rewrite stale system-spaces skill | done | copy-system-spaces.mjs in cli build; standalone CLI resolves `thing` agent; spaces 43/43; drift grep clean (orchestrator also fixed SPACE-SPEC.md:546) |
| P1 | Turn-loop extraction — dedupe statement pipeline, export yield-binding helper | done | eval 64/64, fork+tasklist 80/80, build+tsc clean, no test edits |
| P2 | Exec unification — new `libs/core/src/exec/` (capability, bootstrap, target-match, fork-config, preamble); delegate inherits budget/models/depth/dynamicSpaces | done | A1 fixed + regression-locked (exec/fork-config.test.ts e2e); core 48 files / 473 tests green |
| P3 | TaskEnvelope + neutral salvage + input hard filter; instructs/defaultAction read envelope | done | envelope `{ok,degraded,data,reason?,degradedTasks?}` at tasklist boundary; prose placeholder deleted; seed pick(input keys); core 48 files / 480 tests green |
| P4 | `prelude:` frontmatter — host-executed leaf statements reusing turn-loop binding helper | done | fixes A3; exec/prelude.ts `runPrelude` (+11 tests); fork runs it pre-turn-loop, seeds initialContext + first VARIABLES block; per-statement degrade; yields tick toolCalls, no episode ticks; core 49 files / 491 tests green |
| P5 | canDelegateTo unified semantics (`[]`=none, `"*"`, `registered:*`, yield-time gate) | done | `evaluateDelegatePolicy`/`isDelegateAllowed` in exec/target-match.ts; gates at session+delegate+fork; loader warns on `[]`; thing/architect allowlists, engineer `[]`; defaultAction fast-path exempt (host policy); core 50 files / 519 tests green |
| P6 | System-spaces migration — research preludes, architect shared helpers, user-thing `build_specialist` DAG, DEVELOPMENT.md | done | mock e2e (degraded research → build still runs) + `--dump-system-prompt` audit of all 5 shipped agents, zero violations; core suite 528/51 green, tsc clean |
| P7 | Live verification — nested-vs-standalone A/B, degraded-research build run, bundle fix | done | DEEPSEEK-LIVE-TEST-PLAN.md ladder E0–E7 ✅ both matrices (~30 live runs); 12 fixes applied+validated (5 deep harness bugs incl. shared-WASM bridge corruption, streaming fence token-swallow, usage-await hang); A/B byte-identical investigate prompts at depth-2 vs depth-1; `.issues/` trio deleted per exit criteria; suite 53 files / 541 tests green; NOT committed/deployed |

## Log

- 2026-07-02 — Post-ladder sweep: `git status` revealed 4 stray files the E4 ENGINEER runs wrote into the SOURCE tree (`libs/core/system-spaces/system-engineer/iso*.ts`) — a delegate's relative `writeFile` resolves against the delegated agent's own space dir, which in workspace runs is the source system-spaces tree. Artifacts removed; NEW issue filed per repo lifecycle: `.issues/delegate-writes-resolve-against-system-space-dir.md` (+ Known-issues line). Pre-existing wiring, not introduced by the redesign; fix sketch in the issue (write-root should be the project dir; system spaces read-only to child VMs).
- 2026-07-02 — **P7 DONE — LADDER COMPLETE, all 8 experiments ✅ on both matrices.** E7 D-all trio: (a) reading-list-tracker built/registered/persisted (8 self-recovered fork errors); (b) fresh DeepSeek session delegated DIRECTLY to the registered agent, 0 errors (fix #12 on both models); (c) denial: ONE tempted delegate('system-billing'), structured denial, graceful recovery — DeepSeek offered a manual-input Form fallback + remembered the value. EXIT CRITERIA EXECUTED: deleted `.issues/{system-spaces-bundle-resolution,investigate-forks-degrade-under-delegate-nesting,thing-abandons-build-on-salvaged-research}.md` + their sdk/org/CLAUDE.md Known-issues lines (section now empty) + updated top CLAUDE.md pointer. Session totals: 12 fixes (#1 per-VM WASM isolation, #2 research-depth triage, #3 action-hint seed, #4 envelope casts, #5 JSX key, #6 function/class globalThis, #7 code→engineer always, #8 delegate resolve nudges, #9 neutral request framing, #10 streaming fence filter, #11 bounded usage await, #12 project-space preload+advertise), each with a regression test where harness-class; suite 53 files / 541 tests green; ~30 live Azure runs. NOT committed, NOT deployed — user decisions pending: commit the working tree, deploy timing, incident disposition.
- 2026-07-02 — P7/live-test E0 ✅ both matrices (plan: DEEPSEEK-LIVE-TEST-PLAN.md): fresh core+cli build; traces are NDJSON (forensics toolkit adjusted to `jq -s`). D-leaf (gpt-5.5 session) and D-all (DeepSeek-V4-Flash session) both: 0 typecheck/eval errors, routing chain empty (correct — answered directly), session node done, system prompts byte-identical across matrices modulo model header, STATEMENT_PROTOCOL once, delegate+tasklist DTS present with TaskEnvelope JSDoc, no stale refs. Soft note: DeepSeek skipped the optional instructions.md/documents preload step (harmless on fresh project). Traces in session scratchpad traces/E0-*.json.
- 2026-07-02 — P7/live-test E1 ✅ both matrices: exact expected route (`delegate('user-memory','memory',…)`, nothing else); memory fork ran `remember()` + `currentTask.resolve` cleanly; THING gave natural-language confirmation (DeepSeek used `<Callout variant="success">` correctly). 0 errors both. Verified: memory context prompt has ZERO `declare function delegate` (canDelegateTo:[] DTS strip works live); STATEMENT_PROTOCOL once per context. Note: delegate contexts inherit the session model (gpt-5.5 on D-leaf) — correct by design; LM_MODEL_ROLE_* targets fork() roles only (E3 verifies fork-level propagation).
- 2026-07-02 — P7/live-test E7 D-leaf trio ✅: (b run2) FIX #12 validated — fresh session's FIRST statement delegated straight to the registered reading-list-tracker (Project agents block in prompt, 0 errors, book added via its answer tasklist). (c) denial test TEXTBOOK: THING tried `delegate('system-billing','billing')` once → yield-time denial named the allowed targets (formatDelegateDenial live) → ONE-turn graceful recovery with an error Callout, no retry loop, no improvising. E7 D-all trio started (build first).
- 2026-07-02 — P7/live-test E7a ✅ / E7b run1 ❌→FIX #12: (a) reading-list-tracker built+registered+persisted to `<project>/spaces/`. (b) A FRESH session in the same project couldn't see it — THING checked memory, grepped the project, displayed "looking for it", and stopped (the instruct's "stays registered for later requests" promise had no harness backing across sessions). Two gaps: the CLI `--request` session never passed `preloadSpaceDirs` (the Session mechanism existed but nothing fed it), and NOTHING advertised dynamicSpaces in the system prompt even when loaded. FIX #12: bin.ts scans `<project>/user/spaces/*` into `preloadSpaceDirs`; session.ts renders a "# Project agents (already built & registered)" block (title + exact delegate coordinates + actions) appended to the system block in start() AND resume (resume also gained the preload loop). New `session/preload-spaces.test.ts` locks both (advertised + delegatable via registered:* in a fresh session). Full suite 53 files / 541 tests green, rebuilt. E7b run2 launched.
- 2026-07-02 — P7/live-test E6 ✅ COMPLETE (3/3 per matrix; D-all run3 replaced by run4 after FIX #11): run4 full chain, ZERO unfinished nodes (run3 had 10), agent built+registered+run, 3 recovered nudges. E7 started: part (a) build reading-list agent in persistent /tmp/lmrun-E7-leaf (part (b) reuses same dir so the registered space persists across CLI sessions).
- 2026-07-02 — P7/live-test E6 D-all run3 ❌→FIX #11 (harness hang): the run exited SILENTLY mid-architect with the session still awaiting build_specialist — one build_field fork (fork_40) held its queue slot forever. Trace fingerprint: statements processed, then NO llm_response/turn_end — the fork was stuck on turn-loop's unbounded `await stream.usage` (Azure ended the stream without the final usage chunk; usage promise stayed pending; no timers left → DAG deadlock → Node's event loop drained → clean exit 0, no error anywhere). FIX #11: usage await now races a 10s timeout (cleared timer), and is skipped entirely on streamErrored; usage is telemetry, never worth blocking on. eval+testing suites 145/145, rebuilt. E6 D-all run4 (replacement) launched.
- 2026-07-02 — P7/live-test E6 5/6 runs done, all ✅ on intent (issue-2 kill shot): with the web GENUINELY severed (NODE_USE_ENV_PROXY dead proxy; ECONNREFUSED confirmed in logs), every run completed the FULL chain — research (honest-thin, no live sources) → architect build → agent registered → THING ran it and displayed. No abandonment, no improvising, zero salvage prose in the data plane (all "unavailable"/"budget" purity hits were genuine gardening content). Self-recovered error counts: leaf 0–3/run (fork:design), all-DeepSeek 0–4/run; forced-resolve nudges 0–2/run, all recovered. KEY OBSERVATION vs the plan's written pass criteria: severed search produces HONEST-THIN research (forks resolve empty-sources per their instructions), NOT salvage — so `degraded` stays false, truthfully; the salvage path (envelope degraded:true + degradedTasks) remains covered by the mock e2e (spaces/build-specialist-mock-e2e.test.ts). Criteria graded on intent: build always runs, THING never abandons, purity holds. D-all run3 in flight.
- 2026-07-02 — P7/live-test E5 ✅ BOTH matrices: D-all (all-DeepSeek) also ran the ENTIRE pipeline end-to-end — identical routing chain, hydroponic-advisor built/registered/run, envelope ok, answer displayed; only 2 self-recovered implicit-any typecheck errors inside a fork spawned by the BUILT agent. E6 severing mechanism verified: Node 24's `NODE_USE_ENV_PROXY=1` + dead proxy (127.0.0.1:9) + `NO_PROXY=".azure.com,…"` kills Tavily+DDG+webFetch at connect while LLM traffic flows (sanity-checked both azure hosts). E6 D-leaf run1 launched. NOTE for grading: severed search may yield HONEST-THIN research (forks resolving empty per their instructions) rather than salvage — grade against intent (build runs, no abandon, purity); the salvage path itself is locked by the mock e2e.
- 2026-07-02 — P7/live-test E5 D-leaf run2 ✅ END-TO-END (the issue-1+issue-2 scenario, live): build_specialist DAG → research prelude delegated researcher#deep_research (full pipeline, 6 investigate forks) → build fork delegated architect#synthesize_and_run → architect designed/scaffolded/validated/REGISTERED `hydroponic-advisor` and smoke-ran it → envelope `ok:true,degraded:false` with real coordinates → THING's guarded turn 2 delegated to the REGISTERED space (registered:* grant) → answer displayed with success callout. ZERO errors, salvages, nudges. **A/B PROOF (A1+A2)**: E5's investigate-fork system prompts at depth-2 nesting are BYTE-IDENTICAL to E3's depth-1 baseline (6 forks each, model S→DeepSeek). Fix #10 unblocked the architect (run1's JSON-token corruption gone). E5 D-all launched.
- 2026-07-02 — FIX #10 refinement: first FenceLineFilter draft held EVERY unterminated final line until flush, silently rerouting single-chunk statements to the event-less trailing-flush path (3 suite tests caught it — typecheck_error/llm_progress trace events vanished; would have blinded live forensics on final statements too). Refined: a partial line is held ONLY while it could still become a droppable fence line (`/^\s*(?:\x60{1,3}.*|[A-Za-z]{0,10})$/`); anything else releases immediately into the live streaming pipeline; a line whose head was released is exempt from drop checks on its remainder (headEmitted flag). +2 tests. FULL SUITE 52 files / 540 tests green; rebuilt. E5 run2 launched.
- 2026-07-02 — P7/live-test E5 D-leaf run1 ❌→FIX #10 (harness, streaming corruption): ROUTING PERFECT — the full DAG fired exactly as designed (session tasklist build_specialist → research task's prelude delegates researcher#deep_research → build fork delegates architect#synthesize_and_run; then honest ok:false packaging, THING's `b.ok && b.data.ok` guard held, one retry, honest error report — no improvising). The failure: the architect's seeding statement arrived as `research:.stringify(...)` — the token `JSON` was EATEN mid-statement. Root cause: `stripMarkdownFences` ran PER STREAMING CHUNK; any chunk whose whole content equals a fence-lang suffix ("json", "ts", "on", "script", …) was dropped as a stray fence tag — tokenizers routinely emit such tokens standalone. FIX #10: new `FenceLineFilter` (turn-loop.ts) buffers the trailing partial line so drop decisions happen on COMPLETE lines only; end-of-stream `flush()`; split fence tags now reassemble and drop via the ^``` rule; trailing-flush path feeds released text through the detector (may complete multiple statements — processed with the same stop-on-error/yield rule). stripMarkdownFences kept for whole-text use with a warning doc. +4 unit tests incl. the exact JSON.stringify and lone-"on" cases (eval 68/68). Full suite re-running; E5 run2 next.
- 2026-07-02 — P7/live-test E4-leaf guard post-#9: routing still correct (engineer, on the request's own words). The engineer itself flaked THIS run (gpt-5.5 fumbled exploration syntax ×3 → maxRetries burned) — but every structural guarantee held: forced-resolve nudges fired, engineer resolved an HONEST `{ok:false, summary:"Stopped before implementation…"}`, THING reported the failure verbatim instead of improvising. Watchlist (not fixed): engineer tends to explore an empty project before coding, burning retries; tune engineer instruct if it recurs in E5/E6. E4 stays ✅ (run2 pair). E5 D-leaf launched.
- 2026-07-02 — P7/live-test E3 run3 ✅ — FIX #9 validated live: neutral "User request:" framing restored the correct deep_research route; 0 errors, 0 salvages, envelope ok:true. Suite fix: harness-features' `assertConversationInvariants` asserted the OLD wrapper — updated to require the new framing and the ABSENCE of "Respond with TypeScript code only" in the user message (42/42; full suite green modulo that file re-run). E4-leaf guard re-run launched (its request self-contains "Write a TypeScript function", so path-4 must trigger on the request's own words post-#9).
- 2026-07-02 — P7/live-test E3-regression run ❌→FIX #9: the E3 D-leaf regression re-run (guard for #5–#8) caught a REAL regression — the deep-research request routed to the ENGINEER twice. Root cause is a latent harness defect exposed by #7's wording: session.ts wraps EVERY user message as "Write TypeScript code to accomplish the following task. Respond with TypeScript code only. Task: …" (both start() and continue()), conflating the reply CHANNEL (owned by STATEMENT_PROTOCOL since P2) with the REQUEST semantics — it primes triage toward the code path on every request. FIX #9: neutral `User request:\n\n${message}` framing at both sites (no test asserted the old wrapper; full suite re-running). Lesson recorded: the old wording had masked this by being softer; the regression guard did its job.
- 2026-07-02 — P7/live-test E4 ✅ BOTH matrices: D-all run2 validated FIX #8 live — engineer finished without resolving, ONE forced-resolve nudge fired, it resolved `{ok:true, files:{...}}`, THING displayed the code (Panel/CodeBlock JSX); single delegate, no improvising. E3 D-leaf regression run launched (guard for fixes #5–#8), then E5.
- 2026-07-02 — P7/live-test E4 D-leaf run2 ✅ (fixes #6+#7 validated: correct `delegate(system-engineer, engineer)` route, 0 errors, engineer produced a complete parser + comprehensive test suite in ONE turn, engineer DTS has no delegate). E4 D-all run1 ❌ NEW STRUCTURAL FINDING: DeepSeek engineer did all the work (wrote impl+test files, ran deno tests) but never called currentTask.resolve() → model-driven delegate auto-capture returned NULL to THING (twice) → THING violated its no-improvising rule and coded inline. Root cause: forks have a forced-resolve guarantee, DELEGATES did not. FIX #8: delegate.ts now mirrors fork.ts — up to 2 resolve-only STOP turns (fresh maxEpisodes:4 budget, `:resolve_nudge` trace context, yields unserviced) when the model finishes without resolving and no tasklist was captured. +1 regression test (delegate suites 76/76). E4 D-all run2 launched.
- 2026-07-02 — P7/live-test E4 D-leaf run1 ❌ — TWO findings. (a) ROUTING: gpt-5.5 THING wrote the ISO-8601 parser INLINE (empty delegation chain; watchlist item "coding inline instead of delegating") → FIX #7: thing instruct path 4 now says code requests ALWAYS go to the engineer, never path 1, with the rationale. (b) HARNESS GAP (exposed by the inline coding): `extractBindingNames` (context/variables.ts) only handled const/let/var — `function foo(){}`/`class X{}` declarations were never propagated to globalThis, so the next statement's `foo(...)` passed TYPECHECK (accumulated context) but threw "'foo' is not defined" at eval; the model re-declared → "Duplicate identifier" (exact live error pair). FIX #6: extract function (incl. async/generator) + class declaration names; type/interface deliberately excluded (no runtime binding). +3 unit tests (context+eval suites 90/90). Also noted: gpt-5.5's own test data was wrong ("0S" is not valid ISO-8601 — threw at eval). E4 run2 launched.
- 2026-07-02 — P7/live-test E3 ✅ BOTH matrices (deep research): correct `deep_research` route on both; D-leaf 0 errors / D-all 1 self-recovered; ZERO salvages/nudges both — every scope/plan/6×investigate/synthesize/summarize fork resolved single-turn; envelope ok:true, degraded:false. A1 fix live-proven: roleModels propagate through the delegate into every nested fork (D-leaf: session gpt-5.5, all forks S→DeepSeek). A2 clean: no stray seed keys in investigate prompts; STATEMENT_PROTOCOL once ×7 contexts. Investigate prompts saved as E5 A/B baseline (scratchpad/E3-investigate-prompts-baseline.json). Purity hits were benign content ("budget-constrained", "data unavailable" as research gap). D-all's one error: DeepSeek wrote React-style `key={i}` in a .map() — FIX #5: `JSX.IntrinsicAttributes { key?: string|number }` in library-dts.ts (typecheck+bootstrap suites 23/23, core rebuilt). E4 launched.
- 2026-07-02 — P7/live-test E2 ✅ BOTH matrices (run4 D-leaf, run2 D-all): correct `research` route on both models; fix #3 validated live (first tasklist call carries the query seed, delegate context 2 turns not 3, zero invalid-seed errors); fix #4 no adverse signature; 0 errors, envelope ok:true, purity clean. D-all note: DeepSeek's seed included a stray `context:{}` key — the P3 input hard filter stripped it before the fork (by design). DeepSeek rendered the answer with Stack/Heading/List JSX. E3 (deep research) launched.
- 2026-07-02 — P7/live-test E2 D-leaf run3 — FIX #2 (routing) VALIDATED: THING now picks the `research` action for the plain request; 1 DeepSeek answer fork, single turn, 0 errors, clean envelope, sourced answer displayed. New finding attributed precisely: the researcher's first `tasklist("research", context)` call with empty seed was the HARNESS's OWN action-hint text (delegate.ts:186 dictated `tasklist(name, context)` while `query` is a SEPARATE var) — the model obeyed verbatim → "invalid seed: missing required field query" → wasted turn on EVERY action-style delegate. FIX #3 applied: hint now dictates `tasklist(name, { query, ...context })` (both action and no-action branches) + regression test in harness-features.test.ts (42/42 green) replaying the verbatim-obedience path against a required-query input schema. FIX #4 applied (independent signature, same re-run): thing instruct's two delegate cast examples now include `reason?: string; degradedTasks?: string[]` (run1's typecheck error). E2 run4 launched to validate #3+#4.
- 2026-07-02 — P7/live-test E2 D-leaf run2 — FIX #1 (per-VM WASM module) DECISIVELY VALIDATED on the same heavy shape (THING again over-escalated to deep_research — systematic, useful for attribution): the full pipeline (scope/plan/7×investigate/synthesize/summarize, DeepSeek in every fork) ran with **ZERO typecheck/eval errors (was 16), ZERO forced-resolve nudges (was 14), ZERO salvages (was 7 degraded tasks)**; envelope `ok:true, degraded:false`; every investigate fork resolved in a SINGLE turn (prelude design working); purity clean; answer displayed. Full suite after fix: 52 files / 529 tests green (incl. new `exec/two-delegates-repro.test.ts`); E1 D-leaf regression re-run ✅ (0 errors, correct route). This shared-WASM corruption was very likely the true root cause behind `.issues/investigate-forks-degrade-under-delegate-nesting.md`'s error counts. Remaining from E2: (i) routing over-escalation → FIX #2 applied to `user-thing/agents/thing/instruct.md` (research = default depth, deep_research only on explicit request, cost note) — E2 run3 re-running; (ii) researcher model-driven turn first calls `tasklist("deep_research", {})` with an empty seed, errors, then self-corrects with the query — researcher-instruct misconception, queued; (iii) thing instruct casts still omit `reason?`/`degradedTasks?` — queued as FIX #3.
- 2026-07-02 — P7/live-test E2 D-leaf run1 ❌ — FOUR findings, one MAJOR harness bug. (a) ROUTING: THING over-escalated "Research the current state of WebGPU adoption" to `deep_research` (expected `research`); fix pending in thing instruct triage wording. (b) HARNESS BUG (the big one): late in the heavy run (7 concurrent investigate forks + 213s delegate), bridged host calls SILENTLY no-oped across three independent VMs — the session's 2nd `delegate()` evaluated OK but never pushed its yield (statement entered typecheck context, binding stayed undefined, "non-yielding binding" nudge fired); `fetch()` inside webSearch resolved `undefined` → "cannot read property 'ok' of undefined" in 2 fork preludes + session; `currentTask.resolve()` no-oped in live forks — DeepSeek emitted GOOD schema-valid resolves (verified in console), turn ended 'done', host `didResolve` stayed false → 5/7 investigates + synthesize + summarize salvaged `no_resolve` → envelope `ok:false` → THING retried → dead delegate. Two clean repros (vitest Session 2-sequential-delegates incl. multiline as-cast shape; full CLI --mock via user-memory) PASS — failure needs the heavy concurrent shape. Root cause per quickjs-emscripten README: ONE shared asyncified WASM module hosted every runtime (session+delegate+all forks); asyncify state is the documented cross-context leak, and the pre-existing swallowed `gc_obj_list` teardown assertion ("deep fork/delegate nesting") corrupts shared module state. FIX #1 applied: `sandbox/quickjs.ts` getWASMModule() now instantiates a FRESH WASM module per createVM (kept LM_QJS_DEBUG variant). New `exec/two-delegates-repro.test.ts` locks the sequential-delegate contract. (c) MISCONCEPTION-BAIT: thing instruct's cast examples type delegate results as `{ok,degraded,data}` WITHOUT `reason?`/`degradedTasks?` — gpt-5.5 referenced `r.reason`/`r.degradedTasks` → typecheck error; instruct cast fix queued (one fix per re-run). (d) DeepSeek watchlist hits: redeclared prelude-bound names (`searchAnswer`, `hasSearchInfo`) + used undeclared `sources` in multi-statement recovery turns after broken preludes. Also: forensics Q7 pattern was wrong — the real forced-resolve nudge text is "STOP. Do NOT search" (14 fired); nudge turns' llm_requests do NOT carry the `:resolve_nudge` trace context (minor observability bug, noted). Re-running E2 same-shape after the WASM fix for attribution.
- 2026-07-02 — P6 DONE (system-spaces migration finishing work — mock e2e + dump-system-prompt audit): on top of the in-flight P6 tree (uncommitted `user-thing/build_specialist` DAG — `01-research.md` optional prelude-delegates to `system-research/researcher#deep_research`, `02-build.md` goal always delegates to `system-architect/architect#synthesize_and_run`; DEVELOPMENT.md/skills docs; `system-spaces-dag.test.ts` structural DAG+prelude-typecheck tests, 11 tests) added the missing mock end-to-end coverage and verified the shipped system prompts. New `libs/core/src/spaces/build-specialist-mock-e2e.test.ts`: runs the REAL `build_specialist` tasklist through `runTasklist`+`ForkEngine`+`createMockStreamFn` (the established mock pattern from `tasklist/delegate-in-task.test.ts`/`tasklist/deep-research-flow.test.ts`) with the research fork's model never resolving (returns `''` on every turn incl. both forced resolve-nudges) so `fork.ts` salvages it (`reason: no_resolve`), while a mocked `delegateRunner` proves the build task's delegate to the architect is ALWAYS reached and returns a clean result — asserts `env.degraded===true` + `env.degradedTasks` contains `"research"`, `env.ok===true`, and `env.data` is exactly the build's schema fields with no `"unavailable"`/prose anywhere (this is the exact scenario `.issues/thing-abandons-build-on-salvaged-research.md` and `.issues/investigate-forks-degrade-under-delegate-nesting.md` describe — those issues remain open since they're about the LIVE model path, not the structural DAG guarantee this test locks in). Built `@lmthing/core`+`@lmthing/cli`, then audited `--dump-system-prompt` output for all 5 shipped system agents (task brief said "six"; only 5 agent dirs exist under `libs/core/system-spaces/*/agents/*/` — system-global ships no agent, confirmed by directory listing): `thing`, `architect`, `researcher`, `engineer`, `memory`. Working invocation (a bare `--dump-system-prompt --agent X` fails — `--space <dir>` is required and must exist): `ROOT=$(mktemp -d); mkdir -p "$ROOT/space"; LMTHING_ROOT="$ROOT" node libs/cli/dist/cli/bin.js --space "$ROOT/space" --agent <slug> --dump-system-prompt "$ROOT/<slug>.md"`. All 5 rendered without error; verified per-dump: `STATEMENT_PROTOCOL` (`exec/preamble.ts`) appears exactly once each, at the same lines, no paraphrased restatement elsewhere in the file (the `DEVELOPMENT.md`/`thing/instruct.md` cleanup already in the working tree holds); `canDelegateTo` semantics correctly reflected — `thing`/`architect` (allowlists) have `declare function delegate` (2 overloads) in the ambient DTS, `engineer`/`researcher`/`memory` (`canDelegateTo: []`) have ZERO; the `TaskEnvelope` JSDoc (`Resolves to { ok, degraded, data, reason?, degradedTasks? }`) appears immediately above `declare function tasklist` in all 5. Zero violations found — no space file changes were needed for this deliverable. Final verification: `npx vitest run libs/core/src` → **Test Files 51 passed (51), Tests 528 passed (528)** (519 baseline + 8 already in the in-flight `system-spaces-dag.test.ts` expansion + 1 new e2e test; the 2 QuickJS teardown-assertion stderr lines in `sandbox/teardown-repro.test.ts` are the intentional catchable-abort repro, both tests pass); `pnpm --filter @lmthing/core exec tsc --noEmit` clean; `pnpm --filter @lmthing/cli exec tsc --noEmit` still fails only on the pre-existing `src/web/app.tsx` → `@lmthing/ui/chat` import (unrelated, ignored per brief). Files touched this session: added `libs/core/src/spaces/build-specialist-mock-e2e.test.ts`; updated this PROGRESS.md. P7 (live verification) remains pending.
- 2026-07-02 — Deploy-gate check (post-P5): compute Docker image built with the exact CI invocation (`docker build -f devops/argocd/compute/Dockerfile`, context sdk/org) — exit 0; updated system-space frontmatter confirmed inside the image at `/app/libs/cli/dist/system-spaces/`; web-app Vite build clean; zero dependency changes so `--frozen-lockfile` is safe. Deploying still requires: commit+push sdk/org, bump submodule pointer, CI/ArgoCD roll.
- 2026-07-01 — Plan approved. PROGRESS.md reset for this effort. Dispatching P0 + P1 as parallel subagents (disjoint files).
- 2026-07-01 — P0 DONE: `libs/cli/scripts/copy-system-spaces.mjs` wired into cli build (`tsup && node scripts/...`); standalone built CLI verified to load the `thing` agent from a fresh LMTHING_ROOT (Docker COPY step now redundant — left in place, flagged); deleted system-research package.json+lock, architect `.lmthing/`, engineer TaskInput.tsx (+frontmatter ref); `system-deep-research`→`system-research` fixed across sdk/org docs + system.ts comment + top-level Architecture.md; orchestrator additionally fixed SPACE-SPEC.md:546; `.claude/skills/system-spaces.md` research/engineer sections rewritten to match reality; spaces suite 43/43; `.issues/system-spaces-bundle-resolution.md` kept (delete after P7 live verify).
- 2026-07-01 — P2 dispatched (exec unification, single agent): exec/{fork-config,target-match,bootstrap,preamble}.ts + routeCommonYield fork variant + A1 fix (delegate ForkEngine inherits budgetLimits/roleModels/forkDepth/dynamicSpaces).
- 2026-07-01 — P1 DONE: `processStatement` closure dedupes main-loop vs trailing-flush pipeline (flush-path differences preserved: no tracer/log emissions, no stream.abort); `bindYieldResults(vm, pattern, yieldCount, resolvedValues)` exported with the getVar-preference nested-yield fix byte-equivalent. eval 64/64, fork+tasklist 80/80, core build + tsc --noEmit clean, zero test modifications.
- 2026-07-02 — P2 DONE (exec unification + A1 fix): created `libs/core/src/exec/{capability,preamble,target-match,fork-config,bootstrap}.ts` (+3 test files, 40 tests). Deleted the three copy-pasted bootstrap/DTS blocks (session injectGlobals/injectJSXRuntime/injectSpaceFunctions, fork + delegate inline blocks), fork.ts `forkProcessYield` (fork leaves now route through extended `routeCommonYield` with knowledgeSpaceDir/resolveRegisterSpace/gated runDelegate), fork.ts `resolveTaskDelegate` impl (moved to exec/target-match.ts, re-exported for compat) and delegate.ts's inline `directDeps.find` matcher. `library-dts.ts` restructured additively (ASK/TASKLIST/FORK/DELEGATE/COMMON fragments; LIBRARY_DTS[_NO_ASK] recomposed, declaration-set-equivalent — pinned by exec/bootstrap.test.ts old-vs-new tests). A1: `RunDelegateOpts` gained budgetLimits/roleModels/dynamicSpaces; delegate ForkEngine built via `forkEngineOptsFrom` (mapped type forces every ForkEngineOpts key at both wiring sites) with forkDepth = delegation depth+1; session threads the fields at both `runDelegate` call sites; propagates through `runChildDelegate` spread down arbitrary nesting; e2e-locked (fork under delegate honors maxForkDepth). STATEMENT_PROTOCOL (exec/preamble.ts) now single source for statement rules, consumed by RUNTIME_PREAMBLE (session+delegate) and the fork prompt builder; roles.ts SCOPE paragraph deduped. Full core suite 48 files / 473 tests green; core build + tsc clean; cli build clean. Two pre-existing stale assertions in mock-session per-role-model tests (expected no model on llm_request; defaultModel propagation intentionally changed this before P2 — they fail at HEAD) updated to assert the propagated model. Pre-existing at HEAD, untouched: `pnpm --filter @lmthing/cli exec tsc --noEmit` fails on `src/web/app.tsx` importing `@lmthing/ui/chat` (@lmthing/ui not in cli deps/node_modules) — unrelated to core.
- 2026-07-02 — P3 DONE (TaskEnvelope + neutral salvage + input hard filter): new `libs/core/src/exec/envelope.ts` (`TaskEnvelope`, `DegradeReason`, `salvageData` — strings salvage to `""`, prose constant DELETED; grep-clean). `fork.ts`: `salvageOutput` removed; internal `forkWithMeta<T>() → {value, degraded, reason?}` (bare `fork()` unchanged, returns value); reason codes: `no_resolve` (finished w/o resolve, incl. after nudges), `budget` (BudgetExceededError caught in the nudge/salvage loop; hard main-loop caps still PROPAGATE), `timeout`/`schema_mismatch` declared but unreachable today (explicit timeouts reject; invalid resolve rejects — mirrored, unchanged). `runTasklist` now returns the envelope: `ok` = goal un-salvaged, `degraded` = any salvage, `degradedTasks` labels incl. forEach `task[i]`, `data` = raw goal output; task→task upstream vars stay RAW; hard throws (stuck DAG/invalid seed/skipped goal/budget) unchanged. Input HARD filter: declared `input` schema ⇒ forks get `pick(seed, keys)` (forEach `item`/`index` injected after, unaffected); no schema ⇒ passthrough. Delegate auto-capture passes the envelope untouched; session defaultAction fast-path unwraps `envelope.data` for the `{spaceKey,agentSlug,…}` chain (raw-shape fallback kept). Docs: TASKLIST_DTS JSDoc, system-block globals summary, DEVELOPMENT.md salvage bullet. Instructs updated to envelope reads (`t.ok && t.data.ok`, payload via `.data`): architect (both jobs), researcher (both actions), user-thing (paths 2+3), 04-write_agent generated systemPrompt. Tests updated to the NEW contract (old raw-result/prose assertions): fork.test.ts salvage (`''` not `/unavailable/`) + salvageData; orchestrator/foreach/deep-research/delegate-in-task goal asserts → `.data` (+ok/degraded); harness pipeline display + 2 delegate-capture tests → `d.data.value`. New tests: forkWithMeta degraded/reason; envelope salvage (`(unavailable` nowhere in result/trace); `each[2]` forEach labeling; degraded-non-goal ⇒ `ok:true, degraded:true`; input filter (junk/parentHistory stripped from fork prompt+vars) + undeclared passthrough; defaultAction chain reads `envelope.data` (mock-session). Core suite 48 files / 480 tests green; core build + tsc + cli build clean.
- 2026-07-02 — P4 DONE (`prelude:` host-executed leaf statements, fixes A3): `spaces/tasklist-load.ts` parses `prelude` (YAML block scalar; non-empty-string validated at load, deep validation deferred to run time) → `TaskNode.prelude` → orchestrator threads it onto `ForkTask.prelude`. New `libs/core/src/exec/prelude.ts`: `splitPreludeStatements` (single `ts.createSourceFile` parse of the trusted static source, top-level statements — no streaming heuristics) + `runPrelude({vm, source, ambientDts, processYield, renderHost, budget?, tracer?, scope?})` running the SAME per-statement pipeline as the turn loop (runTsc against fork ambient + accumulated context → transpileStatement → `globalThis['x']=x` propagation → evalStatement → yields resolved through the fork's OWN `routeCommonYield` closure, drained in bounded rounds for multi-round nested yields → binding via the EXPORTED `bindYieldResults`, getVar-preference intact). Failing statement never kills the fork: names bound `undefined` + ambient `any` decls + `// prelude: statement N failed: …` note in the VARIABLES block, remaining statements continue; `BudgetExceededError` still propagates. Prelude yields tick `tickToolCalls`; statements do NOT tick episodes. Trace: statement/typecheck_error/eval_error/yield/yield_resolved/variables events with `<fork-label>:prelude` context + the fork's nodeId. `fork.ts` `runFork`: prelude runs after createChildVM (forEach `item`/`index` + seed already injected — ordering verified by e2e test) and ambient/yieldCtx setup, BEFORE the turn loop; successful statements seed the turn loop's `initialContext`; bound values appended to history as a first VARIABLES message (emitVariables with SCOPE/ALREADY-EXECUTED + failure notes) so the model SEES the values. `currentTask.resolve` in a prelude: typechecked against `buildAmbientDts({currentTask:false})` ⇒ fails typecheck as a noted per-statement prelude error (the safer option; a `globalThis`-cast bypass would behave as today — trusted authors, documented in exec/prelude.ts). DEVELOPMENT.md §3b added; NO system-space task files migrated (that's P6). New `exec/prelude.test.ts` (11 tests): split unit ×2; plain bindings + episodes-not-ticked + first-prompt VARIABLES + `:prelude` trace labels; loadKnowledge yield + toolCalls tick; nested-yield fetcher fixture (`processed:q1` proves getVar preference, real fixture not a spy); error path (stmt 2/3 eval-throws, 1+3 still ran, note surfaced); currentTask-in-prelude rejected (model's resolve wins); forEach+prelude per-element `item` e2e; load-time parse + non-string/empty reject ×3. Core suite 49 files / 491 tests green; core build + `tsc --noEmit` + cli build clean (cli tsc app.tsx failure pre-existing, unrelated).
- 2026-07-02 — P5 DONE (unified `canDelegateTo` semantics — the agent-level silent-no-op trap is closed): policy evaluation lives in ONE place — `libs/core/src/exec/target-match.ts`: `evaluateDelegatePolicy(entries: string[]|undefined, level: 'agent'|'task') → DelegatePolicy {mode: 'none'|'unrestricted'|'allowlist', entries, allowRegistered}` (omitted: agent=unrestricted back-compat, task=none; `[]`=none; `["*"]`=unrestricted; list=allowlist with `"registered:*"` split into a flag) + `isDelegateAllowed(policy, pkg, agent, dynamicSpaces?) → {allowed, allowedActions?}` (the yield-time gate; registered:* matches dynamicSpaces keys/dirs/packageNames with dir-suffix tolerance; agent-existence deliberately NOT checked there so wrong slugs get the precise downstream error) + `formatDelegateDenial` (actionable, retryable, names allowed targets). Enforcement wired at ALL SIX points: capability/DTS — `sessionCapabilities(canDelegate)`/`delegateCapabilities(canDelegate)` now parameterized, session.ts (start/resume/buildSystemPrompt + createSessionVM) and delegate.ts pass `policy.mode!=='none'`, fork.ts derives task `canDelegate` from the policy (so task `["*"]`/`registered:*` now inject delegate); yield-time — session `buildYieldContext({enforceDelegatePolicy:true})` (handleYield path), delegate.ts routeCommonYield runDelegate hook (replaces the old non-denying directDeps.find; allowlist matches also feed `allowedActions` narrowing, which composes with the existing in-runDelegate action check), fork.ts yieldCtx.runDelegate (replaces the inline resolveTaskDelegate gate, same salvage-on-deny behavior). defaultAction fast-path DECISION: host policy, EXEMPT from the model-facing gate (buildYieldContext default enforce=false) — incl. the chained delegate to a build's `{spaceKey,agentSlug}` coordinates (effectively registered:*), so THING/architect flows are structurally safe. Loader (`spaces/load.ts`): `AgentDef.canDelegateTo` is now tri-state `string[]|undefined` (omitted preserved), `LoadSpaceOpts.onWarn` (default console.warn `[spaces]` prefix) warns once per `[]` agent: `canDelegateTo: [] means no delegation; use ["*"] for unrestricted`; `resolveDirectDeps` accepts undefined + skips `*`/`registered:*`; registry.preloadDirect null-safe. System block: new `omitDelegate` (like omitAsk) strips the delegate bullet/yield-list/cast-example when mode='none' (no more baiting the model into typecheck errors). System-space frontmatter (minimal, keeps current behavior; P6 does the real migration): user-thing/thing → [system-research/researcher, system-architect/architect, system-engineer/engineer, user-memory/memory, "registered:*"]; system-architect/architect → ["registered:*", system-research/researcher]; system-engineer/engineer → [] (added; body never delegates — verified); researcher/memory already [] (bodies never delegate — verified). NOTE: architect's writeAgentFile still emits `canDelegateTo: []` for BUILT agents — they now genuinely get no delegate (they never delegated; loader warning will fire per built agent) — P6 should make the generated frontmatter explicit. Tests: NO existing tests asserted the old no-op (zero updated; suite was green untouched); +28 new — exec/target-match.test.ts +11 policy/gate/denial units incl. registered matching; exec/delegate-policy.test.ts (NEW, 15): delegate-VM `[]` (typeof delegate === 'undefined' + scripted delegate() fails TYPECHECK "Cannot find name 'delegate'", child never runs, prompt not advertising delegate), allowlist deny-then-self-correct (error names targets) + `#action` narrowing, `["*"]`, omitted back-compat lock, registered:* (allowed / same-target-without-grant denied / unregistered-with-grant denied), session-VM `[]` + session allowlist deny→retry→success, loader tri-state preservation + warn (spy channel + console.warn default + no-warn on omitted/list); tasklist/delegate-in-task.test.ts +2: task `["*"]` unscoped runner routing, task `registered:*` admit-registered/block-unregistered. Verbatim totals: `npx vitest run libs/core/src` → **Test Files 50 passed (50), Tests 519 passed (519)**; `pnpm --filter @lmthing/core build` + `pnpm --filter @lmthing/core exec tsc --noEmit` + `pnpm --filter @lmthing/cli build` all clean (cli app.tsx tsc failure pre-existing, unrelated). Docs: DEVELOPMENT.md §3a rewritten with the semantics table (+ task-table row), SPACE_DEVELOPMENT.md agent-frontmatter table added, .claude/arch/spaces.md AgentDef/loading notes updated.
