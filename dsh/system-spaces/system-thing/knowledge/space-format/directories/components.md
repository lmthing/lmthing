`components/view/<Name>.tsx` (or `components/form/<Name>.tsx`) is a real React component an agent
can render inline in the chat UI instead of describing something in text — a card, a small form,
a summary view. Like functions and tasklists, this is a file added directly to the space on disk,
not something `create_agent` can grant through conversation.
