'use client'

import { SettingsView } from '@/components/shell/settings-view'

/**
 * Package.json settings page.
 */
export default function PackagesSettingsPage() {
  return <SettingsView isOpen />
}
