/**
 * ThingPanel - AI workspace actions assistant
 *
 * This component is re-exported from its original location in shell/components.
 * The ThingPanel is a complex 2600+ line component with deep integration into
 * workspace context (LLM streaming, tool calling, conversation management).
 * Full element-import migration is tracked separately.
 */
export { ThingPanel } from '@/shell/components/ThingPanel'
export type { ThingPanelProps } from '@/shell/components/ThingPanel'
