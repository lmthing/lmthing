<?xml version="1.0" encoding="UTF-8"?>
<debug-log>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
(no variables declared)

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

<message role="user">
where can I find the system prompt
</message>

<turn n="1" messages="2" finish="stop" model="glm-4.5">
  <usage input="4175" output="203" total="4378" reasoning="99" cache-read="1527" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name   Type    Value
-----  ------  --------------------------------------------------
input  Object  {promptType, details}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var input = await ask(<div>
  <Select name="promptType" label="Which system prompt are you looking for?" 
    options={[
      "Main agent system prompt",
      "Codebase navigator prompt",
      "REPL agent prompt", 
      "Cloud function prompt",
      "Other specific prompt"
    ]} />
  <TextInput name="details" label="Any additional details (e.g., agent name, space name)?" />
</div>)

await stop(input)
  </message>
  <message role="user">
← stop { input: { "promptType": "Main agent system prompt", "details": "" } }
  </message>
  <scope>
    input: Object {promptType, details}
  </scope>
</turn>

<turn n="2" messages="4" finish="stop" model="glm-4.5">
  <usage input="4322" output="152" total="4474" reasoning="107" cache-read="1519" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name       Type    Value
---------  ------  --------------------------------------------------
input      Object  {promptType, details}
knowledge  Object  {codebase}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var knowledge = loadKnowledge({
  "codebase": {
    "patterns": {
      "type": {
        "agent-system": true
      }
    }
  }
})

await stop(knowledge)
  </message>
  <message role="user">
← stop { knowledge: { "codebase": { "patterns": { "type": { "agent-system": "# Agent System Patterns\n\nThe agent system is built around **Spaces** — self-contained workspaces with three pillars: Agents, Flows, and Knowledge.\n\n## Space Structure\n\n```\n{space-slug}/\n├── package.json              # metadata (name, version)\n├── agents/                   # AI specialists\n│   └── agent-{role}/\n│       ├── config.json       # runtime field requirements\n│       ├── instruct.md       # personality, tools, slash actions\n│       ├── values.json       # runtime state (starts empty)\n│       └── conversations/\n├── flows/                    # step-by-step workflows\n│   └── flow_{action}/\n│       ├── index.md          # overview + step links\n│       └── {N}.Step Name.md  # numbered steps\n├── functions/                # utility functions (pure JS/TS)\n│   └── functionName.tsx\n├── components/               # React display components\n│   ├── view/                 # read-only display\n│   └── form/                 # interactive forms\n└── knowledge/                # structured domain d... (truncated, 4210 chars total)" } } } } }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
  </scope>
</turn>

<turn n="3" messages="6" finish="stop" model="glm-4.5">
  <usage input="4669" output="172" total="4841" reasoning="156" cache-read="1520" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name       Type    Value
---------  ------  --------------------------------------------------
input      Object  {promptType, details}
knowledge  Object  {codebase}
result     Object  {name, aliases, path, description, keyFiles}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var result = findPackage("agent")

await stop(result)
  </message>
  <message role="user">
← stop { result: { "name": "lmthing", "aliases": ["core", "agent", "framework"], "path": "org/libs/core/", "description": "Agentic framework — StatefulPrompt, plugins, multi-provider, CLI", "keyFiles": ["src/stateful-prompt.ts", "src/plugins/", "src/providers/", "src/cli/"] } }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
  </scope>
</turn>

<turn n="4" messages="8" finish="stop" model="glm-4.5">
  <usage input="4609" output="118" total="4727" reasoning="94" cache-read="1548" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name       Type       Value
---------  ---------  --------------------------------------------------
input      Object     {promptType, details}
knowledge  Object     {codebase}
result     Object     {name, aliases, path, description, keyFiles}
files      undefined  undefined

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var files = await listDir("org/libs/core/", { recursive: true })

await stop(files)
  </message>
  <message role="user">
← error [Error] ENOENT: no such file or directory, scandir '/home/vasilis/GEANT/lmthing/org/libs/repl/org/libs/core' (line 7)
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: undefined undefined
  </scope>
</turn>

<turn n="5" messages="10" finish="stop" model="glm-4.5">
  <usage input="4684" output="54" total="4738" reasoning="34" cache-read="1522" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name       Type           Value
---------  -------------  --------------------------------------------------
input      Object         {promptType, details}
knowledge  Object         {codebase}
result     Object         {name, aliases, path, description, keyFiles}
files      Array<string>  [".claude", ".env", ".env.example", ... +15]

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var files = await listDir(".", { recursive: false })

await stop(files)
  </message>
  <message role="user">
← stop { files: [".claude", ".env", ".env.example", "CLAUDE.md", "TECH_SPEC.md", "agents", "dist", "docs", "examples", "history.xml", "node_modules", "package.json", "spaceplan.md", "spaces", "src", "tsconfig.json", "vite.config.web.ts", "vitest.config.ts"] }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: Array<string> [".claude", ".env", ".env.example", ... +15]
  </scope>
</turn>

<turn n="6" messages="12" finish="stop" model="glm-4.5">
  <usage input="4755" output="25" total="4780" reasoning="1" cache-read="1522" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name        Type           Value
----------  -------------  --------------------------------------------------
input       Object         {promptType, details}
knowledge   Object         {codebase}
result      Object         {name, aliases, path, description, keyFiles}
files       Array<string>  [".claude", ".env", ".env.example", ... +15]
agentFiles  Array<string>  ["thing.tsx"]

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var agentFiles = await listDir("agents", { recursive: true })

await stop(agentFiles)
  </message>
  <message role="user">
← stop { agentFiles: ["thing.tsx"] }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: Array<string> [".claude", ".env", ".env.example", ... +15]
    agentFiles: Array<string> ["thing.tsx"]
  </scope>
</turn>

<turn n="7" messages="14" finish="stop" model="glm-4.5">
  <usage input="4806" output="600" total="5406" reasoning="1" cache-read="1520" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name           Type           Value
-------------  -------------  --------------------------------------------------
input          Object         {promptType, details}
knowledge      Object         {codebase}
result         Object         {name, aliases, path, description, keyFiles}
files          Array<string>  [".claude", ".env", ".env.example", ... +15]
agentFiles     Array<string>  ["thing.tsx"]
promptContent  string         "/**\n * THING Agent — lmthing/repl Agent Build...

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var promptContent = await readFile("agents/thing.tsx")

await stop(promptContent)
  </message>
  <message role="user">
← stop { promptContent: "/**\n * THING Agent — lmthing/repl Agent Builder\n *\n * A specialist agent that helps users design and build @lmthing/repl agents.\n * Three functions: addFunction, addComponent, addInstruct — each writes to agents/<agentName>.tsx.\n *\n * Run:\n *   npx tsx src/cli/bin.ts agents/thing.tsx -m openai:gpt-4o\n *   npx tsx src/cli/bin.ts agents/thing.tsx -m anthropic:claude-sonnet-4-20250514\n */\n\nimport { writeFileSync, readFileSync, existsSync, mkdirSync } from 'fs'\nimport { join, dirname } from 'path'\n\n// ── State ──\n\nexport let agentName = 'my-agent'\n\n// ── Internal helpers ──\n\nfunction getAgentPath(): string {\n  return join(process.cwd(), 'agents', `${agentName}.tsx`)\n}\n\nfunction ensureFile(): void {\n  const p = getAgentPath()\n  const dir = dirname(p)\n  if (!existsSync(dir)) mkdirSync(dir, { recursive: true })\n  if (!existsSync(p)) writeFileSync(p, \"import React from 'react'\\n\\n\", 'utf-8')\n}\n\n// ── Exported functions ──\n\n/** Append a TypeScript function to the agent file */\nexport function a... (truncated, 5795 chars total)" }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: Array<string> [".claude", ".env", ".env.example", ... +15]
    agentFiles: Array<string> ["thing.tsx"]
    promptContent: string "/**\n * THING Agent — lmthing/repl Agent Build...
  </scope>
</turn>

<turn n="8" messages="16" finish="stop" model="glm-4.5">
  <usage input="5149" output="52" total="5201" reasoning="1" cache-read="1520" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name           Type           Value
-------------  -------------  --------------------------------------------------
input          Object         {promptType, details}
knowledge      Object         {codebase}
result         Object         {name, aliases, path, description, keyFiles}
files          Array<string>  [".claude", ".env", ".env.example", ... +15]
agentFiles     Array<string>  ["codebase", "cooking", "cooking/agents", ... +...
promptContent  string         "/**\n * THING Agent — lmthing/repl Agent Build...

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var agentFiles = await listDir("spaces", { recursive: true })

await stop(agentFiles)

var agentFiles = await glob("**/agent*.ts", { cwd: "." })

await stop(agentFiles)
  </message>
  <message role="user">
← stop { agentFiles: ["codebase", "cooking", "cooking/agents", "cooking/components", "cooking/flows", "cooking/functions", "cooking/knowledge", "cooking/package.json", "cooking/knowledge/cuisine", "cooking/knowledge/dietary", "cooking/knowledge/technique", "cooking/knowledge/technique/config.json", "cooking/knowledge/technique/method", "cooking/knowledge/technique/method/braise.md", "cooking/knowledge/technique/method/config.json", "cooking/knowledge/technique/method/grill.md", "cooking/knowledge/technique/method/saute.md", "cooking/knowledge/dietary/config.json", "cooking/knowledge/dietary/restriction", "cooking/knowledge/dietary/restriction/config.json", "cooking/knowledge/dietary/restriction/dairy-free.md", "cooking/knowledge/dietary/restriction/gluten-free.md", "cooking/knowledge/dietary/restriction/vegetarian.md", "cooking/knowledge/cuisine/config.json", "cooking/knowledge/cuisine/type", "cooking/knowledge/cuisine/type/config.json", "cooking/knowledge/cuisine/type/italian.md", "cooking/knowledge/cuisine/type/japanese.md", "cooking/knowledge/cuisine/type/mexican.md", "cooking/functions/buildGroceryList.tsx", "cooking/functions/estimateCalories.tsx", "cooking/functions/formatTime.tsx", "cooking/functions/scaleIngredients.tsx", "cooking/flows/meal_plan", "cooking/flows/recipe_create", "cooking/flows/technique_learn", "cooking/flows/technique_learn/1.Identify Technique.md", "cooking/flows/technique_learn/2.Explain Core Principles.md", "cooking/flows/technique_learn/3.Practice Exercises.md", "cooking/flows/technique_learn/4.Reference Recipe.md", "cooking/flows/technique_learn/index.md", "cooking/flows/recipe_create/1.Select Cuisine and Dish.md", "cooking/flows/recipe_create/2.Gather Ingredients.md", "cooking/flows/recipe_create/3.Prepare Mise en Place.md", "cooking/flows/recipe_create/4.Cook Step by Step.md", "cooking/flows/recipe_create/5.Analyze Nutrition.md", "cooking/flows/recipe_create/index.md", "cooking/flows/meal_plan/1.Define Meal Preferences.md", "cooking/flows/meal_plan/2.Design Weekly Menu.md", "cooking/flows/meal_plan/3.Create Prep Schedule.md", ... +77 more] }
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: Array<string> [".claude", ".env", ".env.example", ... +15]
    agentFiles: Array<string> ["codebase", "cooking", "cooking/agents", ... +...
    promptContent: string "/**\n * THING Agent — lmthing/repl Agent Build...
  </scope>
</turn>

<turn n="9" messages="18" finish="stop" model="glm-4.5">
  <usage input="5794" output="159" total="5953" reasoning="135" cache-read="1595" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name           Type           Value
-------------  -------------  --------------------------------------------------
input          Object         {promptType, details}
knowledge      Object         {codebase}
result         Object         {name, aliases, path, description, keyFiles}
files          Array<string>  [".claude", ".env", ".env.example", ... +15]
agentFiles     Array<string>  ["codebase", "cooking", "cooking/agents", ... +...
promptContent  string         "/**\n * THING Agent — lmthing/repl Agent Build...
promptFiles    undefined      undefined

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/findPackage.tsx)
  findPackage(query: string): { name: string; aliases: string[]; path: string; description: string; keyFiles: string[]; } | { matches: { name: string; path: string; description: string; }[]; error?: undefined; } | { error: string; matches?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/listEndpoints.tsx)
  listEndpoints(category?: string): { name: string; method: string; purpose: string; auth: string; category: string; }[] | { error: string; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/resolveImport.tsx)
  resolveImport(alias: string): { alias: string; resolvedPath: string; description: string; error?: undefined; } | { error: string; alias?: undefined; resolvedPath?: undefined; description?: undefined; }
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/functions/formatDependencyTree.tsx)
  formatDependencyTree(packageName: string): { error: string; package?: undefined; dependsOn?: undefined; dependedBy?: undefined; impactLevel?: undefined; warning?: undefined; } | { package: string; dependsOn: string[]; dependedBy: string[]; impactLevel: string; warning: string; error?: undefined; }

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/PackageCard.tsx)
  <PackageCard name={string} path={string} description={string} keyFiles={string[]} dependsOn?={string[]} dependedBy?={string[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/FileTreeCard.tsx)
  <FileTreeCard title={string} root={string} entries={{ path: string; annotation?: string; highlight?: boolean; }[]} />
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/codebase/components/view/EndpointCard.tsx)
  <EndpointCard name={string} method={string} purpose={string} auth={string} category={string} requestBody?={string} responseFormat?={string} />

## Knowledge Tree
knowledge/
└── codebase/
    ├── packages/          📦 Packages — Monorepo packages organized by area — shared libs, cloud backend, and product apps
    │   └── area             [select] codebaseArea — Which area of the monorepo to explore
    │       ├── shared-libs       Shared Libraries — Core libraries in org/libs/ used across all product domains
    │       ├── cloud-backend       Cloud Backend — Supabase Edge Functions in cloud/ — the sole backend for all lmthing products
    │       └── product-apps       Product Apps — Frontend product domains — each TLD has its own directory and Vite dev server
    ├── patterns/          🏗️ Code Patterns — Conventions, patterns, and architectural decisions used across the codebase
    │   └── type             [multiSelect] patternType — Which pattern or convention to explore
    │       ├── agent-system       Agent System — Space architecture — agents, flows, knowledge, naming conventions, and runtime behavior
    │       ├── state-management       State Management — Virtual file system, React context hierarchy, hooks, and workspace sync patterns
    │       └── authentication       Authentication — Cross-domain SSO flow, GitHub OAuth, JWT and API key auth, session management
    └── stack/          🔧 Tech Stack — Technology layers — frontend frameworks, backend runtime, and infrastructure
        └── layer             [multiSelect] stackLayer — Which layer of the technology stack to explore
            ├── frontend       Frontend Stack — React 19, Vite 7, TanStack Router, Tailwind 4, and shared UI libraries
            ├── backend       Backend Stack — Supabase Edge Functions (Deno), PostgreSQL with RLS, Stripe billing integration
            └── infrastructure       Infrastructure — Fly.io containers, pnpm workspaces, nginx proxy, GitHub sync, and deployment

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
# Navigator — Codebase Guide

You are the **Navigator**, a codebase orientation specialist for the lmthing monorepo. You help developers find files, understand package relationships, discover endpoints, and navigate the project structure.

## Behavior

1. When a user asks a question, start with `ask()` using a form component to clarify what they're looking for (a package, a file, an endpoint, or a general area).
2. Use `loadKnowledge()` to load relevant knowledge from the space based on the user's query area.
3. Call `await stop()` to read the loaded knowledge content.
4. Use utility functions (`findPackage`, `listEndpoints`, `resolveImport`, `formatDependencyTree`) to compute structured answers.
5. Display results using the appropriate component (`PackageCard`, `FileTreeCard`, `EndpointCard`).
6. Add a tasklist when the user's question requires exploring multiple areas.

## Rules

- Always load knowledge BEFORE answering — don't guess when the knowledge base has the answer.
- Never load all files from the space — only load the specific options relevant to the user's question.
- When pointing to files, always include the full path from the monorepo root.
- Explain the *purpose* of each package/file, not just its location.
- If the user asks about something that spans multiple domains (e.g., "how does auth work end-to-end"), load knowledge from multiple domains and connect the dots.

## Display Components

- **PackageCard** — Show package name, path, description, key files, and dependencies
- **FileTreeCard** — Show directory structure with annotations
- **EndpointCard** — Show API endpoint details (method, path, auth, purpose)

## Utility Functions

- `findPackage(name)` — Resolve a package name to its path and metadata
- `listEndpoints()` — List all cloud edge function endpoints with methods and purposes
- `resolveImport(alias)` — Resolve a `@lmthing/*` import to its actual file path
- `formatDependencyTree(packageName)` — Show what a package depends on and what depends on it

</system-prompt>

  <message role="assistant">
var promptFiles = await glob("**/*prompt*", { cwd: "." })

await stop(promptFiles)
  </message>
  <message role="user">
← error [TypeError] The "paths[1]" argument must be of type string. Received an instance of Object (line 16)
  </message>
  <scope>
    input: Object {promptType, details}
    knowledge: Object {codebase}
    result: Object {name, aliases, path, description, keyFiles}
    files: Array<string> [".claude", ".env", ".env.example", ... +15]
    agentFiles: Array<string> ["codebase", "cooking", "cooking/agents", ... +...
    promptContent: string "/**\n * THING Agent — lmthing/repl Agent Build...
    promptFiles: undefined undefined
  </scope>
</turn>

<turn n="10" messages="20" finish="stop" model="glm-4.5">
  <usage input="5856" output="40" total="5896" reasoning="24" cache-read="1631" />
</turn>

<finalize model="zai:glm-4.5" turns="10" status="complete">
  <tokens input="48819" output="1575" total="50394" />
</finalize>

</debug-log>