---
description: "Generates detailed descriptions of UI components present on the identified page"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

List and describe every key UI component that appears on the selected page. For each component, provide its code identifier (C1-C10), name, what it does on this specific page, and where it sits in the layout. Reference the component library: C1 Application Header, C2 Sidebar Navigation, C3 Card Grid, C4 Empty State, C5 Pill/Tag Bar, C6 Markdown Editor, C7 Tree Explorer, C8 Modal Dialog, C9 Floating Action Button, C10 Sliding Side Panel.

<output target="componentBreakdown">
{
  "type": "object",
  "properties": {
    "components": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "code": { "type": "string", "description": "Component code (C1-C10)" },
          "name": { "type": "string", "description": "Component name" },
          "role": { "type": "string", "description": "What this component does on this specific page" },
          "location": { "type": "string", "description": "Where it appears in the layout" }
        }
      }
    }
  },
  "required": ["components"]
}
</output>