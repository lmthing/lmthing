---
description: "Select the optimal Fly.io region for the computer node"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Help the user select the best Fly.io region for their computer based on their location, user base, and data sovereignty requirements.

Available regions:
- **IAD** (US East) — Default. Best for Americas, lowest inter-service latency.
- **LHR** (London) — Best for UK/Western Europe, GDPR considerations.
- **AMS** (Amsterdam) — Best for Central Europe, excellent peering.
- **SJC** (US West) — Best for West Coast, Asia-Pacific connectivity.

Consider the user's physical location, where their users are, and any regulatory requirements. When uncertain, recommend IAD as the default with the best connectivity to other lmthing services.

<output target="regionSelection">
{
  "type": "object",
  "properties": {
    "region": { "type": "string", "enum": ["iad", "lhr", "ams", "sjc"], "description": "Selected Fly.io region code" },
    "regionName": { "type": "string", "description": "Human-readable region name (e.g., US East)" },
    "reasoning": { "type": "string", "description": "Why this region was selected" },
    "latencyNotes": { "type": "string", "description": "Expected latency characteristics for the user" }
  },
  "required": ["region", "regionName", "reasoning"]
}
</output>
