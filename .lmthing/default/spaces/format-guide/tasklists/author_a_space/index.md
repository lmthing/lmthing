---
input:
  project: string
  spaceId: string
---
Walk the creation of a complete space — functions, knowledge, an agent, and this tasklist pattern itself — ending in a validation gate that proves the space is usable, not merely parseable.
