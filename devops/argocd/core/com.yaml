apiVersion: apps/v1
kind: Deployment
metadata:
  name: com
  namespace: lmthing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: com
  template:
    metadata:
      labels:
        app: com
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: com
          image: lmthingacr.azurecr.io/com:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: com
  namespace: lmthing
spec:
  selector:
    app: com
  ports:
    - port: 80
      targetPort: 80
