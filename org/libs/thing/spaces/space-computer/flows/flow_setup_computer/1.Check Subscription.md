---
description: "Verify the user has an active Computer tier subscription"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Verify the user has an active Computer tier Stripe subscription. The computer requires a paid subscription — without it, provisioning will fail.

Check subscription status via the get-usage endpoint. If the user is not subscribed, generate instructions to create a checkout session with the Computer tier price_id via create-checkout.

Explain what the Computer tier includes: everything in Pro (full model catalog, higher token limits) plus a dedicated Fly.io machine with terminal access, 1 GB persistent storage, and start/stop control.

<output target="subscriptionStatus">
{
  "type": "object",
  "properties": {
    "isActive": { "type": "boolean", "description": "Whether the user has an active Computer tier subscription" },
    "tier": { "type": "string", "enum": ["free", "pro", "computer"], "description": "Current subscription tier" },
    "canProvision": { "type": "boolean", "description": "Whether the user can proceed with provisioning" },
    "action": { "type": "string", "description": "Required action if not subscribed (e.g., checkout URL or confirmation to proceed)" }
  },
  "required": ["isActive", "tier", "canProvision"]
}
</output>
