---
description: "Generates a structured space brief capturing the subject, target audience, and core use cases"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

You are helping a user define the subject for a new lmthing space. Your goal is to produce a clear, structured space brief that will guide all subsequent creation steps.

Gather the following from the user's input and any prior conversation context:

1. **Subject** — What is this space about? Identify the core domain (e.g., "meal planning", "code review", "customer onboarding").
2. **Audience** — Who will use this space? Be specific about roles, skill levels, and contexts.
3. **Use cases** — What are 2-3 core tasks this space should help with? Each should be a concrete action, not a vague goal.
4. **Expert knowledge** — What does a domain expert know that the agents in this space should encode? Think about terminology, decision frameworks, reference data, and user preferences.

Calibrate depth based on the creator's experience level from their context:
- Beginners: Help narrow a broad idea into something concrete and achievable
- Intermediate: Validate the concept and suggest refinements or gaps
- Advanced: Confirm scope quickly and identify architectural considerations

Synthesize everything into a space brief.

<output target="subject">
{
  "type": "object",
  "properties": {
    "name": { "type": "string", "description": "Short name for the space in kebab-case (e.g., meal-planner)" },
    "subject": { "type": "string", "description": "One-sentence description of the space's core domain" },
    "audience": { "type": "string", "description": "Description of the target users and their context" },
    "useCases": { "type": "array", "items": { "type": "string" }, "description": "2-3 core tasks this space enables" },
    "expertKnowledge": { "type": "array", "items": { "type": "string" }, "description": "Key knowledge areas the space should encode" },
    "brief": { "type": "string", "description": "One-paragraph summary of the space's purpose, audience, and scope" }
  },
  "required": ["name", "subject", "audience", "useCases", "brief"]
}
</output>
