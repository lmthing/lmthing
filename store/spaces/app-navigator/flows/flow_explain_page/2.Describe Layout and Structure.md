---
description: "Generates layout analysis for the identified page including column structure and content zones"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Analyze the identified page and describe its layout pattern. Determine which layout type it uses: L1 Single-Column Centered (Home Page), L2 Three-Column Dashboard (Studio Dashboard, Agent Config), or L3 Two-Column Split (Knowledge Area Details). Explain the column arrangement, how content zones interact, and why this layout suits the page's purpose. Describe the visual hierarchy and spatial relationships.

<output target="layoutAnalysis">
{
  "type": "object",
  "properties": {
    "layoutType": {
      "type": "string",
      "description": "Layout pattern identifier (L1, L2, or L3)"
    },
    "layoutName": {
      "type": "string",
      "description": "Human-readable layout name"
    },
    "columns": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "position": { "type": "string" },
          "width": { "type": "string" },
          "purpose": { "type": "string" }
        }
      },
      "description": "Column definitions with position, sizing, and purpose"
    },
    "rationale": {
      "type": "string",
      "description": "Why this layout suits the page content"
    }
  },
  "required": ["layoutType", "layoutName", "columns", "rationale"]
}
</output>