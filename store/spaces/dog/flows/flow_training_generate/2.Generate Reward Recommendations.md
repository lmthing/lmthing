---
description: "Generates a reward strategy with treat tiers, non-food rewards, and timing guidance"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate a personalized reward strategy for the dog's training plan. Create a tiered reward system: high-value treats for new or difficult skills (freeze-dried liver, boiled chicken, cheese for tolerant dogs), low-value treats for reinforcing known commands (kibble, commercial mini-treats), and non-food rewards (verbal praise, head scratches, favorite toy, brief tug-of-war). Include timing guidance for reward delivery (immediate marking within 1-2 seconds), fading schedules for transitioning from continuous to intermittent reinforcement, and portion management to avoid overfeeding during training sessions.

<output target="rewardStrategy">
{
  "type": "object",
  "properties": {
    "highValueTreats": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "treat": { "type": "string" },
          "useCase": { "type": "string" },
          "portionSize": { "type": "string" }
        }
      }
    },
    "lowValueTreats": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "treat": { "type": "string" },
          "useCase": { "type": "string" }
        }
      }
    },
    "nonFoodRewards": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "reward": { "type": "string" },
          "effectiveness": { "type": "string" }
        }
      }
    },
    "timingGuidance": {
      "type": "string",
      "description": "How quickly to deliver rewards after desired behavior"
    },
    "fadingSchedule": {
      "type": "string",
      "description": "How to transition from continuous to intermittent reinforcement"
    }
  },
  "required": ["highValueTreats", "lowValueTreats", "nonFoodRewards", "timingGuidance"]
}
</output>