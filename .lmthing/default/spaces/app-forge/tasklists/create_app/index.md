---
input:
  projectId: string
  purpose: string
---
Create a project app end to end: scaffold the documented layout, author tables and endpoints in
parallel, then the pages that bind to them, wire the automation, and prove the app against the
done-gate before calling it built.
