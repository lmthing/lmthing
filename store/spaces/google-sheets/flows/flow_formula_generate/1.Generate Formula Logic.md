---
description: "Generates the core logical approach for solving the user's spreadsheet problem"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Analyze the user's spreadsheet problem and design the core formula logic. Break down the problem into logical steps, identify which Google Sheets functions are needed (VLOOKUP, INDEX/MATCH, ARRAYFORMULA, QUERY, etc.), determine if nested functions or helper columns are required, and outline the data flow from input cells to output. Consider the user's data size and structure from knowledge context.

<output target="formulaLogic">
{
  "type": "object",
  "properties": {
    "problemSummary": {
      "type": "string",
      "description": "Concise restatement of the user's problem"
    },
    "logicalSteps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Ordered logical steps to solve the problem"
    },
    "functionsRequired": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Google Sheets functions needed (e.g., VLOOKUP, IF, ARRAYFORMULA)"
    },
    "complexity": {
      "type": "string",
      "enum": ["simple", "moderate", "complex"],
      "description": "Overall formula complexity level"
    },
    "approach": {
      "type": "string",
      "description": "High-level description of the solution approach"
    }
  },
  "required": ["problemSummary", "logicalSteps", "functionsRequired", "complexity", "approach"]
}
</output>