---
description: "Generates a competitive analysis comparing the listing against similar agents"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

You are analyzing the competitive landscape for an agent listing on the lmthing store. Compare the listing against similar agents to identify opportunities for differentiation.

Analyze competitors across these dimensions:

1. **Descriptions** — How do top-performing similar agents structure and write their listings? What hooks work well? What formatting patterns are common?
2. **Pricing** — What do comparable agents charge? Is our agent priced higher or lower? Is there a price gap no one is targeting?
3. **Distribution model** — Are competitors using the same model? Would switching to a different model provide an advantage?
4. **Reviews and downloads** — What do popular agents in this category do well? What common complaints exist across the category?
5. **Feature gaps** — What capabilities do competitors lack that this agent could highlight?

Identify specific, actionable opportunities:
- Market gaps that this agent could fill
- Differentiators that aren't being communicated effectively
- Competitive weaknesses to exploit in positioning

Do not recommend copying competitors — recommend learning from patterns and finding a unique angle.

<output target="competitiveAnalysis">
{
  "type": "object",
  "properties": {
    "category": { "type": "string", "description": "The store category or niche being analyzed" },
    "competitorCount": { "type": "number", "description": "Number of competing agents in the same category" },
    "topCompetitors": { "type": "array", "items": { "type": "object", "properties": { "name": { "type": "string" }, "model": { "type": "string" }, "pricePoint": { "type": "string" }, "strengths": { "type": "array", "items": { "type": "string" } }, "weaknesses": { "type": "array", "items": { "type": "string" } } } }, "description": "Analysis of top 3-5 competing agents" },
    "pricingLandscape": { "type": "string", "description": "Overview of pricing across the category" },
    "marketGaps": { "type": "array", "items": { "type": "string" }, "description": "Opportunities no competitor is addressing" },
    "differentiationOpportunities": { "type": "array", "items": { "type": "string" }, "description": "Ways to stand out from competitors" },
    "recommendations": { "type": "string", "description": "Summary of recommended competitive positioning" }
  },
  "required": ["category", "competitorCount", "topCompetitors", "marketGaps", "differentiationOpportunities", "recommendations"]
}
</output>
