---
id: plan
output:
  angles: array
  count: number
dependsOn: []
goal: false
---

Split the `topic` you were given into exactly THREE distinct angles worth thinking about
separately. Each angle is a short noun phrase (at most six words) — no sentences, no numbering.

Return `angles` as the list of those three strings, and `count` as their number (3).
