---
id: scaffold
output:
  spaceId: string
---

Create the target space with create_space (project `default` unless told otherwise). Every later step addresses it by ref `<project>/<id>`. Record the created space id as `spaceId`.