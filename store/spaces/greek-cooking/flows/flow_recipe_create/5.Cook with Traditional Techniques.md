---
description: "Generates step-by-step cooking instructions using authentic Greek techniques"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Generate detailed cooking instructions using authentic Greek techniques. Apply the appropriate method: Yiahni (slow-braised in tomato sauce), Ladero (vegetables in olive oil), Avgolemono (egg-lemon sauce), Sto Fourno (oven-roasted), or grilling. Include heat management guidance (start aromatics in cold olive oil, build layers of flavor, add wine and let evaporate, tomatoes after onions soften). Provide timing for each step and sensory cues for doneness. Mention the Greek "secrets" — generous olive oil, fresh lemon at the end, and resting before serving.

<output target="cookingInstructions">
{
  "type": "object",
  "properties": {
    "technique": {
      "type": "string",
      "description": "Primary Greek cooking technique used"
    },
    "techniqueGreek": {
      "type": "string",
      "description": "Technique name in Greek"
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "stepNumber": { "type": "number" },
          "instruction": { "type": "string" },
          "duration": { "type": "string" },
          "heatLevel": { "type": "string" },
          "donenessCue": { "type": "string", "description": "Sensory indicator that this step is complete" }
        }
      }
    },
    "estimatedCookTime": {
      "type": "string",
      "description": "Total cooking time estimate"
    },
    "tips": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Technique-specific tips for authentic results"
    }
  },
  "required": ["technique", "steps", "estimatedCookTime"]
}
</output>