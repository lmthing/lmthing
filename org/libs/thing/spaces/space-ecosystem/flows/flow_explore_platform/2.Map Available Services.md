---
description: "Maps the 3-4 most relevant lmthing services to the user's identified goal"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Using the user's goal from the previous step, select and describe the 3-4 most relevant lmthing services. Do not cover all ten services — focus only on those that matter for this user's workflow.

Group the selected services by workflow stage:
- **Create** — Studio (build agents), Chat (test conversations)
- **Deploy** — Space (containers), Computer (personal node)
- **Share** — Store (marketplace), Social (public), Team (private)
- **Manage** — Com (account), Blog (research), Casa (home)

For each selected service, provide: a one-sentence description, the required tier (free, Pro, or Computer), and how it fits the user's specific workflow.

Exclude services that are not relevant to the user's goal. Prioritize services the user will interact with first.

<output target="serviceMap">
{
  "type": "object",
  "properties": {
    "relevantServices": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Service name (e.g., Studio, Chat, Space)" },
          "domain": { "type": "string", "description": "The lmthing.* domain (e.g., lmthing.studio)" },
          "workflowStage": { "type": "string", "description": "create, deploy, share, or manage" },
          "description": { "type": "string", "description": "One-sentence explanation of what this service does" },
          "requiredTier": { "type": "string", "description": "free, pro, or computer" },
          "relevanceToGoal": { "type": "string", "description": "How this service fits the user's specific workflow" }
        },
        "required": ["name", "domain", "workflowStage", "description", "requiredTier", "relevanceToGoal"]
      },
      "description": "The 3-4 services most relevant to the user's goal"
    },
    "minimumTier": { "type": "string", "description": "The lowest tier that covers all recommended services" }
  },
  "required": ["relevantServices", "minimumTier"]
}
</output>
