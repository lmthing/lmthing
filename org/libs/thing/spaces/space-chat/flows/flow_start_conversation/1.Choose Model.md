---
description: "Recommend the best model based on user's task and subscription tier"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Analyze the user's task requirements and subscription tier to recommend the optimal model.

For free tier users, choose between gpt-4o-mini (general questions, tool-heavy tasks) and claude-3-5-haiku (writing, concise analysis).

For Pro/Computer tier users, match from the full catalog: GPT-4o for speed and versatility, Claude Sonnet for quality writing, o3 for complex reasoning, Gemini Pro for long documents.

Evaluate the tradeoffs between speed vs capability and cost vs quality. Recommend the most efficient model that meets the task requirements — not every task needs a flagship model.

Output a model recommendation with reasoning.

<output target="modelSelection">
{
  "type": "object",
  "properties": {
    "recommendedModel": { "type": "string", "description": "The recommended model identifier" },
    "reasoning": { "type": "string", "description": "Why this model fits the user's task and tier" },
    "alternatives": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Alternative models the user could consider"
    },
    "tierRequired": { "type": "string", "enum": ["free", "pro", "computer"], "description": "Minimum subscription tier needed" }
  },
  "required": ["recommendedModel", "reasoning", "tierRequired"]
}
</output>
