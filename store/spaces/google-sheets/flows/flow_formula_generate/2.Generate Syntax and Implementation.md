---
description: "Generates the exact Google Sheets formula syntax with cell references and implementation notes"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Based on the formula logic, generate the exact Google Sheets formula syntax. Provide the complete formula with placeholder cell references the user can adapt, explain each nested function and its role, note whether ARRAYFORMULA wrapping is needed for range operations, and include any required helper formulas for intermediate calculations. Use proper Google Sheets syntax (not Excel).

<output target="formulaSyntax">
{
  "type": "object",
  "properties": {
    "primaryFormula": {
      "type": "string",
      "description": "The complete formula with cell reference placeholders"
    },
    "formulaBreakdown": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "segment": { "type": "string" },
          "explanation": { "type": "string" }
        }
      },
      "description": "Each nested function explained"
    },
    "helperFormulas": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "cell": { "type": "string" },
          "formula": { "type": "string" },
          "purpose": { "type": "string" }
        }
      },
      "description": "Any supporting formulas needed in helper columns"
    },
    "implementationNotes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Important notes about cell references, ranges, or sheet names"
    }
  },
  "required": ["primaryFormula", "formulaBreakdown", "implementationNotes"]
}
</output>