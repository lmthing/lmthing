---
description: "Generates main course selections balanced across proteins, with vegetarian options"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Select main courses based on the occasion, season, and guest parameters. Choose from lamb (kleftiko, yiouvetsi, chops), pork (tigania, souvlaki, roast), chicken (lemon chicken, stifado, kokkinisto), seafood (grilled fish, octopus, shrimp saganaki, bakaliaros), and vegetarian mains (moussaka, yemista, briam, gigantes plaki). Balance the menu with one protein-heavy main and one vegetable/grain option. For larger groups, offer both meat and seafood. Consider seasonal availability and the regional style selected.

<output target="mainCourses">
{
  "type": "object",
  "properties": {
    "dishes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "nameGreek": { "type": "string" },
          "category": { "type": "string", "enum": ["meat", "seafood", "vegetarian"] },
          "description": { "type": "string" },
          "cookingMethod": { "type": "string" },
          "servings": { "type": "number" }
        }
      }
    },
    "balanceNotes": {
      "type": "string",
      "description": "How the main course selection achieves balance"
    }
  },
  "required": ["dishes", "balanceNotes"]
}
</output>