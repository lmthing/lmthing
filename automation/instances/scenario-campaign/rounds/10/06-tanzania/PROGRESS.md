# PROGRESS — scenario-campaign · task `06-tanzania` · round 10

_Started 2026-07-15T23:43:07.049Z. The agent MUST update this file at every step._

## Steps

- Read the attempt ledger and confirmed round 10 must verify the existing L2 `organize_material` path through step 2.
- Rebuilt `@lmthing/cli` so the clean local server adopts the existing L2 tasklist and 20-minute harness timeout.
- Ran a clean replay through step 2: inventory found five stable scopes and invoked one architect build per scope, but four architect delegates tried to read an unbound `t` in a manual second turn, leaving only the last space registered. Fixed the architect action to emit only its action tasklist statement, letting the runtime return its envelope; added a prompt contract test and updated the system-spaces documentation. Targeted tests and `pnpm docs:check` pass.
- The first fix rerun registered all requested leg spaces but overproduced ten specialists by splitting out facts, photos, memories, shared overview, and cross-cutting finances. Tightened only the inventory L1 principle: those are not independent scopes; only independently owned locations/stages qualify, with an uninterrupted operational stage allowed to combine subparts. Rebuilt and rechecked before one final fresh step-2 replay.

## Files added to context

- `automation/instances/scenario-campaign/attempts/06-tanzania.md` — prior-step ladder history and mandatory escalation direction.
- `automation/instances/scenario-campaign/rounds/10/06-tanzania/PROGRESS.md` — required current-round progress record.
- `sdk/org/scenarios/06-tanzania/scenario.yaml` — execution and judging contract.
- `automation/instances/scenario-campaign/judge.md` — campaign-run and reporting rules.
- `sdk/org/libs/core/system-spaces/user-thing/agents/thing/instruct.md` — existing live-project routing context.
- Existing L2 tasklist and related diffs — confirmed that earlier work is present and preserved.
