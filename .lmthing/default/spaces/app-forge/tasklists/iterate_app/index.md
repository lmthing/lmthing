---
input:
  projectId: string
  query: string
---
Iterate a LIVE project app from evidence: inspect what exists, split the findings into must-fix and
polish, work the two lanes, and re-verify the whole app fresh from disk — an iteration ends honestly
or it does not end.
