---
id: functions
dependsOn:
  - scaffold
output:
  functions: array
---

Author each pure function with write_function and inspect the returned schema: arrays must carry items; a degraded verdict names the parameter to fix — improve the type instead of accepting it. Record the function names as `functions`.