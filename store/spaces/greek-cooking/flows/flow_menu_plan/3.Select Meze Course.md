---
description: "Generates meze course selection with cold and hot appetizers balanced for the occasion"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Design the meze course, which is essential in Greek dining — never serve main courses without appetizers first. Select 2-3 cold meze (prepare-ahead items like tzatziki, taramosalata, melitzanosalata, fava, htipiti, dolmades) and 1-2 hot meze (saganaki, kolokithokeftedes, spanakopita, keftedakia). Always include feta with oregano and olive oil, Kalamata olives, and crusty bread. Calculate quantities knowing guests will fill up on meze. Specify that meze is served immediately when guests arrive with ouzo, wine, or tsipouro.

<output target="mezeCourse">
{
  "type": "object",
  "properties": {
    "coldMeze": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "nameGreek": { "type": "string" },
          "description": { "type": "string" },
          "prepAhead": { "type": "boolean" }
        }
      }
    },
    "hotMeze": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "nameGreek": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    },
    "essentials": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Always-include items (feta, olives, bread)"
    },
    "drinkPairing": {
      "type": "string",
      "description": "Recommended drink to serve with meze"
    }
  },
  "required": ["coldMeze", "hotMeze", "essentials"]
}
</output>