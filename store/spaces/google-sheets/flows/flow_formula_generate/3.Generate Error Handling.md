---
description: "Generates error handling wrappers and edge case protections for the formula"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Wrap the generated formula with appropriate error handling. Identify potential error conditions (#N/A, #VALUE!, #REF!, #DIV/0!, blank cells, mismatched types) and generate IFERROR, IF(ISBLANK(...)), or other protective wrappers. Provide the enhanced formula with error handling included and explain what each protection guards against. Consider edge cases specific to the user's data type and structure.

<output target="errorHandling">
{
  "type": "object",
  "properties": {
    "enhancedFormula": {
      "type": "string",
      "description": "The formula with error handling wrappers applied"
    },
    "errorConditions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "errorType": { "type": "string", "description": "Error type (e.g., #N/A, #VALUE!)" },
          "cause": { "type": "string", "description": "What triggers this error" },
          "protection": { "type": "string", "description": "How the wrapper handles it" },
          "fallbackValue": { "type": "string", "description": "What displays instead of the error" }
        }
      }
    },
    "edgeCases": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Edge cases the user should be aware of"
    }
  },
  "required": ["enhancedFormula", "errorConditions", "edgeCases"]
}
</output>