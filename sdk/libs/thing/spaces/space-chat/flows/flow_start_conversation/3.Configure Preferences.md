---
description: "Determine the conversation mode and model settings"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Based on the user's task and context, determine the optimal conversation mode and temperature settings.

Available modes:
- **Casual** — Relaxed, exploratory. Good for brainstorming. Temperature range: 0.7-0.8.
- **Focused** — Task-oriented, structured. Good for specific questions. Temperature range: 0.3-0.5.
- **Creative** — Imaginative, bold. Good for writing and ideation. Temperature range: 0.8-1.0.

Select the mode that best matches the user's intent. Consider how the chosen model's strengths interact with the mode (e.g., Claude excels in creative mode, GPT-4o in focused mode).

If the user has explicit preferences, honor them. Otherwise, infer the best mode from the task description and context.

<output target="conversationPreferences">
{
  "type": "object",
  "properties": {
    "mode": { "type": "string", "enum": ["casual", "focused", "creative"], "description": "Selected conversation mode" },
    "temperature": { "type": "number", "description": "Specific temperature value within the mode's range" },
    "modeReasoning": { "type": "string", "description": "Why this mode was selected for the user's task" },
    "additionalSettings": { "type": "object", "description": "Any model-specific or mode-specific tuning parameters" }
  },
  "required": ["mode", "temperature", "modeReasoning"]
}
</output>
