apiVersion: apps/v1
kind: Deployment
metadata:
  name: space
  namespace: lmthing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: space
  template:
    metadata:
      labels:
        app: space
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: space
          image: lmthingacr.azurecr.io/space:e15285b
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: space
  namespace: lmthing
spec:
  selector:
    app: space
  ports:
    - port: 80
      targetPort: 80
