---
description: "Generates knowledge domain structure with fields and option hierarchies"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Based on the domain definition, generate 3-4 top-level knowledge domains that comprehensively cover the subject. For each domain, define sub-fields with their types (select, multiSelect, text, number) and initial options. Each domain should have a descriptive label, emoji icon, and hex color. Structure the knowledge so agents can pull rich, user-configured context at runtime.

<output target="knowledgeDomains">
{
  "type": "object",
  "properties": {
    "domains": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "slug": { "type": "string", "description": "kebab-case domain identifier" },
          "label": { "type": "string", "description": "Human-readable domain name" },
          "icon": { "type": "string", "description": "Emoji icon for the domain" },
          "color": { "type": "string", "description": "Hex color code" },
          "description": { "type": "string", "description": "What types of knowledge live here" },
          "fields": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "slug": { "type": "string" },
                "type": { "type": "string", "enum": ["select", "multiSelect", "text", "number"] },
                "options": { "type": "array", "items": { "type": "string" } }
              }
            }
          }
        }
      }
    }
  },
  "required": ["domains"]
}
</output>