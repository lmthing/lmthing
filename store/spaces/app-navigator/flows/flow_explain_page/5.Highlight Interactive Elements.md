---
description: "Generates a catalog of all interactive elements on the page with their triggers and visual signals"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Catalog every interactive element on the selected page. Group them by interaction type: modal-triggering actions, navigation actions, toggle/panel actions, contextual menus, and editor/chat interactions. For each element, specify the trigger (click, hover, etc.), what it opens or changes, and how the interaction is visually signalled (hover shadows, color changes, icon transitions, badge updates).

<output target="interactiveElements">
{
  "type": "object",
  "properties": {
    "modalTriggers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "element": { "type": "string" },
          "trigger": { "type": "string" },
          "result": { "type": "string" },
          "visualSignal": { "type": "string" }
        }
      }
    },
    "navigationActions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "element": { "type": "string" },
          "destination": { "type": "string" },
          "visualSignal": { "type": "string" }
        }
      }
    },
    "toggleActions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "element": { "type": "string" },
          "states": { "type": "string" },
          "visualSignal": { "type": "string" }
        }
      }
    },
    "contextMenus": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "element": { "type": "string" },
          "options": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  },
  "required": ["modalTriggers", "navigationActions", "toggleActions"]
}
</output>