---
type: string
variable: fileType
default: instruct-md
label: File Type
fieldType: select
required: false
renderAs: field
---

Which workspace file format to reference. Each option provides the complete schema, field-by-field rules, validation checklist, and annotated examples for that file type. Options include `instruct.md` (agent identity), `package.json` (workspace manifest), domain config, field config, flow index, and option markdown files.
