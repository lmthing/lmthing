---
description: "Generates a test plan and validation checklist for the completed workspace"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate a comprehensive test plan for the newly built workspace. Include validation checks for workspace structure (agent count, flow count, knowledge area count), configuration validity (runtime fields, knowledge attachments), sample test messages for each agent, slash command tests for each flow, and expected behavior descriptions. Produce a checklist that the user can work through to verify everything functions correctly.

<output target="testPlan">
{
  "type": "object",
  "properties": {
    "structureChecks": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Validation items for workspace structure"
    },
    "agentTests": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "agentName": { "type": "string" },
          "sampleMessage": { "type": "string" },
          "expectedBehavior": { "type": "string" }
        }
      }
    },
    "flowTests": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "slashCommand": { "type": "string" },
          "expectedStepCount": { "type": "number" },
          "expectedOutput": { "type": "string" }
        }
      }
    }
  },
  "required": ["structureChecks", "agentTests", "flowTests"]
}
</output>