---
description: "Recommends the best service to start with and provides a concrete first action"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Based on the user's goal and the mapped services, recommend the single best service to start with. Apply these heuristics:

- Most users should start with **Studio** — build a first agent, explore the workspace, test conversations
- Developers wanting API access should start with **com/** for API keys, then use generate-ai directly
- Users wanting quick conversations should start with **Chat** — no setup needed
- Home automation users should start with **Casa** (requires Computer tier)

Provide a concrete, actionable first step: the exact URL to visit, the first action to take when they arrive, and what they will see. Remove all ambiguity about what to do next.

Also outline the recommended sequence for the remaining services (steps 2, 3, etc.) so the user has a clear path forward.

<output target="startingPoint">
{
  "type": "object",
  "properties": {
    "recommendedService": { "type": "string", "description": "The service name to start with" },
    "url": { "type": "string", "description": "The URL to visit (e.g., https://lmthing.studio)" },
    "firstAction": { "type": "string", "description": "The exact first thing to do when they arrive" },
    "whatToExpect": { "type": "string", "description": "What the user will see and experience" },
    "nextSteps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "order": { "type": "number", "description": "Sequence number (2, 3, 4...)" },
          "service": { "type": "string", "description": "Service name" },
          "action": { "type": "string", "description": "What to do in this service" }
        },
        "required": ["order", "service", "action"]
      },
      "description": "Ordered list of subsequent services and actions"
    }
  },
  "required": ["recommendedService", "url", "firstAction", "whatToExpect", "nextSteps"]
}
</output>
