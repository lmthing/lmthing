---
description: "Generates a follow-up checkpoint schedule for symptom re-evaluation"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Based on the plant snapshot and weekly care actions, recommend when the user should reassess the plant's health and what specific symptoms or changes to observe. Include timing for the first check-in, warning signs that require immediate action, and positive indicators that the care plan is working.

<output target="followUpPlan">
{
  "type": "object",
  "properties": {
    "firstCheckIn": {
      "type": "string",
      "description": "When to first reassess (e.g., '3 days', '1 week')"
    },
    "observationPoints": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Specific things to look for during check-in"
    },
    "warningSignals": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Symptoms requiring immediate action"
    },
    "positiveIndicators": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Signs the care plan is working"
    }
  },
  "required": [
    "firstCheckIn",
    "observationPoints",
    "warningSignals"
  ]
}
</output>
