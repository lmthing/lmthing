---
description: "Generates a log analysis report for spaces with health issues"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

You are analyzing logs for spaces that showed health issues or failures in the previous steps. Examine machine logs for errors, warnings, and unusual patterns.

Focus your analysis on:
1. **Error messages and stack traces** — Application-level issues
2. **Out-of-memory (OOM) events** — Machine exceeded its 1 GB RAM limit
3. **Crash loops** — Process keeps crashing and restarting
4. **Connection failures** — Issues reaching external services
5. **Health check failures** — Logged when /health doesn't respond in time

Log sources to examine:
- Machine logs via K8s dashboard or kubectl
- Application logs within the container
- Health check results from K8s monitoring

Correlate log entries with the reported status and any timing patterns. Issues at specific times may be triggered by scheduled tasks, traffic spikes, or external service outages.

Produce a structured diagnosis for each problematic space.

<output target="logAnalysis">
{
  "type": "object",
  "properties": {
    "analyzedSpaces": { "type": "array", "items": { "type": "object", "properties": { "slug": { "type": "string" }, "issueType": { "type": "string", "enum": ["error", "oom", "crash-loop", "connection-failure", "health-timeout", "unknown"] }, "rootCause": { "type": "string" }, "logEvidence": { "type": "array", "items": { "type": "string" } }, "severity": { "type": "string", "enum": ["critical", "high", "medium", "low"] }, "timingPattern": { "type": "string" } } }, "description": "Log analysis for each problematic space" },
    "correlations": { "type": "array", "items": { "type": "string" }, "description": "Cross-space patterns or shared root causes" },
    "summary": { "type": "string", "description": "Overall log analysis findings" }
  },
  "required": ["analyzedSpaces", "summary"]
}
</output>
