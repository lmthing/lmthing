---
description: "Generates health check results for all running spaces"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

You are performing health checks on all running spaces identified in the previous step. For each running space, verify that health checks are passing.

Check each running space's health endpoint at `{fly_app_name}.fly.dev/health`:
- A healthy space responds with 200 status within 5 seconds
- Automatic health checks run every 10 seconds

Identify signs of trouble for each space:
- **No response** — Machine may be crashed or overloaded
- **Slow responses (>3 seconds)** — Approaching timeout, possible resource pressure
- **Intermittent failures** — Unstable state, may need restart

For stopped spaces, note that health checks don't apply. For failed spaces, the machine is likely not responding at all.

Produce a health report for each running space with a clear pass/warn/fail assessment.

<output target="healthReport">
{
  "type": "object",
  "properties": {
    "checkedAt": { "type": "string", "description": "ISO timestamp of when health checks were performed" },
    "results": { "type": "array", "items": { "type": "object", "properties": { "slug": { "type": "string" }, "healthUrl": { "type": "string" }, "status": { "type": "string", "enum": ["healthy", "degraded", "unhealthy", "unreachable"] }, "responseTimeMs": { "type": "number" }, "details": { "type": "string" } } }, "description": "Health check results for each running space" },
    "healthyCount": { "type": "number", "description": "Number of spaces passing health checks" },
    "unhealthyCount": { "type": "number", "description": "Number of spaces failing or degraded" },
    "overallAssessment": { "type": "string", "description": "Summary assessment of fleet health" }
  },
  "required": ["checkedAt", "results", "healthyCount", "unhealthyCount", "overallAssessment"]
}
</output>
