/**
 * SettingsView - Workspace settings panel (env files, configuration).
 * Re-exported from shell/components/SettingsView for the new component structure.
 */
export { SettingsView } from '@/shell/components/SettingsView'
