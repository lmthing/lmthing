---
description: "Compares subscription tiers and recommends the best plan for the user's needs"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Compare the three lmthing subscription tiers in the context of the user's current usage and goals. Provide an objective comparison, then make a personalized recommendation.

Tier details:
- **Free** — No cost. Basic models (gpt-4o-mini, claude-3-5-haiku). Limited monthly token budget. Full Studio and Chat access. Best for exploring and learning.
- **Pro** — Full model catalog including GPT-4o, Claude Opus, Gemini Pro. Higher token limits. Full Blog access. Store publishing. Best for regular agent builders and daily users.
- **Computer** — Everything in Pro plus a dedicated Fly.io machine (1 CPU, 1 GB RAM, terminal access). Best for power users needing persistent runtimes, autonomous agents, or cloud development environments.

Based on the user's current tier, usage patterns, and stated needs, recommend whether they should stay on their current plan or change. Explain the reasoning clearly.

<output target="planComparison">
{
  "type": "object",
  "properties": {
    "tiers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "free, pro, or computer" },
          "models": { "type": "string", "description": "Available model list summary" },
          "tokenLimit": { "type": "string", "description": "Monthly token budget description" },
          "keyFeatures": { "type": "array", "items": { "type": "string" }, "description": "Top 3 features of this tier" },
          "bestFor": { "type": "string", "description": "User profile this tier suits best" }
        },
        "required": ["name", "models", "tokenLimit", "keyFeatures", "bestFor"]
      },
      "description": "All three tier summaries"
    },
    "recommendedTier": { "type": "string", "description": "The tier recommended for this user" },
    "reasoning": { "type": "string", "description": "Why this tier is the best fit" },
    "upgradeWorthIt": { "type": "boolean", "description": "Whether upgrading from current tier is recommended" }
  },
  "required": ["tiers", "recommendedTier", "reasoning", "upgradeWorthIt"]
}
</output>
