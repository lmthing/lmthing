---
label: UI Components
description: The reusable UI components that make up the lmthing interface
icon: 🧩
color: "#0ea5e9"
renderAs: section
---

The UI Components domain covers all reusable interface building blocks in the lmthing platform — from navigation shells and content grids to overlay dialogs and the Thing assistant panel. Use this domain to understand which components appear on a given page, how they behave, and how they interact with each other.
