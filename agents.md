# Agent Notes

This repository is a monorepo.

## Packages

- `app/` — Vite/React application (UI, mock data, docs, and end-to-end tests).
  - Agent guidance: `app/agents.md`
  - Additional context: `app/claude.md`

- `lib/core/` — Core TypeScript library and CLI for building/running AI agents.
  - Agent guidance: `lib/core/CLAUDE.md`
