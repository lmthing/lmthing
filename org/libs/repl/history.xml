<?xml version="1.0" encoding="UTF-8"?>
<debug-log>

<message role="assistant">
[setup] tasklist("mealplan", "Creates a weekly meal plan with balanced nutrition, grocery list, and prep schedule", [
{
  "id": "define-meal-preferences",
  "instructions": "Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.",
  "outputSchema": {
    "people": {
      "type": "number",
      "description": "Number of people to feed"
    },
    "mealsPerDay": {
      "type": "number",
      "description": "Number of meals to plan per day"
    },
    "cuisinePreferences": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Preferred cuisine types"
    },
    "dietaryRestrictions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"
    },
    "weekdayCookingTime": {
      "type": "string",
      "description": "Available cooking time on weekdays (e.g., '30 minutes')"
    },
    "weekendCookingTime": {
      "type": "string",
      "description": "Available cooking time on weekends (e.g., '1-2 hours')"
    },
    "nutritionalGoal": {
      "type": "string",
      "description": "Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"
    },
    "calorieTarget": {
      "type": "number",
      "description": "Daily calorie target per person, if specified"
    }
  }
},
{
  "id": "design-weekly-menu",
  "instructions": "Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.",
  "outputSchema": {
    "days": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": {
            "type": "string"
          },
          "meals": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "mealType": {
                  "type": "string",
                  "description": "breakfast, lunch, dinner, or snack"
                },
                "dishName": {
                  "type": "string"
                },
                "cuisineType": {
                  "type": "string"
                },
                "cookingTime": {
                  "type": "string"
                },
                "technique": {
                  "type": "string",
                  "description": "Primary cooking method"
                },
                "isLeftover": {
                  "type": "boolean",
                  "description": "Whether this uses leftovers from a previous meal"
                },
                "estimatedCalories": {
                  "type": "number"
                }
              }
            }
          },
          "dailyCalories": {
            "type": "number",
            "description": "Total estimated calories for the day"
          }
        }
      }
    },
    "weeklyStrategy": {
      "type": "string",
      "description": "Overview of the meal plan strategy (variety, leftovers, prep batching)"
    }
  },
  "dependsOn": [
    "define-meal-preferences"
  ]
},
{
  "id": "create-prep-schedule",
  "instructions": "Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.",
  "outputSchema": {
    "batchPrepSession": {
      "type": "object",
      "properties": {
        "day": {
          "type": "string",
          "description": "When to do batch prep (e.g., 'Sunday afternoon')"
        },
        "totalTime": {
          "type": "string"
        },
        "tasks": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "task": {
                "type": "string"
              },
              "usedFor": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Which meals this prep serves"
              },
              "duration": {
                "type": "string"
              },
              "storage": {
                "type": "string",
                "description": "How to store (fridge, freezer, container type)"
              },
              "shelfLife": {
                "type": "string",
                "description": "How long the prepped item keeps"
              }
            }
          }
        }
      }
    },
    "dailyPrepTasks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": {
            "type": "string"
          },
          "tasks": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "activeTime": {
            "type": "string",
            "description": "Hands-on time needed"
          }
        }
      }
    }
  },
  "dependsOn": [
    "design-weekly-menu"
  ]
},
{
  "id": "build-grocery-list",
  "instructions": "Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.",
  "outputSchema": {
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "section": {
            "type": "string",
            "description": "Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "ingredient": {
                  "type": "string"
                },
                "quantity": {
                  "type": "string"
                },
                "unit": {
                  "type": "string"
                },
                "usedIn": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Which meals use this ingredient"
                },
                "isSpecialty": {
                  "type": "boolean",
                  "description": "Whether this may need a specialty store"
                }
              }
            }
          }
        }
      }
    },
    "pantryCheck": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Items likely already in the pantry — verify before buying"
    },
    "estimatedTotal": {
      "type": "string",
      "description": "Rough cost estimate for the grocery list"
    }
  },
  "dependsOn": [
    "create-prep-schedule"
  ]
}
])
</message>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
(no variables declared)

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
(no variables declared)

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<message role="user">
for 1 person
</message>

<turn n="1" messages="3" finish="stop" model="glm-4.5">
  <usage input="7062" output="343" total="7405" reasoning="1" cache-read="7058" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name   Type    Value
-----  ------  --------------------------------------------------
input  Object  {people, mealsPerDay, cuisinePreferences, dieta...

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var input = await ask(<div>
  <NumberInput name="people" label="Number of people to feed" min="1" max="10" defaultValue="1" />
  <NumberInput name="mealsPerDay" label="Meals per day" min="1" max="5" defaultValue="3" />
  <MultiSelect name="cuisinePreferences" label="Preferred cuisine types" options={["italian", "japanese", "mexican", "american", "mediterranean", "asian"]} />
  <MultiSelect name="dietaryRestrictions" label="Dietary restrictions (if any)" options={["vegetarian", "gluten-free", "dairy-free", "vegan", "low-carb", "keto"]} />
  <Select name="weekdayCookingTime" label="Available cooking time on weekdays" options={["15-20 minutes", "30 minutes", "45-60 minutes", "1+ hours"]} defaultValue="30 minutes" />
  <Select name="weekendCookingTime" label="Available cooking time on weekends" options={["30 minutes", "1 hour", "1-2 hours", "2+ hours"]} defaultValue="1-2 hours" />
  <Select name="nutritionalGoal" label="Primary nutrition goal" options={["balanced", "high protein", "low carb", "weight loss", "maintenance", "muscle gain"]} defaultValue="balanced" />
  <NumberInput name="calorieTarget" label="Daily calorie target (optional)" placeholder="Leave blank if no specific target" />
</div>)
await stop(input)
  </message>
  <message role="user">
← stop { input: { "people": "1", "mealsPerDay": "3", "cuisinePreferences": "mediterranean", "dietaryRestrictions": "vegetarian", "weekdayCookingTime": "30 minutes", "weekendCookingTime": "1-2 hours", "nutritionalGoal": "balanced", "calorieTarget": "2000" } }
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, dieta...
  </scope>
</turn>

<turn n="2" messages="5" finish="stop" model="glm-4.5">
  <usage input="7514" output="77" total="7591" reasoning="1" cache-read="1536" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name       Type    Value
---------  ------  --------------------------------------------------
input      Object  {people, mealsPerDay, cuisinePreferences, dieta...
knowledge  Object  {cooking}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var knowledge = loadKnowledge({
  "cooking": {
    "cuisine": {
      "type": {
        "italian": true,
        "mediterranean": true
      }
    },
    "dietary": {
      "restriction": {
        "vegetarian": true
      }
    }
  }
})
await stop(knowledge)
  </message>
  <message role="user">
← stop { knowledge: { "cooking": { "cuisine": { "type": { "italian": "# Italian Cuisine\n\nItalian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.\n\n## Key Principles\n- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.\n- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.\n- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.\n- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.\n\n## Essential Techniques\n- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.\n- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.\n- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.\n\n## Staple Ingredients\nOlive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.\n\n## Classic Dishes\nCacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu." } }, "dietary": { "restriction": { "vegetarian": "# Vegetarian Cooking\n\nVegetarian cooking excludes meat and fish but includes dairy, eggs, and honey.\n\n## Substitution Strategies\n- **Protein sources** — Eggs, cheese, legumes (lentils, chickpeas, black beans), tofu, tempeh, seitan, nuts\n- **Umami replacements** — Soy sauce, miso paste, nutritional yeast, aged cheeses, mushrooms, tomato paste\n- **Stock** — Use vegetable stock or mushroom stock instead of chicken/beef stock\n- **Gelatin** — Replace with agar-agar (1:1 ratio, but boil to activate)\n\n## Tips\n- Roast vegetables at high heat (220°C+) for caramelization and depth\n- Use smoked paprika, liquid smoke, or charred vegetables for smoky notes usually provided by meat\n- Don't treat vegetarian food as \"meat minus the meat\" — build dishes around vegetables as the star" } } } } }
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, dieta...
    knowledge: Object {cooking}
  </scope>
</turn>

<turn n="3" messages="7" finish="length" model="glm-4.5">
  <usage input="8143" output="4096" total="12239" reasoning="1" cache-read="1536" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            undefined      undefined

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var mealPreferences = {
  people: 1,
  mealsPerDay: 3,
  cuisinePreferences: ["mediterranean"],
  dietaryRestrictions: ["vegetarian"],
  weekdayCookingTime: "30 minutes",
  weekendCookingTime: "1-2 hours",
  nutritionalGoal: "balanced",
  calorieTarget: 2000
}
completeTask("mealplan", "define-meal-preferences", mealPreferences)

var weeklyMenu = {
  days: [
    {
      day: "Monday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Yogurt Bowl",
          cuisineType: "mediterranean",
          cookingTime: "5 minutes",
          technique: "assembly",
          isLeftover: false,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Caprese Quinoa Bowl",
          cuisineType: "mediterranean",
          cookingTime: "15 minutes",
          technique: "assembly",
          isLeftover: false,
          estimatedCalories: 550
        },
        {
          mealType: "dinner",
          dishName: "Eggplant Parmesan",
          cuisineType: "italian",
          cookingTime: "30 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 650
        }
      ],
      dailyCalories: 1550
    },
    {
      day: "Tuesday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Yogurt Bowl",
          cuisineType: "mediterranean",
          cookingTime: "5 minutes",
          technique: "assembly",
          isLeftover: true,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Leftover Eggplant Parmesan",
          cuisineType: "italian",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 650
        },
        {
          mealType: "dinner",
          dishName: "Mediterranean Stuffed Peppers",
          cuisineType: "mediterranean",
          cookingTime: "35 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 550
        }
      ],
      dailyCalories: 1550
    },
    {
      day: "Wednesday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Yogurt Bowl",
          cuisineType: "mediterranean",
          cookingTime: "5 minutes",
          technique: "assembly",
          isLeftover: true,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Leftover Stuffed Peppers",
          cuisineType: "mediterranean",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 550
        },
        {
          mealType: "dinner",
          dishName: "Mediterranean Pasta Primavera",
          cuisineType: "italian",
          cookingTime: "25 minutes",
          technique: "saute",
          isLeftover: false,
          estimatedCalories: 650
        }
      ],
      dailyCalories: 1550
    },
    {
      day: "Thursday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Yogurt Bowl",
          cuisineType: "mediterranean",
          cookingTime: "5 minutes",
          technique: "assembly",
          isLeftover: true,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Leftover Pasta Primavera",
          cuisineType: "italian",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 650
        },
        {
          mealType: "dinner",
          dishName: "Mediterranean Veggie Burgers",
          cuisineType: "mediterranean",
          cookingTime: "30 minutes",
          technique: "grill",
          isLeftover: false,
          estimatedCalories: 550
        }
      ],
      dailyCalories: 1550
    },
    {
      day: "Friday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Yogurt Bowl",
          cuisineType: "mediterranean",
          cookingTime: "5 minutes",
          technique: "assembly",
          isLeftover: true,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Leftover Veggie Burgers",
          cuisineType: "mediterranean",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 550
        },
        {
          mealType: "dinner",
          dishName: "Mediterranean Pizza Night",
          cuisineType: "italian",
          cookingTime: "45 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 650
        }
      ],
      dailyCalories: 1550
    },
    {
      day: "Saturday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mediterranean Frittata",
          cuisineType: "italian",
          cookingTime: "25 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 450
        },
        {
          mealType: "lunch",
          dishName: "Mediterranean Quinoa Salad",
          cuisineType: "mediterranean",
          cookingTime: "15 minutes",
          technique: "assembly",
          isLeftover: false,
          estimatedCalories: 550
        },
        {
          mealType: "dinner",
          dishName: "Ratatouille with Polenta",
          cuisineType: "mediterranean",
          cookingTime: "1 hour 30 minutes",
          technique: "braise",
          isLeftover: true,
          estimatedCalories: 800
        }
      ],
      dailyCalories: 1800
    },
    {
      day: "Sunday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Leftover Frittata",
          cuisineType: "italian",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 450
        },
        {
          mealType: "lunch",
          dishName: "Leftover Ratatouille with Polenta",
          cuisineType: "mediterranean",
          cookingTime: "15 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 800
        },
        {
          mealType: "dinner",
          dishName: "Mediterranean Mezze Platter",
          cuisineType: "mediterranean",
          cookingTime: "30 minutes",
          technique: "assembly",
          isLeftover: false,
          estimatedCalories: 750
        }
      ],
      dailyCalories: 2000
    }
  ],
  weeklyStrategy: "Mediterranean-inspired vegetarian meals with strategic leftovers for busy weekdays. Weekends feature more elaborate dishes that provide 2-3 servings for weekday lunches. Balance between Italian pasta dishes and Mediterranean vegetable-focused meals. All recipes emphasize fresh vegetables, olive oil, herbs, and plant-based proteins."
}
completeTask("mealplan", "design-weekly-menu", weeklyMenu)

var prepSchedule = {
  batchPrepSession: {
    day: "Sunday afternoon",
    totalTime: "2 hours",
    tasks: [
      {
        task: "Cook quinoa (4 cups)",
        usedFor: ["Caprese Quinoa Bowl", "Mediterranean Quinoa Salad"],
        duration: "25 minutes",
        storage: "Airtight container in fridge",
        shelfLife: "4-5 days"
      },
      {
        task: "Chop vegetables (onions, bell peppers, eggplant, zucchini)",
        usedFor: ["Eggplant Parmesan", "Stuffed Peppers", "Pasta Primavera", "Ratatouille"],
        duration: "30 minutes",
        storage: "Containers in fridge",
        shelfLife: "3-4 days"
      },
      {
        task: "Make tomato sauce (from scratch)",
        usedFor: ["Eggplant Parmesan", "Stuffed Peppers", "Pasta Primavera"],
        duration: "20 minutes",
        storage: "Jar in fridge",
        shelfLife: "1 week"
      },
      {
        task: "Prepare Mediterranean yogurt mix (with herbs and garlic)",
        usedFor: ["Mediterranean Yogurt Bowl"],
        duration: "10 minutes",
        storage: "Container in fridge",
        shelfLife: "4 days"
      },
      {
        task: "Make veggie burger patties",
        usedFor: ["Mediterranean Veggie Burgers"],
        duration: "20 minutes",
        storage: "Fridge (uncooked) or freeze half",
        shelfLife: "Fridge: 2 days, Freezer: 1 month"
      },
      {
        task: "Prepare pizza dough (3 portions)",
        usedFor: ["Mediterranean Pizza Night"],
        duration: "15 minutes (plus 1 hour rise time)",
        storage: "Fridge, divide into 3 portions",
        shelfLife: "3 days in fridge, freeze for longer"
      }
    ]
  },
  dailyPrepTasks: [
    {
      day: "Monday",
      tasks: [
        "Assemble yogurt bowl with fresh berries and nuts",
        "Toss quinoa with cherry tomatoes and mozzarella",
        "Bake eggplant parmesan (uses prepped sauce and veggies)"
      ],
      activeTime: "15 minutes"
    },
    {
      day: "Tuesday",
      tasks: [
        "Assemble yogurt bowl",
        "Reheat leftover eggplant parmesan",
        "Stuff bell peppers with quinoa and bake"
      ],
      activeTime: "10 minutes"
    },
    {
      day: "Wednesday",
      tasks: [
        "Assemble yogurt bowl",
        "Reheat leftover stuffed peppers",
        "Cook pasta with sautéed prepped vegetables and sauce"
      ],
      activeTime: "15 minutes"
    },
    {
      day: "Thursday",
      tasks: [
        "Assemble yogurt bowl",
        "Reheat leftover pasta primavera",
        "Cook veggie burgers (pre-prepped patties)"
      ],
      activeTime: "10 minutes"
    },
    {
      day: "Friday",
      tasks: [
        "Assemble yogurt bowl",
        "Reheat leftover veggie burgers",
        "Prepare pizza with prepped dough and toppings"
      ],
      activeTime: "15 minutes"
    },
    {
      day: "Saturday",
      tasks: [
        "Make Mediterranean frittata with eggs and prepped vegetables",
        "Assemble quinoa salad with fresh herbs and vegetables",
        "Start ratatouille (uses prepped vegetables) in slow cooker"
      ],
      activeTime: "20 minutes"
    },
    {
      day: "Sunday",
      tasks: [
        "Reheat leftover frittata",
        "Reheat ratatouille and make quick polenta",
        "Assemble mezze platter with hummus, olives, and prepped vegetables"
      ],
      activeTime: "15 minutes"
    }
  ]
}
completeTask("mealplan", "create-prep-schedule", prepSchedule)

var recipesForGroceryList = [
  {
    name: "Mediterranean Yogurt Bowl",
    ingredients: [
      "Greek yogurt, 32 oz",
      "Fresh berries, 2 cups",
      "Mixed nuts, 1 cup",
      "Honey, 1 cup",
      "Fresh mint, 1 bunch",
      "Lemon, 2"
    ]
  },
  {
    name: "Caprese Quinoa Bowl",
    ingredients: [
      "Quinoa, 2 cups",
      "Cherry tomatoes, 2 pints",
      "Fresh mozzarella balls, 16 oz",
      "Fresh basil, 2 bunches",
      "Balsamic vinegar, 1 bottle",
      "Olive oil, 1 bottle"
    ]
  },
  {
    name: "Eggplant Parmesan",
    ingredients: [
      "Eggplant, 2 large",
      "Breadcrumbs, 1 cup",
      "Parmesan cheese, 8 oz",
      "Mozzarella cheese, 16 oz",
      "Canned tomatoes, 28 oz can",
      "Onion, 1",
      "Garlic, 1 head"
    ]
  },
  {
    name: "Mediterranean Stuffed Peppers",
    ingredients: [
      "Bell peppers, 6",
      "Quinoa, 1 cup",
      "Feta cheese, 8 oz",
      "Kalamata olives, 1 cup",
      "Fresh parsley, 1 bunch",
      "Lemon, 1"
    ]
  },
  {
    name: "Mediterranean Pasta Primavera",
    ingredients: [
      "Pasta, 1 lb",
      "Zucchini, 3",
      "Yellow squash, 2",
      "Cherry tomatoes, 1 pint",
      "Fresh basil, 1 bunch",
      "Parmesan cheese, 4 oz"
    ]
  },
  {
    name: "Mediterranean Veggie Burgers",
    ingredients: [
      "Black beans, 2 cans",
      "Breadcrumbs, 1 cup",
      "Eggs, 1 dozen",
      "Cumin, 1 jar",
      "Paprika, 1 jar",
      "Burger buns, 8"
    ]
  },
  {
    name: "Mediterranean Pizza Night",
    ingredients: [
      "Pizza flour, 5 lb",
      "Active dry yeast, 1 packet",
      "Pizza sauce, 1 jar",
      "Mozzarella cheese, 16 oz",
      "Artichoke hearts, 1 jar",
      "Sun-dried tomatoes, 1 jar",
      "Fresh oregano, 1 bunch"
    ]
  },
  {
    name: "Mediterranean Frittata",
    ingredients: [
      "Eggs, 1 dozen",
      "Spinach, 5 oz",
      "Feta cheese, 8 oz",
      "Red onion, 1",
      "Fresh dill, 1 bunch"
    ]
  },
  {
    name: "Mediterranean Quinoa Salad",
    ingredients: [
      "Quinoa, 1 cup",
      "Cucumber, 2",
      "Cherry tomatoes, 1 pint",
      "Red onion, 1",
      "Fresh parsley, 1 bunch",
      "Lemon, 2",
      "Olive oil"
    ]
  },
  {
    name: "Ratatouille with Polenta",
    ingredients: [
      "Eggplant, 2",
      "Zucchini, 2",
      "Bell peppers, 2",
      "Canned tomatoes, 28 oz",
      "Polenta, 1 tube",
      "Fresh thyme, 1 bunch",
      "Olive oil"
    ]
  },
  {
    name: "Mediterranean Mezze Platter",
    ingredients: [
      "Hummus, 1 container",
      "Pita bread, 1 pack",
      "Olives, 1 cup",
      "Feta cheese, 8 oz",
      "Cucumber, 1",
      "Grapes, 1 bunch",
      "Tahini"
    ]
  }
]

var groceryListRaw = buildGroceryList(recipesForGroceryList)

var groceryList = {
  sections: [
    {
      section: "Produce",
      items: [
        {
          ingredient: "Eggplant (4 large)",
          quantity: "4",
          unit: "each",
          usedIn: ["Eggplant Parmesan", "Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Bell peppers (8 total)",
          quantity: "8",
          unit: "each",
          usedIn: ["Stuffed Peppers", "Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Zucchini (5 total)",
          quantity: "5",
          unit: "each",
          usedIn: ["Pasta Primavera", "Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Yellow squash",
          quantity: "2",
          unit: "each",
          usedIn: ["Pasta Primavera"],
          isSpecialty: false
        },
        {
          ingredient: "Cherry tomatoes (4 pints)",
          quantity: "4",
          unit: "pint",
          usedIn: ["Caprese Quinoa Bowl", "Pasta Primavera", "Quinoa Salad"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh basil (3 bunches)",
          quantity: "3",
          unit: "bunch",
          usedIn: ["Caprese Quinoa Bowl", "Pasta Primavera", "Pizza Night"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh mint",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Yogurt Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh parsley (2 bunches)",
          quantity: "2",
          unit: "bunch",
          usedIn: ["Stuffed Peppers", "Quinoa Salad"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh oregano",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Pizza Night"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh thyme",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh dill",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Frittata"],
          isSpecialty: false
        },
        {
          ingredient: "Spinach",
          quantity: "5",
          unit: "oz",
          usedIn: ["Frittata"],
          isSpecialty: false
        },
        {
          ingredient: "Onions (3 total)",
          quantity: "3",
          unit: "each",
          usedIn: ["Eggplant Parmesan", "Frittata
  </message>
  <message role="user">
⚠ [system] Tasklist "mealplan" incomplete. Ready: build-grocery-list. Continue with a ready task.

{{TASKS}}
┌ mealplan ─────────────────────────────────────────────────┐
│ ✓ define-meal-preferences → {"people":1,"mealsPerDay":3,"cuisineP...│
│ ✓ design-weekly-menu → {"days":[{"day":"Monday","meals":[{"m...│
│ ✓ create-prep-schedule → {"batchPrepSession":{"day":"Sunday af...│
│ ◎ build-grocery-list (ready — deps satisfied)                │
└───────────────────────────────────────────────────────────────┘
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, dieta...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: undefined undefined
  </scope>
</turn>

<turn n="4" messages="9" finish="stop" model="glm-4.5">
  <usage input="12154" output="1628" total="13782" reasoning="1" cache-read="1520" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            undefined      undefined

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
{
          ingredient: "Cucumber (3 total)",
          quantity: "3",
          unit: "each",
          usedIn: ["Quinoa Salad", "Mezze Platter"],
          isSpecialty: false
        },
        {
          ingredient: "Red onion (2 total)",
          quantity: "2",
          unit: "each",
          usedIn: ["Frittata", "Quinoa Salad"],
          isSpecialty: false
        },
        {
          ingredient: "Garlic",
          quantity: "1",
          unit: "head",
          usedIn: ["Eggplant Parmesan"],
          isSpecialty: false
        },
        {
          ingredient: "Lemons (4 total)",
          quantity: "4",
          unit: "each",
          usedIn: ["Yogurt Bowl", "Stuffed Peppers", "Quinoa Salad"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh berries",
          quantity: "2",
          unit: "cups",
          usedIn: ["Yogurt Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Grapes",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Mezze Platter"],
          isSpecialty: false
        }
      ]
    },
    {
      section: "Dairy & Eggs",
      items: [
        {
          ingredient: "Greek yogurt",
          quantity: "32",
          unit: "oz",
          usedIn: ["Yogurt Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Fresh mozzarella balls",
          quantity: "16",
          unit: "oz",
          usedIn: ["Caprese Quinoa Bowl"],
          isSpecialty: true
        },
        {
          ingredient: "Parmesan cheese",
          quantity: "12",
          unit: "oz",
          usedIn: ["Eggplant Parmesan", "Pasta Primavera"],
          isSpecialty: false
        },
        {
          ingredient: "Mozzarella cheese",
          quantity: "32",
          unit: "oz",
          usedIn: ["Eggplant Parmesan", "Pizza Night"],
          isSpecialty: false
        },
        {
          ingredient: "Feta cheese",
          quantity: "16",
          unit: "oz",
          usedIn: ["Stuffed Peppers", "Frittata", "Mezze Platter"],
          isSpecialty: false
        },
        {
          ingredient: "Eggs",
          quantity: "1",
          unit: "dozen",
          usedIn: ["Veggie Burgers", "Frittata"],
          isSpecialty: false
        }
      ]
    },
    {
      section: "Pantry",
      items: [
        {
          ingredient: "Quinoa",
          quantity: "3",
          unit: "cups",
          usedIn: ["Caprese Quinoa Bowl", "Stuffed Peppers", "Quinoa Salad"],
          isSpecialty: false
        },
        {
          ingredient: "Breadcrumbs",
          quantity: "2",
          unit: "cups",
          usedIn: ["Eggplant Parmesan", "Veggie Burgers"],
          isSpecialty: false
        },
        {
          ingredient: "Canned tomatoes",
          quantity: "56",
          unit: "oz (2 cans)",
          usedIn: ["Eggplant Parmesan", "Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Mixed nuts",
          quantity: "1",
          unit: "cup",
          usedIn: ["Yogurt Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Olive oil",
          quantity: "1",
          unit: "large bottle",
          usedIn: ["Caprese Quinoa Bowl", "Quinoa Salad", "Ratatouille"],
          isSpecialty: false
        },
        {
          ingredient: "Balsamic vinegar",
          quantity: "1",
          unit: "bottle",
          usedIn: ["Caprese Quinoa Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Honey",
          quantity: "1",
          unit: "cup",
          usedIn: ["Yogurt Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Cumin",
          quantity: "1",
          unit: "jar",
          usedIn: ["Veggie Burgers"],
          isSpecialty: false
        },
        {
          ingredient: "Paprika",
          quantity: "1",
          unit: "jar",
          usedIn: ["Veggie Burgers"],
          isSpecialty: false
        },
        {
          ingredient: "Pizza flour",
          quantity: "5",
          unit: "lb",
          usedIn: ["Pizza Night"],
          isSpecialty: true
        },
        {
          ingredient: "Active dry yeast",
          quantity: "1",
          unit: "packet",
          usedIn: ["Pizza Night"],
          isSpecialty: false
        },
        {
          ingredient: "Polenta",
          quantity: "1",
          unit: "tube",
          usedIn: ["Ratatouille"],
          isSpecialty: true
        }
      ]
    },
    {
      section: "Canned & Jarred",
      items: [
        {
          ingredient: "Artichoke hearts",
          quantity: "1",
          unit: "jar",
          usedIn: ["Pizza Night"],
          isSpecialty: true
        },
        {
          ingredient: "Sun-dried tomatoes",
          quantity: "1",
          unit: "jar",
          usedIn: ["Pizza Night"],
          isSpecialty: true
        },
        {
          ingredient: "Kalamata olives",
          quantity: "1",
          unit: "cup",
          usedIn: ["Stuffed Peppers"],
          isSpecialty: true
        },
        {
          ingredient: "Black beans",
          quantity: "2",
          unit: "cans",
          usedIn: ["Veggie Burgers"],
          isSpecialty: false
        },
        {
          ingredient: "Pizza sauce",
          quantity: "1",
          unit: "jar",
          usedIn: ["Pizza Night"],
          isSpecialty: false
        },
        {
          ingredient: "Hummus",
          quantity: "1",
          unit: "container",
          usedIn: ["Mezze Platter"],
          isSpecialty: false
        },
        {
          ingredient: "Tahini",
          quantity: "1",
          unit: "jar",
          usedIn: ["Mezze Platter"],
          isSpecialty: true
        }
      ]
    },
    {
      section: "Bakery",
      items: [
        {
          ingredient: "Burger buns",
          quantity: "8",
          unit: "count",
          usedIn: ["Veggie Burgers"],
          isSpecialty: false
        },
        {
          ingredient: "Pita bread",
          quantity: "1",
          unit: "pack",
          usedIn: ["Mezze Platter"],
          isSpecialty: false
        }
      ]
    }
  ],
  pantryCheck: [
    "Olive oil",
    "Balsamic vinegar",
    "Honey",
    "Cumin",
    "Paprika",
    "Salt",
    "Black pepper",
    "Garlic powder",
    "Dried oregano"
  ],
  estimatedTotal: "$120-150"
}
completeTask("mealplan", "build-grocery-list", groceryList)
  </message>
  <message role="user">
← error [ReferenceError] quantity is not defined (line 15)
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, dieta...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: undefined undefined
  </scope>
</turn>

<turn n="5" messages="11" finish="stop" model="glm-4.5">
  <usage input="13799" output="2386" total="16185" reasoning="1" cache-read="13781" />
</turn>

<finalize model="zai:glm-4.5" turns="5" status="complete">
  <tokens input="187199" output="17600" total="204799" />
</finalize>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<message role="user">
show me the grocery list
</message>

<turn n="1" messages="12" finish="stop" model="glm-4.5">
  <usage input="13813" output="1249" total="15062" reasoning="1" cache-read="1651" />
</turn>

<finalize model="zai:glm-4.5" turns="6" status="complete">
  <tokens input="187199" output="17600" total="204799" />
</finalize>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<message role="user">
for how many persons this is
</message>

<turn n="1" messages="13" finish="stop" model="glm-4.5">
  <usage input="13821" output="1170" total="14991" reasoning="1" cache-read="13812" />
</turn>

<finalize model="zai:glm-4.5" turns="7" status="complete">
  <tokens input="187199" output="17600" total="204799" />
</finalize>

<message role="assistant">
[setup] tasklist("mealplan", "Creates a weekly meal plan with balanced nutrition, grocery list, and prep schedule", [
{
  "id": "define-meal-preferences",
  "instructions": "Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.",
  "outputSchema": {
    "people": {
      "type": "number",
      "description": "Number of people to feed"
    },
    "mealsPerDay": {
      "type": "number",
      "description": "Number of meals to plan per day"
    },
    "cuisinePreferences": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Preferred cuisine types"
    },
    "dietaryRestrictions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"
    },
    "weekdayCookingTime": {
      "type": "string",
      "description": "Available cooking time on weekdays (e.g., '30 minutes')"
    },
    "weekendCookingTime": {
      "type": "string",
      "description": "Available cooking time on weekends (e.g., '1-2 hours')"
    },
    "nutritionalGoal": {
      "type": "string",
      "description": "Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"
    },
    "calorieTarget": {
      "type": "number",
      "description": "Daily calorie target per person, if specified"
    }
  }
},
{
  "id": "design-weekly-menu",
  "instructions": "Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.",
  "outputSchema": {
    "days": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": {
            "type": "string"
          },
          "meals": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "mealType": {
                  "type": "string",
                  "description": "breakfast, lunch, dinner, or snack"
                },
                "dishName": {
                  "type": "string"
                },
                "cuisineType": {
                  "type": "string"
                },
                "cookingTime": {
                  "type": "string"
                },
                "technique": {
                  "type": "string",
                  "description": "Primary cooking method"
                },
                "isLeftover": {
                  "type": "boolean",
                  "description": "Whether this uses leftovers from a previous meal"
                },
                "estimatedCalories": {
                  "type": "number"
                }
              }
            }
          },
          "dailyCalories": {
            "type": "number",
            "description": "Total estimated calories for the day"
          }
        }
      }
    },
    "weeklyStrategy": {
      "type": "string",
      "description": "Overview of the meal plan strategy (variety, leftovers, prep batching)"
    }
  },
  "dependsOn": [
    "define-meal-preferences"
  ]
},
{
  "id": "create-prep-schedule",
  "instructions": "Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.",
  "outputSchema": {
    "batchPrepSession": {
      "type": "object",
      "properties": {
        "day": {
          "type": "string",
          "description": "When to do batch prep (e.g., 'Sunday afternoon')"
        },
        "totalTime": {
          "type": "string"
        },
        "tasks": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "task": {
                "type": "string"
              },
              "usedFor": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Which meals this prep serves"
              },
              "duration": {
                "type": "string"
              },
              "storage": {
                "type": "string",
                "description": "How to store (fridge, freezer, container type)"
              },
              "shelfLife": {
                "type": "string",
                "description": "How long the prepped item keeps"
              }
            }
          }
        }
      }
    },
    "dailyPrepTasks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "day": {
            "type": "string"
          },
          "tasks": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "activeTime": {
            "type": "string",
            "description": "Hands-on time needed"
          }
        }
      }
    }
  },
  "dependsOn": [
    "design-weekly-menu"
  ]
},
{
  "id": "build-grocery-list",
  "instructions": "Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.",
  "outputSchema": {
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "section": {
            "type": "string",
            "description": "Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "ingredient": {
                  "type": "string"
                },
                "quantity": {
                  "type": "string"
                },
                "unit": {
                  "type": "string"
                },
                "usedIn": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Which meals use this ingredient"
                },
                "isSpecialty": {
                  "type": "boolean",
                  "description": "Whether this may need a specialty store"
                }
              }
            }
          }
        }
      }
    },
    "pantryCheck": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Items likely already in the pantry — verify before buying"
    },
    "estimatedTotal": {
      "type": "string",
      "description": "Rough cost estimate for the grocery list"
    }
  },
  "dependsOn": [
    "create-prep-schedule"
  ]
}
])
</message>

<message role="user">
← error [Error] tasklist() tasklist "mealplan" already declared (line 21)
</message>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, dieta...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

<message role="user">
for a poor family of 4
</message>

<turn n="1" messages="16" finish="stop" model="glm-4.5">
  <usage input="15377" output="348" total="15725" reasoning="1" cache-read="7712" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, weekd...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var input = await ask(<div>
  <NumberInput name="people" label="Number of people to feed" min="1" max="10" defaultValue="4" />
  <NumberInput name="mealsPerDay" label="Meals per day" min="1" max="5" defaultValue="3" />
  <MultiSelect name="cuisinePreferences" label="Preferred cuisine types" options={["italian", "japanese", "mexican", "american", "mediterranean", "asian"]} />
  <MultiSelect name="dietaryRestrictions" label="Dietary restrictions (if any)" options={["vegetarian", "gluten-free", "dairy-free", "vegan", "low-carb", "keto"]} />
  <Select name="weekdayCookingTime" label="Available cooking time on weekdays" options={["15-20 minutes", "30 minutes", "45-60 minutes", "1+ hours"]} defaultValue="30 minutes" />
  <Select name="weekendCookingTime" label="Available cooking time on weekends" options={["30 minutes", "1 hour", "1-2 hours", "2+ hours"]} defaultValue="1-2 hours" />
  <Select name="nutritionalGoal" label="Primary nutrition goal" options={["balanced", "high protein", "low carb", "weight loss", "maintenance", "muscle gain", "budget-friendly"]} defaultValue="budget-friendly" />
  <NumberInput name="calorieTarget" label="Daily calorie target (optional)" placeholder="Leave blank if no specific target" />
</div>)
await stop(input)
  </message>
  <message role="user">
← stop { input: { "people": "4", "mealsPerDay": "3", "cuisinePreferences": "mexican", "weekdayCookingTime": "30 minutes", "weekendCookingTime": "1-2 hours", "nutritionalGoal": "budget-friendly", "calorieTarget": "1000" } }
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, weekd...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: Object {sections, pantryCheck, estimatedTotal}
  </scope>
</turn>

<turn n="2" messages="18" finish="stop" model="glm-4.5">
  <usage input="15802" output="70" total="15872" reasoning="1" cache-read="1546" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, weekd...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var knowledge = loadKnowledge({
  "cooking": {
    "cuisine": {
      "type": {
        "mexican": true
      }
    },
    "dietary": {
      "restriction": {
        "vegetarian": true
      }
    }
  }
})
await stop(knowledge)
  </message>
  <message role="user">
← stop { knowledge: { "cooking": { "cuisine": { "type": { "mexican": "# Mexican Cuisine\n\nMexican cooking is one of the world's great culinary traditions — built on indigenous ingredients, bold chiles, and techniques refined over millennia.\n\n## Key Principles\n- **Chile complexity** — Different chiles serve different roles: ancho for sweetness, chipotle for smoke, habanero for heat. Toast dried chiles before rehydrating.\n- **Nixtamalization** — Treating corn with lime (cal) transforms it into masa, the foundation of tortillas and tamales. This ancient technique unlocks niacin and changes the flavor profile.\n- **Layered seasoning** — Flavors are built in stages: toast, char, blend, simmer.\n- **Low and slow** — Barbacoa, birria, and carnitas all rely on long, gentle cooking to break down tough cuts.\n\n## Essential Techniques\n- **Salsa making** — Char tomatoes and chiles on a comal or under a broiler before blending. The char adds smoky depth.\n- **Tortilla pressing** — Masa should be the texture of Play-Doh. Press thin, cook on a dry comal at high heat, and look for the puff.\n- **Mole building** — Toast chiles, nuts, seeds, and spices separately, then blend and simmer for hours. Patience is the key ingredient.\n\n## Staple Ingredients\nCorn masa, dried chiles (ancho, guajillo, chipotle), black beans, tomatillos, cilantro, lime, avocado, queso fresco, epazote, Mexican oregano.\n\n## Classic Dishes\nTacos al pastor, mole poblano, pozole, tamales, chiles rellenos, elote, churros." } }, "dietary": { "restriction": { "vegetarian": "# Vegetarian Cooking\n\nVegetarian cooking excludes meat and fish but includes dairy, eggs, and honey.\n\n## Substitution Strategies\n- **Protein sources** — Eggs, cheese, legumes (lentils, chickpeas, black beans), tofu, tempeh, seitan, nuts\n- **Umami replacements** — Soy sauce, miso paste, nutritional yeast, aged cheeses, mushrooms, tomato paste\n- **Stock** — Use vegetable stock or mushroom stock instead of chicken/beef stock\n- **Gelatin** — Replace with agar-agar (1:1 ratio, but boil to activate)\n\n## Tips\n- Roast vegetables at high heat (220°C+) for caramelization and depth\n- Use smoked paprika, liquid smoke, or charred vegetables for smoky notes usually provided by meat\n- Don't treat vegetarian food as \"meat minus the meat\" — build dishes around vegetables as the star" } } } } }
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, weekd...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: Object {sections, pantryCheck, estimatedTotal}
  </scope>
</turn>

<turn n="3" messages="20" finish="length" model="glm-4.5">
  <usage input="16491" output="4096" total="20587" reasoning="1" cache-read="15871" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, weekd...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var mealPreferences = {
  people: 4,
  mealsPerDay: 3,
  cuisinePreferences: ["mexican"],
  dietaryRestrictions: [],
  weekdayCookingTime: "30 minutes",
  weekendCookingTime: "1-2 hours",
  nutritionalGoal: "budget-friendly",
  calorieTarget: 1000
}
completeTask("mealplan", "define-meal-preferences", mealPreferences)

var weeklyMenu = {
  days: [
    {
      day: "Monday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mexican Bean & Cheese Burritos",
          cuisineType: "mexican",
          cookingTime: "15 minutes",
          technique: "saute",
          isLeftover: false,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Rice & Bean Bowls",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "assembly",
          isLeftover: false,
          estimatedCalories: 400
        },
        {
          mealType: "dinner",
          dishName: "Black Bean Tacos",
          cuisineType: "mexican",
          cookingTime: "20 minutes",
          technique: "saute",
          isLeftover: false,
          estimatedCalories: 450
        }
      ],
      dailyCalories: 1200
    },
    {
      day: "Tuesday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Leftover Bean & Cheese Burritos",
          cuisineType: "mexican",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 350
        },
        {
          mealType: "lunch",
          dishName: "Leftover Rice & Bean Bowls",
          cuisineType: "mexican",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 400
        },
        {
          mealType: "dinner",
          dishName: "Cheesy Bean Enchiladas",
          cuisineType: "mexican",
          cookingTime: "35 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 500
        }
      ],
      dailyCalories: 1250
    },
    {
      day: "Wednesday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Mexican Oatmeal with Cinnamon",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "simmer",
          isLeftover: false,
          estimatedCalories: 250
        },
        {
          mealType: "lunch",
          dishName: "Leftover Enchiladas",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 500
        },
        {
          mealType: "dinner",
          dishName: "Bean & Veggie Tostadas",
          cuisineType: "mexican",
          cookingTime: "25 minutes",
          technique: "bake",
          isLeftover: false,
          estimatedCalories: 450
        }
      ],
      dailyCalories: 1200
    },
    {
      day: "Thursday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Leftover Mexican Oatmeal",
          cuisineType: "mexican",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 250
        },
        {
          mealType: "lunch",
          dishName: "Leftover Tostadas",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 450
        },
        {
          mealType: "dinner",
          dishName: "Mexican Rice & Lentil Bowl",
          cuisineType: "mexican",
          cookingTime: "30 minutes",
          technique: "simmer",
          isLeftover: false,
          estimatedCalories: 500
        }
      ],
      dailyCalories: 1200
    },
    {
      day: "Friday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Leftover Mexican Oatmeal",
          cuisineType: "mexican",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 250
        },
        {
          mealType: "lunch",
          dishName: "Leftover Rice & Lentil Bowl",
          cuisineType: "mexican",
          cookingTime: "5 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 500
        },
        {
          mealType: "dinner",
          dishName: "Bean & Cheese Quesadillas",
          cuisineType: "mexican",
          cookingTime: "20 minutes",
          technique: "grill",
          isLeftover: false,
          estimatedCalories: 450
        }
      ],
      dailyCalories: 1200
    },
    {
      day: "Saturday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Chilaquiles with Eggs",
          cuisineType: "mexican",
          cookingTime: "30 minutes",
          technique: "saute",
          isLeftover: false,
          estimatedCalories: 400
        },
        {
          mealType: "lunch",
          dishName: "Mexican Lentil Soup",
          cuisineType: "mexican",
          cookingTime: "45 minutes",
          technique: "simmer",
          isLeftover: true,
          estimatedCalories: 450
        },
        {
          mealType: "dinner",
          dishName: "Big Pot of Pinto Beans",
          cuisineType: "mexican",
          cookingTime: "2 hours",
          technique: "simmer",
          isLeftover: true,
          estimatedCalories: 500
        }
      ],
      dailyCalories: 1350
    },
    {
      day: "Sunday",
      meals: [
        {
          mealType: "breakfast",
          dishName: "Leftover Chilaquiles",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 400
        },
        {
          mealType: "lunch",
          dishName: "Leftover Lentil Soup",
          cuisineType: "mexican",
          cookingTime: "10 minutes",
          technique: "reheat",
          isLeftover: true,
          estimatedCalories: 450
        },
        {
          mealType: "dinner",
          dishName: "Bean & Potato Tacos",
          cuisineType: "mexican",
          cookingTime: "30 minutes",
          technique: "saute",
          isLeftover: false,
          estimatedCalories: 450
        }
      ],
      dailyCalories: 1300
    }
  ],
  weeklyStrategy: "Budget-friendly Mexican meals centered around beans, rice, and lentils. Focus on bulk cooking with weekend preparations that provide 2-3 meals each. Uses inexpensive staples like dried beans, rice, potatoes, and corn tortillas. Incorporates Mexican spices and techniques for authentic flavor while keeping costs low. All meals are designed to be filling and nutritious on a tight budget."
}
completeTask("mealplan", "design-weekly-menu", weeklyMenu)

var prepSchedule = {
  batchPrepSession: {
    day: "Saturday morning",
    totalTime: "2.5 hours",
    tasks: [
      {
        task: "Sort and cook dried pinto beans (4 lbs)",
        usedFor: ["Pinto Beans", "Bean & Cheese Burritos", "Black Bean Tacos", "Enchiladas", "Tostadas", "Quesadillas"],
        duration: "2 hours",
        storage: "Airtight containers in fridge, portion for freezer",
        shelfLife: "Fridge: 5 days, Freezer: 3 months"
      },
      {
        task: "Cook Mexican rice (8 cups)",
        usedFor: ["Rice & Bean Bowls", "Mexican Rice & Lentil Bowl"],
        duration: "25 minutes",
        storage: "Airtight container in fridge",
        shelfLife: "4-5 days"
      },
      {
        task: "Chop onions, garlic, and jalapeños",
        usedFor: ["All week's meals"],
        duration: "20 minutes",
        storage: "Containers in fridge",
        shelfLife: "5-7 days"
      },
      {
        task: "Make salsa roja (from dried chiles)",
        usedFor: ["Tacos", "Enchiladas", "Chilaquiles"],
        duration: "30 minutes",
        storage: "Jar in fridge",
        shelfLife: "1-2 weeks"
      },
      {
        task: "Prepare lentils (2 cups dry)",
        usedFor: ["Mexican Lentil Soup", "Mexican Rice & Lentil Bowl"],
        duration: "20 minutes",
        storage: "Fridge",
        shelfLife: "4-5 days"
      }
    ]
  },
  dailyPrepTasks: [
    {
      day: "Monday",
      tasks: [
        "Assemble bean & cheese burritos for breakfast",
        "Warm beans and rice for lunch bowls",
        "Season beans for tacos with spices and salsa"
      ],
      activeTime: "20 minutes"
    },
    {
      day: "Tuesday",
      tasks: [
        "Reheat breakfast burritos",
        "Reheat rice & bean bowls for lunch",
        "Roll enchiladas with beans and cheese, bake"
      ],
      activeTime: "15 minutes"
    },
    {
      day: "Wednesday",
      tasks: [
        "Cook Mexican oatmeal with cinnamon",
        "Reheat leftover enchiladas for lunch",
        "Bake tortillas with bean topping for tostadas"
      ],
      activeTime: "15 minutes"
    },
    {
      day: "Thursday",
      tasks: [
        "Reheat Mexican oatmeal",
        "Reheat leftover tostadas for lunch",
        "Combine rice, lentils, and spices for dinner bowl"
      ],
      activeTime: "10 minutes"
    },
    {
      day: "Friday",
      tasks: [
        "Reheat Mexican oatmeal",
        "Reheat rice & lentil bowl for lunch",
        "Grill quesadillas with beans and cheese"
      ],
      activeTime: "10 minutes"
    },
    {
      day: "Saturday",
      tasks: [
        "Fry tortillas for chilaquiles, add eggs and salsa",
        "Simmer lentils with vegetables for soup",
        "Sort and soak pinto beans, then simmer with seasonings"
      ],
      activeTime: "30 minutes"
    },
    {
      day: "Sunday",
      tasks: [
        "Reheat chilaquiles",
        "Reheat lentil soup for lunch",
        "Sauté potatoes with beans and spices for tacos"
      ],
      activeTime: "20 minutes"
    }
  ]
}
completeTask("mealplan", "create-prep-schedule", prepSchedule)

var recipesForGroceryList = [
  {
    name: "Mexican Bean & Cheese Burritos",
    ingredients: [
      "Dried pinto beans, 2 lbs",
      "Corn tortillas, 20",
      "Cheddar cheese, 16 oz",
      "Onions, 3",
      "Garlic, 1 head",
      "Cumin, 1 jar"
    ]
  },
  {
    name: "Rice & Bean Bowls",
    ingredients: [
      "White rice, 4 cups",
      "Cooked pinto beans, 4 cups",
      "Onions, 2",
      "Cilantro, 1 bunch",
      "Limes, 6"
    ]
  },
  {
    name: "Black Bean Tacos",
    ingredients: [
      "Black beans, 2 cans",
      "Corn tortillas, 20",
      "White onion, 1",
      "Cilantro, 1 bunch",
      "Limes, 4",
      "Salsa"
    ]
  },
  {
    name: "Cheesy Bean Enchiladas",
    ingredients: [
      "Corn tortillas, 12",
      "Cooked pinto beans, 3 cups",
      "Cheddar cheese, 16 oz",
      "Enchilada sauce, 1 can"
    ]
  },
  {
    name: "Mexican Oatmeal with Cinnamon",
    ingredients: [
      "Oats, 1 lb",
      "Cinnamon, 1 jar",
      "Brown sugar, 1 lb",
      "Milk, 1 gallon"
    ]
  },
  {
    name: "Bean & Veggie Tostadas",
    ingredients: [
      "Corn tortillas, 12",
      "Cooked pinto beans, 2 cups",
      "Lettuce, 1 head",
      "Tomatoes, 4",
      "Queso fresco, 8 oz"
    ]
  },
  {
    name: "Mexican Rice & Lentil Bowl",
    ingredients: [
      "White rice, 2 cups",
      "Brown lentils, 2 cups",
      "Onions, 2",
      "Tomatoes, 4",
      "Cumin, 1 jar",
      "Cilantro, 1 bunch"
    ]
  },
  {
    name: "Bean & Cheese Quesadillas",
    ingredients: [
      "Flour tortillas, 12",
      "Cooked pinto beans, 2 cups",
      "Cheddar cheese, 8 oz",
      "Jalapeños, 4"
    ]
  },
  {
    name: "Chilaquiles with Eggs",
    ingredients: [
      "Corn tortillas, 20",
      "Eggs, 1 dozen",
      "Salsa roja",
      "Queso fresco, 8 oz",
      "Cilantro, 1 bunch"
    ]
  },
  {
    name: "Mexican Lentil Soup",
    ingredients: [
      "Brown lentils, 2 cups",
      "Carrots, 6",
      "Celery, 4 stalks",
      "Onions, 2",
      "Cumin, 1 jar",
      "Bay leaves"
    ]
  },
  {
    name: "Big Pot of Pinto Beans",
    ingredients: [
      "Dried pinto beans, 4 lbs",
      "Onions, 4",
      "Garlic, 1 head",
      "Cumin, 1 jar",
      "Epazote, 1 bunch"
    ]
  },
  {
    name: "Bean & Potato Tacos",
    ingredients: [
      "Potatoes, 6 lbs",
      "Cooked pinto beans, 2 cups",
      "Corn tortillas, 20",
      "Onions, 2",
      "Cilantro, 1 bunch",
      "Limes, 4"
    ]
  }
]

var groceryListRaw = buildGroceryList(recipesForGroceryList)

var groceryList = {
  sections: [
    {
      section: "Dry Goods & Pantry",
      items: [
        {
          ingredient: "Dried pinto beans",
          quantity: "6",
          unit: "lbs",
          usedIn: ["Bean Burritos", "Big Pot of Pinto Beans", "Most meals"],
          isSpecialty: false
        },
        {
          ingredient: "White rice",
          quantity: "6",
          unit: "cups",
          usedIn: ["Rice & Bean Bowls", "Mexican Rice & Lentil Bowl"],
          isSpecialty: false
        },
        {
          ingredient: "Brown lentils",
          quantity: "4",
          unit: "cups",
          usedIn: ["Mexican Rice & Lentil Bowl", "Mexican Lentil Soup"],
          isSpecialty: false
        },
        {
          ingredient: "Oats",
          quantity: "1",
          unit: "lb",
          usedIn: ["Mexican Oatmeal"],
          isSpecialty: false
        },
        {
          ingredient: "Cumin",
          quantity: "1",
          unit: "jar",
          usedIn: ["Multiple dishes"],
          isSpecialty: false
        },
        {
          ingredient: "Cinnamon",
          quantity: "1",
          unit: "jar",
          usedIn: ["Mexican Oatmeal"],
          isSpecialty: false
        },
        {
          ingredient: "Brown sugar",
          quantity: "1",
          unit: "lb",
          usedIn: ["Mexican Oatmeal"],
          isSpecialty: false
        },
        {
          ingredient: "Enchilada sauce",
          quantity: "1",
          unit: "can",
          usedIn: ["Cheesy Bean Enchiladas"],
          isSpecialty: false
        },
        {
          ingredient: "Black beans",
          quantity: "2",
          unit: "cans",
          usedIn: ["Black Bean Tacos"],
          isSpecialty: false
        },
        {
          ingredient: "Bay leaves",
          quantity: "1",
          unit: "small container",
          usedIn: ["Mexican Lentil Soup"],
          isSpecialty: false
        }
      ]
    },
    {
      section: "Produce",
      items: [
        {
          ingredient: "Onions",
          quantity: "18",
          unit: "each",
          usedIn: ["All main dishes"],
          isSpecialty: false
        },
        {
          ingredient: "Garlic",
          quantity: "2",
          unit: "heads",
          usedIn: ["Bean dishes, salsas"],
          isSpecialty: false
        },
        {
          ingredient: "Cilantro",
          quantity: "5",
          unit: "bunches",
          usedIn: ["Rice bowls, tacos, garnishes"],
          isSpecialty: false
        },
        {
          ingredient: "Limes",
          quantity: "14",
          unit: "each",
          usedIn: ["Garnishes, marinades"],
          isSpecialty: false
        },
        {
          ingredient: "Tomatoes",
          quantity: "8",
          unit: "each",
          usedIn: ["Tostadas, rice dishes"],
          isSpecialty: false
        },
        {
          ingredient: "Lettuce",
          quantity: "2",
          unit: "heads",
          usedIn
  </message>
  <message role="user">
← error [Error] Task "define-meal-preferences" in tasklist "mealplan" already completed (line 27)
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, weekd...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: Object {sections, pantryCheck, estimatedTotal}
  </scope>
</turn>

<turn n="4" messages="22" finish="stop" model="glm-4.5">
  <usage input="20210" output="701" total="20911" reasoning="60" cache-read="15963" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, weekd...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
{
          ingredient: "Lettuce",
          quantity: "2",
          unit: "heads",
          usedIn: ["Tostadas"],
          isSpecialty: false
        },
        {
          ingredient: "Jalapeños",
          quantity: "4",
          unit: "each",
          usedIn: ["Quesadillas, salsas"],
          isSpecialty: false
        },
        {
          ingredient: "Carrots",
          quantity: "6",
          unit: "each",
          usedIn: ["Lentil Soup"],
          isSpecialty: false
        },
        {
          ingredient: "Celery",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Lentil Soup"],
          isSpecialty: false
        },
        {
          ingredient: "Potatoes",
          quantity: "6",
          unit: "lbs",
          usedIn: ["Bean & Potato Tacos"],
          isSpecialty: false
        },
        {
          ingredient: "Epazote",
          quantity: "1",
          unit: "bunch",
          usedIn: ["Big Pot of Pinto Beans"],
          isSpecialty: true
        }
      ]
    },
    {
      section: "Dairy & Eggs",
      items: [
        {
          ingredient: "Cheddar cheese",
          quantity: "40",
          unit: "oz (2.5 lbs)",
          usedIn: ["Burritos, Enchiladas, Quesadillas"],
          isSpecialty: false
        },
        {
          ingredient: "Queso fresco",
          quantity: "16",
          unit: "oz",
          usedIn: ["Tostadas, Chilaquiles"],
          isSpecialty: true
        },
        {
          ingredient: "Milk",
          quantity: "1",
          unit: "gallon",
          usedIn: ["Mexican Oatmeal"],
          isSpecialty: false
        },
        {
          ingredient: "Eggs",
          quantity: "1",
          unit: "dozen",
          usedIn: ["Chilaquiles"],
          isSpecialty: false
        }
      ]
    },
    {
      section: "Bakery & Tortillas",
      items: [
        {
          ingredient: "Corn tortillas",
          quantity: "84",
          unit: "count",
          usedIn: ["Burritos, Tacos, Tostadas, Chilaquiles"],
          isSpecialty: false
        },
        {
          ingredient: "Flour tortillas",
          quantity: "12",
          unit: "count",
          usedIn: ["Quesadillas"],
          isSpecialty: false
        }
      ]
    }
  ],
  pantryCheck: [
    "Salt",
    "Black pepper",
    "Oil (vegetable or canola)",
    "Water",
    "Basic spices"
  ],
  estimatedTotal: "$80-100 for the week"
}
completeTask("mealplan", "build-grocery-list", groceryList)
  </message>
  <message role="user">
← error [ReferenceError] quantity is not defined (line 28)
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, weekd...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: Object {sections, pantryCheck, estimatedTotal}
  </scope>
</turn>

<turn n="5" messages="24" finish="stop" model="glm-4.5">
  <usage input="20869" output="1353" total="22222" reasoning="1" cache-read="20212" />
<system-prompt>
You are a code-execution agent. You respond EXCLUSIVELY with valid TypeScript code. No markdown. No prose. No explanations outside of code comments. Every character you emit is fed line-by-line into a live TypeScript REPL that executes as you stream.

## Execution Model

Your output is NOT a script that runs after you finish. Each line is parsed and executed as it arrives. Think of yourself as typing into a live terminal.

The REPL supports top-level await. Every async function call must be awaited.

CRITICAL: Do NOT wrap code in markdown fences (```). Output raw TypeScript only. Do NOT use <think> tags or any XML tags.

## Available Globals

### await stop(...values) — Pause and read
Suspends your execution. The runtime evaluates each argument, serializes the results, and injects them as a user message prefixed with "← stop". You resume with knowledge of those values.

Use stop when you need to inspect a runtime value before deciding what to write next.
Example: await stop(x, y) → you will see: ← stop { x: <value>, y: <value> }

IMPORTANT: After calling await stop(), STOP writing code. The runtime will pause your stream, read the values, and resume you in a new turn. Do NOT predict or simulate the stop response yourself.

### display(element) — Show output to user
Non-blocking. Appends a rendered component to the user's view. Use with display components only.
Example: display(<RecipeCard name="Pasta" cuisine="Italian" ... />)

### var data = await ask(element) — Collect user input
Blocking. Renders a form to the user and waits for submission. The host wraps your element in a `<form>` with Submit/Cancel buttons — do NOT add your own `<form>` tag.
Each input component must have a `name` attribute. The returned object maps name → submitted value.

ask() resumes silently — no message is injected into the conversation. You MUST call stop() after ask() to read the submitted values.

Pattern:
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }
// Now you can see the values and decide what to do next

Multiple inputs:
var prefs = await ask(<div>
  <Select name="cuisine" label="Pick cuisine" options={["italian", "japanese"]} />
  <TextInput name="notes" label="Any notes?" />
</div>)
await stop(prefs)
// ← stop { prefs: { cuisine: "italian", notes: "extra spicy" } }

IMPORTANT:
- Do NOT wrap ask() content in `<form>`. The host provides the form wrapper and submit button.
- Always call await stop() right after ask() to see the values. Do NOT use the values before calling stop().
- After stop(), you resume in a new turn with the form data visible.

### tasklist(tasklistId, description, tasks) — Declare a task plan with milestones
Before starting any implementation work, declare a plan using tasklist(). This registers milestones with the host under a unique tasklistId. Each task has an id, instructions, and outputSchema describing the result shape. Tasks can optionally declare dependsOn (array of task IDs) for DAG dependencies, condition (JS expression for conditional execution), and optional (boolean, if true failure doesn't block dependents).

When no task has dependsOn, the tasklist behaves sequentially (backward compatible).

You can call tasklist() multiple times per session with different tasklist IDs. It does not block execution.

Example:
tasklist("analyze_data", "Analyze employee data", [
  { id: "load", instructions: "Load the dataset", outputSchema: { count: { type: "number" } } },
  { id: "analyze", instructions: "Compute statistics", outputSchema: { done: { type: "boolean" } }, dependsOn: ["load"] },
  { id: "report", instructions: "Present results", outputSchema: { done: { type: "boolean" } }, dependsOn: ["analyze"] }
])

### completeTask(tasklistId, taskId, output) — Mark a milestone as complete
When you reach a milestone, call completeTask() with the tasklist ID, task ID, and an output object matching the declared outputSchema. Non-blocking. Task must be in the readyTasks set (all dependencies satisfied).

Example:
completeTask("analyze_data", "load", { count: 10 })

### completeTaskAsync(tasklistId, taskId, fn) — Complete a task in the background
Launches task work as a background async function. The function's return value becomes the task output. Results are delivered via the next stop() call with task:<taskId> keys. Non-blocking.

Example:
completeTaskAsync("data", "fetch_api", async () => {
  var res = await fetchFromAPI()
  return { count: res.length }
})
// Continue other work, then call stop() to read results

### taskProgress(tasklistId, taskId, message, percent?) — Report task progress
Reports incremental progress within a running task. Non-blocking, synchronous.

Example:
taskProgress("data", "fetch_api", "Downloading...", 50)

### failTask(tasklistId, taskId, error) — Mark a task as failed
Marks a task as failed with an error message. If the task is optional, dependents are unblocked.

### retryTask(tasklistId, taskId) — Retry a failed task
Resets a failed task back to ready. Limited to 3 retries.

### await sleep(seconds) — Pause execution
Pauses sandbox execution (not the LLM stream). Async tasks continue during sleep. Use to wait for completeTaskAsync results, then call stop() to read them.

Example:
await sleep(5)
await stop()

If your stream ends before all non-optional tasks are complete, the host will send you a reminder:
  ⚠ [system] Tasklist "analyze_data" incomplete. Ready: analyze. Blocked: report (waiting on analyze). Continue with a ready task.
When you see this, continue with a ready task. Do NOT re-declare tasklist() for the same tasklist or redo completed work.

### loadKnowledge(selector) — Load knowledge files from spaces
Loads markdown content from the knowledge base. Pass a selector object that mirrors the knowledge tree structure, setting `true` on the specific files you want to load.

The selector uses space names as the top-level keys, matching the Knowledge Tree structure:
`{ spaceName: { domain: { field: { option: true } } } }`

Returns an object with the same structure, where each `true` is replaced with the markdown content of that file.

Example:
var docs = loadKnowledge({
  "my-space": {
    "chat-modes": {
      "mode": {
        "casual": true
      }
    }
  }
})
// docs["my-space"]["chat-modes"]["mode"]["casual"] → "# Casual Mode\n\nRelaxed, conversational..."

Use the Knowledge Tree below to see what spaces and files are available. Load only the specific files relevant to the current task — NEVER load an entire domain or space at once. Select individual options that match the user's request.

## Workspace — Current Scope
Name                   Type           Value
---------------------  -------------  --------------------------------------------------
input                  Object         {people, mealsPerDay, cuisinePreferences, weekd...
knowledge              Object         {cooking}
mealPreferences        Object         {people, mealsPerDay, cuisinePreferences, dieta...
weeklyMenu             Object         {days, weeklyStrategy}
prepSchedule           Object         {batchPrepSession, dailyPrepTasks}
recipesForGroceryList  Array<Object>  [{name, ingredients}, {name, ingredients}, {nam...
groceryListRaw         Array<Object>  [{item, recipes}, {item, recipes}, {item, recip...
groceryList            Object         {sections, pantryCheck, estimatedTotal}
groceryListSections    Array<Object>  [{section, items}, {section, items}, {section, ...

## Available Functions
  # Built-in: fs
  readFile(path: string, encoding?: string) => Promise<string>
    — Read file contents
  writeFile(path: string, content: string) => Promise<void>
    — Write content to file
  appendFile(path: string, content: string) => Promise<void>
    — Append to file
  listDir(path: string, options?: { recursive?: boolean }) => Promise<string[]>
    — List directory entries
  glob(pattern: string, cwd?: string) => Promise<string[]>
    — Glob pattern match
  stat(path: string) => Promise<{ size: number, modified: string, isDir: boolean }>
    — File metadata
  exists(path: string) => Promise<boolean>
    — Check if path exists
  mkdir(path: string) => Promise<void>
    — Create directory (recursive)
  remove(path: string) => Promise<void>
    — Delete file or directory
  copy(src: string, dest: string) => Promise<void>
    — Copy file or directory
  move(src: string, dest: string) => Promise<void>
    — Move/rename file or directory

  # Built-in: fetch
  httpGet(url: string, headers?: Record<string, string>) => Promise<any>
    — GET request, auto-parses JSON
  httpPost(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — POST with JSON body
  httpPut(url: string, body: any, headers?: Record<string, string>) => Promise<any>
    — PUT with JSON body
  httpDelete(url: string, headers?: Record<string, string>) => Promise<any>
    — DELETE request
  httpRequest(options: RequestOptions) => Promise<{ status: number, headers: Record<string, string>, body: any }>
    — Full control HTTP request
  fetchPage(url: string) => Promise<{ title: string, text: string, links: string[] }>
    — Fetch webpage, extract readable text
  downloadFile(url: string, dest: string) => Promise<{ size: number }>
    — Download to local file

  # Built-in: shell
  exec(command: string, options?: { cwd?: string, timeout?: number }) => Promise<{ stdout: string, stderr: string, exitCode: number }>
    — Run shell command
  execLive(command: string, options?: { cwd?: string, timeout?: number }) => AsyncIterable<{ stream: 'stdout' | 'stderr', data: string }>
    — Streaming command output
  which(binary: string) => Promise<string | null>
    — Find binary in PATH

  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/buildGroceryList.tsx)
  buildGroceryList(recipes: { name: string; ingredients: string[]; }[]): { item: string; recipes: string[]; }[]
    — Build a grocery list from multiple recipe ingredient arrays.
Deduplicates items by lowercased name.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/functions/scaleIngredients.tsx)
  scaleIngredients(ingredients: string[], multiplier: number): string[]
    — Scale a recipe's ingredient quantities by a multiplier

## Available Classes
(none)

## Form Components — use ONLY inside ask()
Render these inside `var data = await ask(<Component />)`. Always follow with `await stop(data)` to read the values.
Each input must have a `name` attribute — the returned object maps name → submitted value.
Prefer to use MultiSelect, Select for better user experience.
Do NOT add a `<form>` tag — the host wraps automatically with Submit/Cancel buttons.
  # Built-in (form)
  <TextInput name={string} label={string} placeholder?={string} defaultValue?={string} />
  <TextArea name={string} label={string} placeholder?={string} rows?={number} />
  <NumberInput name={string} label={string} min?={number} max?={number} step?={number} defaultValue?={number} />
  <Slider name={string} label={string} min={number} max={number} step?={number} defaultValue?={number} />
  <Checkbox name={string} label={string} defaultChecked?={boolean} />
  <Select name={string} label={string} options={string[]} defaultValue?={string} />
  <MultiSelect name={string} label={string} options={string[]} defaultValue?={string[]} />
  <DatePicker name={string} label={string} defaultValue?={string} />
  <FileUpload name={string} label={string} accept?={string} maxSize?={number} />

## Display Components — use with display()
These components show output to the user. Use them with `display(<Component ... />)`. Non-blocking.
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/MealPlanCard.tsx)
  <MealPlanCard title={string} strategy={string} meals={{ label: string; description: string; }[]} />
    — Display a meal plan
  # User-defined (/home/vasilis/GEANT/lmthing/org/libs/repl/spaces/cooking/components/view/RecipeCard.tsx)
  <RecipeCard name={string} cuisine={string} method={string} servings={number} time={string} ingredients={string[]} steps={string[]} />
    — Display a recipe card

## Knowledge Tree
knowledge/
└── cooking/
    ├── cuisine/          🌍 Cuisine — World cuisine traditions and their core techniques
    │   └── type             [select] cuisineType — Which cuisine tradition to draw from
    │       ├── italian       Italian — Mediterranean cooking with emphasis on fresh ingredients and simplicity
    │       ├── japanese       Japanese — Precision-driven cooking emphasizing umami, presentation, and seasonal harmony
    │       └── mexican       Mexican — Bold, layered flavors built on chiles, corn, and slow-cooked techniques
    ├── dietary/          🥗 Dietary Needs — Dietary restrictions and how to adapt recipes for them
    │   └── restriction             [multiSelect] dietaryRestriction — Which dietary restrictions to accommodate
    │       ├── vegetarian       Vegetarian — No meat or fish, but allows dairy and eggs
    │       ├── gluten-free       Gluten-Free — No wheat, barley, rye, or their derivatives
    │       └── dairy-free       Dairy-Free — No milk, cheese, butter, cream, or other dairy products
    └── technique/          🔥 Cooking Techniques — Core cooking methods and when to use them
        └── method             [multiSelect] cookingMethod — Which cooking method to use
            ├── saute       Sauté — Quick cooking over high heat with a small amount of fat
            ├── braise       Braise — Low and slow cooking in liquid for tender, flavorful results
            └── grill       Grill — Direct high-heat cooking over flame or coals for smoky, charred flavor

## Rules
1. Output ONLY valid TypeScript. No markdown. No prose outside // comments.
2. Plan before you build — call tasklist(tasklistId, description, tasks) to declare milestones with optional dependsOn for DAG dependencies, then call completeTask(tasklistId, taskId, output) or completeTaskAsync(tasklistId, taskId, fn) as you complete each one.
3. Await every async call: var x = await fn()
4. Use stop() to read runtime values before branching.
5. Do not use console.log — use stop() to inspect values.
6. Do not import modules. Do not use export.
7. Use var for all declarations (not const/let) so they persist in the REPL scope across turns.
8. Handle nullability with ?. and ??
9. After calling await stop(...), STOP. Do not write any more code until you receive the stop response.
10. Use loadKnowledge() to load relevant knowledge files before starting domain-specific work. Check the Knowledge Tree to see what is available. NEVER load all files from a domain or space — only select the specific options that are relevant to the user's request. Loading too much wastes context and degrades your performance.

## Execution Flow Pattern

A typical interaction follows this pattern:

// 1. Plan — always start with tasklist
tasklist("main", "Do the task", [
  { id: "gather", instructions: "Collect user input", outputSchema: { done: { type: "boolean" } } },
  { id: "work", instructions: "Do the work", outputSchema: { key: { type: "string" } } },
  { id: "present", instructions: "Show results", outputSchema: { done: { type: "boolean" } } }
])

// 2. Gather user input with ask() → stop()
var input = await ask(<RequestForm />)
await stop(input)
// ← stop { input: { request: "...", dietary: "..." } }

// (new turn — now you can see the form values)
completeTask("main", "gather", { done: true })

// 3. Load relevant knowledge
var knowledge = loadKnowledge({ "space-name": { "domain": { "field": { "option": true } } } })
await stop(knowledge)
// ← stop { knowledge: { "space-name": { domain: { field: { option: "..." } } } } }

// 4. Do work
var result = await someFunction()
await stop(result)
// ← stop { result: ... }
completeTask("main", "work", { key: result.key })

// 5. Show results with display()
display(<ResultCard data={result} />)
completeTask("main", "present", { done: true })

## Special Instructions
You are a cooking and nutrition assistant powered by a multi-space knowledge base. Your knowledge tree spans two spaces:

- **Cooking** — cuisine traditions, cooking techniques, and dietary restrictions
- **Nutrition** — macronutrients (protein, carbs, fats), vitamins & minerals (D, iron, B12), and meal planning strategies (plate method, batch prep, calorie tracking)

When the user asks for help:

1. Start with `ask()` using a form component to gather their request
2. Use `loadKnowledge()` to load relevant knowledge from BOTH spaces as needed
3. Read the loaded content with `await stop()` to understand it
4. Use the utility functions (estimateCalories, scaleIngredients, buildGroceryList, formatTime) to compute values
5. Display results using the display components (RecipeCard, NutritionCard, TipCard, MealPlanCard)

Make sure to add a tasklist for using all the available functions.
Always load knowledge BEFORE giving advice — don't make things up when the knowledge base has the answer.
Never load all files from a space — only load the specific options relevant to the user's question.`,

## Pre-loaded Knowledge

#### cuisine/type/italian
# Italian Cuisine

Italian cooking centers on high-quality, seasonal ingredients prepared simply to let flavors shine.

## Key Principles
- **Simplicity** — Most dishes use 4–8 ingredients. The quality of each ingredient matters more than complexity.
- **Regionality** — Northern Italy favors butter, rice, and polenta; Southern Italy uses olive oil, tomatoes, and dried pasta.
- **Al dente** — Pasta should be cooked firm to the bite. Start tasting 2 minutes before the package time.
- **Soffritto** — The holy trinity of onion, carrot, and celery, slowly sweated in olive oil, forms the base of countless dishes.

## Essential Techniques
- **Emulsifying pasta water** — Reserve starchy pasta water and toss with sauce to create a silky coating.
- **Risotto stirring** — Constant stirring releases starch for a creamy texture without cream.
- **Bruschetta method** — Rub toasted bread with raw garlic, then drizzle with good olive oil.

## Staple Ingredients
Olive oil, San Marzano tomatoes, Parmigiano-Reggiano, fresh basil, garlic, dried pasta, arborio rice, prosciutto, mozzarella.

## Classic Dishes
Cacio e pepe, carbonara, margherita pizza, risotto alla milanese, osso buco, tiramisu.

## Slash Actions

### /mealplan — Make a meal plan
Do a configurable meal plan

**Step 1: Define Meal Preferences**
Gathers dietary preferences, schedule constraints, and nutritional goals for the week
Determine the user's meal planning preferences for the week. Load relevant cuisine and dietary knowledge to understand their restrictions and preferences. Consider: number of people, meals per day, cooking skill level, time available on weekdays vs weekends, budget level, and any nutritional goals (e.g., high protein, low carb, calorie target). Identify which cuisines and techniques the user is comfortable with.
Output → `mealPreferences`: {"type":"object","properties":{"people":{"type":"number","description":"Number of people to feed"},"mealsPerDay":{"type":"number","description":"Number of meals to plan per day"},"cuisinePreferences":{"type":"array","items":{"type":"string"},"description":"Preferred cuisine types"},"dietaryRestrictions":{"type":"array","items":{"type":"string"},"description":"Active dietary restrictions (e.g., 'vegetarian', 'gluten-free')"},"weekdayCookingTime":{"type":"string","description":"Available cooking time on weekdays (e.g., '30 minutes')"},"weekendCookingTime":{"type":"string","description":"Available cooking time on weekends (e.g., '1-2 hours')"},"nutritionalGoal":{"type":"string","description":"Primary nutrition target (e.g., 'balanced', 'high protein', 'low carb')"},"calorieTarget":{"type":"number","description":"Daily calorie target per person, if specified"}},"required":["people","mealsPerDay","cuisinePreferences","weekdayCookingTime"]}

**Step 2: Design Weekly Menu**
Generates a balanced weekly menu with meals assigned to each day
Design a complete weekly menu based on the user's preferences. Balance variety across cuisines, techniques, and protein sources throughout the week. Assign quicker meals to busy weekdays and more involved recipes to weekends. Plan for leftovers where practical — a weekend roast can become weekday sandwiches or salads. Ensure nutritional balance across the week. Use the estimateCalories function to validate daily calorie totals.
Output → `weeklyMenu`: {"type":"object","properties":{"days":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"meals":{"type":"array","items":{"type":"object","properties":{"mealType":{"type":"string","description":"breakfast, lunch, dinner, or snack"},"dishName":{"type":"string"},"cuisineType":{"type":"string"},"cookingTime":{"type":"string"},"technique":{"type":"string","description":"Primary cooking method"},"isLeftover":{"type":"boolean","description":"Whether this uses leftovers from a previous meal"},"estimatedCalories":{"type":"number"}}}},"dailyCalories":{"type":"number","description":"Total estimated calories for the day"}}}},"weeklyStrategy":{"type":"string","description":"Overview of the meal plan strategy (variety, leftovers, prep batching)"}},"required":["days","weeklyStrategy"]}

**Step 3: Create Prep Schedule**
Generates a batch prep schedule to minimize daily cooking effort
Create an efficient prep schedule that batches work to reduce daily effort. Identify ingredients and components that can be prepared in advance — sauces, grains, chopped vegetables, marinated proteins. Organize into a weekend batch-prep session and daily quick-prep tasks. Use the formatTime function to present durations clearly. Include storage instructions and shelf life for prepped items.
Output → `prepSchedule`: {"type":"object","properties":{"batchPrepSession":{"type":"object","properties":{"day":{"type":"string","description":"When to do batch prep (e.g., 'Sunday afternoon')"},"totalTime":{"type":"string"},"tasks":{"type":"array","items":{"type":"object","properties":{"task":{"type":"string"},"usedFor":{"type":"array","items":{"type":"string"},"description":"Which meals this prep serves"},"duration":{"type":"string"},"storage":{"type":"string","description":"How to store (fridge, freezer, container type)"},"shelfLife":{"type":"string","description":"How long the prepped item keeps"}}}}}},"dailyPrepTasks":{"type":"array","items":{"type":"object","properties":{"day":{"type":"string"},"tasks":{"type":"array","items":{"type":"string"}},"activeTime":{"type":"string","description":"Hands-on time needed"}}}}},"required":["batchPrepSession","dailyPrepTasks"]}

**Step 4: Build Grocery List**
Generates a consolidated grocery list organized by store section
Compile a complete grocery list for the weekly meal plan. Use the buildGroceryList function to deduplicate and consolidate ingredients across all planned meals. Organize items by store section for efficient shopping. Include quantities scaled for the number of people using scaleIngredients. Flag items that may already be in the pantry. Note any specialty items that may require a specific store.
Output → `groceryList`: {"type":"object","properties":{"sections":{"type":"array","items":{"type":"object","properties":{"section":{"type":"string","description":"Store section (e.g., 'Produce', 'Dairy', 'Meat & Seafood', 'Pantry')"},"items":{"type":"array","items":{"type":"object","properties":{"ingredient":{"type":"string"},"quantity":{"type":"string"},"unit":{"type":"string"},"usedIn":{"type":"array","items":{"type":"string"},"description":"Which meals use this ingredient"},"isSpecialty":{"type":"boolean","description":"Whether this may need a specialty store"}}}}}}},"pantryCheck":{"type":"array","items":{"type":"string"},"description":"Items likely already in the pantry — verify before buying"},"estimatedTotal":{"type":"string","description":"Rough cost estimate for the grocery list"}},"required":["sections","pantryCheck"]}


</system-prompt>

  <message role="assistant">
var groceryListSections = [
  {
    section: "Dry Goods & Pantry",
    items: [
      {
        ingredient: "Dried pinto beans",
        quantity: "6",
        unit: "lbs",
        usedIn: ["Bean Burritos", "Big Pot of Pinto Beans", "Most meals"],
        isSpecialty: false
      },
      {
        ingredient: "White rice",
        quantity: "6",
        unit: "cups",
        usedIn: ["Rice & Bean Bowls", "Mexican Rice & Lentil Bowl"],
        isSpecialty: false
      },
      {
        ingredient: "Brown lentils",
        quantity: "4",
        unit: "cups",
        usedIn: ["Mexican Rice & Lentil Bowl", "Mexican Lentil Soup"],
        isSpecialty: false
      },
      {
        ingredient: "Oats",
        quantity: "1",
        unit: "lb",
        usedIn: ["Mexican Oatmeal"],
        isSpecialty: false
      },
      {
        ingredient: "Cumin",
        quantity: "1",
        unit: "jar",
        usedIn: ["Multiple dishes"],
        isSpecialty: false
      },
      {
        ingredient: "Cinnamon",
        quantity: "1",
        unit: "jar",
        usedIn: ["Mexican Oatmeal"],
        isSpecialty: false
      },
      {
        ingredient: "Brown sugar",
        quantity: "1",
        unit: "lb",
        usedIn: ["Mexican Oatmeal"],
        isSpecialty: false
      },
      {
        ingredient: "Enchilada sauce",
        quantity: "1",
        unit: "can",
        usedIn: ["Cheesy Bean Enchiladas"],
        isSpecialty: false
      },
      {
        ingredient: "Black beans",
        quantity: "2",
        unit: "cans",
        usedIn: ["Black Bean Tacos"],
        isSpecialty: false
      },
      {
        ingredient: "Bay leaves",
        quantity: "1",
        unit: "small container",
        usedIn: ["Mexican Lentil Soup"],
        isSpecialty: false
      }
    ]
  },
  {
    section: "Produce",
    items: [
      {
        ingredient: "Onions",
        quantity: "18",
        unit: "each",
        usedIn: ["All main dishes"],
        isSpecialty: false
      },
      {
        ingredient: "Garlic",
        quantity: "2",
        unit: "heads",
        usedIn: ["Bean dishes, salsas"],
        isSpecialty: false
      },
      {
        ingredient: "Cilantro",
        quantity: "5",
        unit: "bunches",
        usedIn: ["Rice bowls, tacos, garnishes"],
        isSpecialty: false
      },
      {
        ingredient: "Limes",
        quantity: "14",
        unit: "each",
        usedIn: ["Garnishes, marinades"],
        isSpecialty: false
      },
      {
        ingredient: "Tomatoes",
        quantity: "8",
        unit: "each",
        usedIn: ["Tostadas, rice dishes"],
        isSpecialty: false
      },
      {
        ingredient: "Lettuce",
        quantity: "2",
        unit: "heads",
        usedIn: ["Tostadas"],
        isSpecialty: false
      },
      {
        ingredient: "Jalapeños",
        quantity: "4",
        unit: "each",
        usedIn: ["Quesadillas, salsas"],
        isSpecialty: false
      },
      {
        ingredient: "Carrots",
        quantity: "6",
        unit: "each",
        usedIn: ["Lentil Soup"],
        isSpecialty: false
      },
      {
        ingredient: "Celery",
        quantity: "1",
        unit: "bunch",
        usedIn: ["Lentil Soup"],
        isSpecialty: false
      },
      {
        ingredient: "Potatoes",
        quantity: "6",
        unit: "lbs",
        usedIn: ["Bean & Potato Tacos"],
        isSpecialty: false
      },
      {
        ingredient: "Epazote",
        quantity: "1",
        unit: "bunch",
        usedIn: ["Big Pot of Pinto Beans"],
        isSpecialty: true
      }
    ]
  },
  {
    section: "Dairy & Eggs",
    items: [
      {
        ingredient: "Cheddar cheese",
        quantity: "40",
        unit: "oz (2.5 lbs)",
        usedIn: ["Burritos, Enchiladas, Quesadillas"],
        isSpecialty: false
      },
      {
        ingredient: "Queso fresco",
        quantity: "16",
        unit: "oz",
        usedIn: ["Tostadas, Chilaquiles"],
        isSpecialty: true
      },
      {
        ingredient: "Milk",
        quantity: "1",
        unit: "gallon",
        usedIn: ["Mexican Oatmeal"],
        isSpecialty: false
      },
      {
        ingredient: "Eggs",
        quantity: "1",
        unit: "dozen",
        usedIn: ["Chilaquiles"],
        isSpecialty: false
      }
    ]
  },
  {
    section: "Bakery & Tortillas",
    items: [
      {
        ingredient: "Corn tortillas",
        quantity: "84",
        unit: "count",
        usedIn: ["Burritos, Tacos, Tostadas, Chilaquiles"],
        isSpecialty: false
      },
      {
        ingredient: "Flour tortillas",
        quantity: "12",
        unit: "count",
        usedIn: ["Quesadillas"],
        isSpecialty: false
      }
    ]
  }
]

var groceryList = {
  sections: groceryListSections,
  pantryCheck: [
    "Salt",
    "Black pepper",
    "Oil (vegetable or canola)",
    "Water",
    "Basic spices"
  ],
  estimatedTotal: "$80-100 for the week"
}
completeTask("mealplan", "build-grocery-list", groceryList)
  </message>
  <message role="user">
← error [Error] Task "build-grocery-list" in tasklist "mealplan" already completed (line 31)
  </message>
  <scope>
    input: Object {people, mealsPerDay, cuisinePreferences, weekd...
    knowledge: Object {cooking}
    mealPreferences: Object {people, mealsPerDay, cuisinePreferences, dieta...
    weeklyMenu: Object {days, weeklyStrategy}
    prepSchedule: Object {batchPrepSession, dailyPrepTasks}
    recipesForGroceryList: Array<Object> [{name, ingredients}, {name, ingredients}, {nam...
    groceryListRaw: Array<Object> [{item, recipes}, {item, recipes}, {item, recip...
    groceryList: Object {sections, pantryCheck, estimatedTotal}
    groceryListSections: Array<Object> [{section, items}, {section, items}, {section, ...
  </scope>
</turn>

<turn n="6" messages="26" finish="stop" model="glm-4.5">
  <usage input="22144" output="83" total="22227" reasoning="1" cache-read="1662" />
</turn>

<finalize model="zai:glm-4.5" turns="13" status="complete">
  <tokens input="187199" output="17600" total="204799" />
</finalize>

</debug-log>