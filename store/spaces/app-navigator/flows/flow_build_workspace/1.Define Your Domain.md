---
description: "Generates domain definition including scope, target users, and core use cases"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Analyze the user's workspace idea and generate a structured domain definition. Identify the subject area, target user personas, 2-3 core use cases the workspace will address, and the key expert knowledge categories that will form the knowledge base. The domain definition should be specific enough to guide all subsequent workspace design decisions.

<output target="domainDefinition">
{
  "type": "object",
  "properties": {
    "domainName": {
      "type": "string",
      "description": "Concise name for the domain (e.g., 'customer-support', 'recipe-management')"
    },
    "description": {
      "type": "string",
      "description": "One-paragraph summary of what this workspace covers"
    },
    "targetUsers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "User personas who will use this workspace"
    },
    "coreUseCases": {
      "type": "array",
      "items": { "type": "string" },
      "description": "2-3 primary problems this workspace solves"
    },
    "knowledgeCategories": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Key knowledge areas an expert in this domain would carry"
    }
  },
  "required": ["domainName", "description", "targetUsers", "coreUseCases", "knowledgeCategories"]
}
</output>