---
description: "Generates concrete example usage with sample data showing the formula in action"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Create a concrete example demonstrating the formula with realistic sample data. Generate a small sample dataset (3-5 rows), show the formula applied with actual cell references, display the expected output for each row, and provide a step-by-step walkthrough of how the formula processes one row. Tailor the sample data to the user's industry and use case from knowledge context.

<output target="exampleUsage">
{
  "type": "object",
  "properties": {
    "sampleData": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "Row of sample data with column headers as keys"
      },
      "description": "3-5 rows of realistic sample data"
    },
    "appliedFormula": {
      "type": "string",
      "description": "The formula with actual cell references for the sample data"
    },
    "expectedResults": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "row": { "type": "number" },
          "result": { "type": "string" },
          "explanation": { "type": "string" }
        }
      },
      "description": "Expected output for each sample row"
    },
    "walkthrough": {
      "type": "string",
      "description": "Step-by-step trace of how the formula processes one row"
    }
  },
  "required": ["sampleData", "appliedFormula", "expectedResults", "walkthrough"]
}
</output>