---
description: "Generates user stories for the identified page with interaction descriptions"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Generate the key user stories for the selected page using the format: "As a [user type], I want to [action], so that [benefit]." For each story, describe what the user sees on screen and what happens when they interact. Cover all primary workflows available on this page. Use the page-specific story IDs (US-001+ for Home, US-101+ for Studio Dashboard, US-201+ for Agent Config, US-301+ for Knowledge Area Details).

<output target="userStories">
{
  "type": "object",
  "properties": {
    "stories": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "Story identifier (e.g., US-101)" },
          "asA": { "type": "string", "description": "User role" },
          "iWantTo": { "type": "string", "description": "Desired action" },
          "soThat": { "type": "string", "description": "Expected benefit" },
          "interaction": { "type": "string", "description": "What the user sees and does" }
        }
      }
    }
  },
  "required": ["stories"]
}
</output>