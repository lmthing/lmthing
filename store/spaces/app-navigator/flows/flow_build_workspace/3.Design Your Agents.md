---
description: "Generates agent specifications with roles, instructions, and knowledge attachments"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.4
---

Design 2-3 agents with distinct, complementary roles for the workspace. For each agent, generate a PascalCase name, one-sentence description, system prompt defining personality and communication style, the list of knowledge domains to attach, and recommended tools. Keep instructions focused and avoid redundancy with knowledge content. Agents should reference domain knowledge naturally (e.g., "Use the user's experience level to calibrate your response").

<output target="agentDesigns">
{
  "type": "object",
  "properties": {
    "agents": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "PascalCase agent name" },
          "description": { "type": "string", "description": "One-sentence expertise description" },
          "systemPrompt": { "type": "string", "description": "Full system prompt defining role, personality, tone, and boundaries" },
          "knowledgeDomains": { "type": "array", "items": { "type": "string" }, "description": "Domain slugs to attach" },
          "tools": { "type": "array", "items": { "type": "string" }, "description": "Recommended tool names in kebab-case" }
        }
      }
    }
  },
  "required": ["agents"]
}
</output>