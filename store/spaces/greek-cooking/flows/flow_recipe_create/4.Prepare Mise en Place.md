---
description: "Generates preparation instructions including cutting, pre-cooking steps, and station setup"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Generate detailed mise en place instructions for the selected dish. Include cutting techniques (Greek cuisine prefers rustic cuts), pre-cooking steps (salting eggplant, soaking beans, marinating meat), ingredient grouping by cooking stage, equipment needed (heavy-bottomed pans, clay pots, phyllo brush), and estimated preparation time. Adapt complexity to the user's skill level from knowledge context.

<output target="miseEnPlace">
{
  "type": "object",
  "properties": {
    "cuttingInstructions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "ingredient": { "type": "string" },
          "technique": { "type": "string" },
          "notes": { "type": "string" }
        }
      }
    },
    "preCookingSteps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "step": { "type": "string" },
          "duration": { "type": "string" },
          "timing": { "type": "string", "description": "When to start relative to cooking (e.g., '30 min before', 'night before')" }
        }
      }
    },
    "equipmentNeeded": {
      "type": "array",
      "items": { "type": "string" }
    },
    "estimatedPrepTime": {
      "type": "string",
      "description": "Total preparation time estimate"
    }
  },
  "required": ["cuttingInstructions", "preCookingSteps", "equipmentNeeded", "estimatedPrepTime"]
}
</output>