{
  "name": "integration-sms",
  "version": "1.0.0",
  "private": true,
  "lmthing": {
    "kind": "integration",
    "title": "SMS",
    "tags": ["integration", "messaging"],
    "icon": "📱",
    "description": "Send and receive text messages (SMS/MMS) through your own Twilio number via THING.",
    "settings": {
      "type": "object",
      "properties": {
        "INTEGRATION_SMS_ACCOUNT_SID": { "type": "string", "title": "Account SID", "format": "password" },
        "INTEGRATION_SMS_AUTH_TOKEN": { "type": "string", "title": "Auth Token", "format": "password" },
        "INTEGRATION_SMS_FROM_NUMBER": { "type": "string", "title": "Your Twilio number, E.164 e.g. +15551234567" }
      },
      "required": ["INTEGRATION_SMS_ACCOUNT_SID", "INTEGRATION_SMS_AUTH_TOKEN", "INTEGRATION_SMS_FROM_NUMBER"]
    },
    "connection": {
      "provider": "sms",
      "apiBase": "https://api.twilio.com/2010-04-01/Accounts/{env:INTEGRATION_SMS_ACCOUNT_SID}",
      "tokenEnv": "INTEGRATION_SMS_AUTH_TOKEN",
      "auth": { "kind": "basic", "userEnv": "INTEGRATION_SMS_ACCOUNT_SID" }
    }
  }
}
