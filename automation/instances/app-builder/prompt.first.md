# Autonomous task — build & ship the `{{task}}` project-application (round {{round}}: {{roundMode}})

This is the **first round** for `{{task}}`.

**Round 1 — CORE BUILD.** Establish the app exactly as its spec describes: the core database, the
one project-scoped space and its agents, the core pages/api/hooks, and the core loop working
end-to-end **with the live model**. Modest, high-quality spec improvements are welcome; the goal is
a correct, green baseline. (Aim to already ship ≥2 project-scoped spaces where sensible — any app
still at one space MUST reach ≥2 on its next expansion round.)

{{include:prompt.common.md}}

Begin now with Phase 0.
