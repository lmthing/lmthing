---
description: "Identifies which platform page the user wants explained and resolves its route"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Determine which lmthing platform page the user wants to understand. Match their description or URL to one of the known pages: Home Page (/), Studio Dashboard (/workspace/[workspaceId]/studio), Agent Configuration (/workspace/[workspaceId]/studio/agent/[agentId]), or Knowledge Area Details (/workspace/[workspaceId]/studio/domain/[domainId]). Resolve the canonical page name and route pattern.

<output target="pageIdentification">
{
  "type": "object",
  "properties": {
    "pageName": {
      "type": "string",
      "description": "Canonical page name (e.g., 'Home Page', 'Studio Dashboard')"
    },
    "routePattern": {
      "type": "string",
      "description": "URL route pattern for this page"
    },
    "pageCategory": {
      "type": "string",
      "enum": ["landing", "dashboard", "configuration", "editor"],
      "description": "General category of the page"
    },
    "summary": {
      "type": "string",
      "description": "One-sentence summary of what this page is for"
    }
  },
  "required": ["pageName", "routePattern", "pageCategory", "summary"]
}
</output>