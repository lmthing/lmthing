---
id: knowledge
dependsOn:
  - scaffold
output:
  domains: array
---

Author the knowledge tree with write_knowledge: `domain/field/option`, one topic per leaf, each leaf under one screen. Record the `domain/field` refs as `domains` — the agent in the next step will declare them.