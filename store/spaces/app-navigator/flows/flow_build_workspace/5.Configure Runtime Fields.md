---
description: "Generates runtime field configuration for agents requiring user input at conversation start"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

Analyze each agent's knowledge domain attachments and determine which fields should be runtime fields (values that prompt the user for input when starting a conversation). Generate the runtimeFields configuration mapping domain slugs to field slugs. Mark frequently-changing values as runtime fields. Include default values for optional fields and required flags for essential fields.

<output target="runtimeConfig">
{
  "type": "object",
  "properties": {
    "agentConfigs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "agentName": { "type": "string", "description": "PascalCase agent name" },
          "runtimeFields": {
            "type": "object",
            "description": "Mapping of domain-slug to array of field-slug strings"
          }
        }
      }
    }
  },
  "required": ["agentConfigs"]
}
</output>