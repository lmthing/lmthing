---
description: "Generates the final store submission package and post-launch verification checklist"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.2
---

You are finalizing the store submission for an agent. Using the readiness assessment, distribution model, description, and pricing from previous steps, produce the submission package and a post-launch verification checklist.

Submission process by distribution model:

**Free and source purchase:**
- Package the agent files: config.json, instruct.md, knowledge domains, and flows
- The store validates the package structure
- Listing goes live with the description and pricing configured earlier

**API access:**
- The agent must first be deployed to a K8s space (use space-deploy flow if not already deployed)
- Link the running deployment to the store listing
- The store routes API requests to the space and handles billing

Post-submission verification:
1. Listing appears correctly on the store
2. Description renders properly with formatting intact
3. Pricing is displayed accurately
4. Preview/demo works if included
5. Test the purchase/download/API flow from a buyer's perspective

Generate the submission package and include monitoring recommendations for tracking listing performance after launch.

<output target="storeSubmission">
{
  "type": "object",
  "properties": {
    "packageContents": { "type": "array", "items": { "type": "string" }, "description": "List of files included in the submission package" },
    "deploymentRequired": { "type": "boolean", "description": "Whether a K8s deployment is needed for this listing" },
    "deploymentSlug": { "type": "string", "description": "Slug of the K8s space if API access model, null otherwise" },
    "verificationChecklist": { "type": "array", "items": { "type": "object", "properties": { "check": { "type": "string" }, "expectedResult": { "type": "string" } } }, "description": "Post-submission verification checks" },
    "performanceMetrics": { "type": "array", "items": { "type": "string" }, "description": "Metrics to track after launch (downloads, revenue, feedback)" },
    "iterationPlan": { "type": "string", "description": "Plan for iterating on the listing based on performance data" }
  },
  "required": ["packageContents", "deploymentRequired", "verificationChecklist", "performanceMetrics"]
}
</output>
