---
description: "Generates guest profile analysis including dietary needs, group size, and budget"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: 0.3
---

Analyze the guest parameters to shape the menu. Determine group size (intimate 2-4, standard 6-10, large 12+), identify dietary requirements (religious fasting/nisteia, vegetarian, nut-free, gluten-free, dairy-free), assess cooking capacity (oven space, burners, prep-ahead capability), and set budget constraints (seafood and lamb are premium, legumes are economical). Greek cuisine naturally accommodates many dietary restrictions through its rich ladera and seafood traditions.

<output target="guestProfile">
{
  "type": "object",
  "properties": {
    "groupSize": {
      "type": "number",
      "description": "Number of guests"
    },
    "groupCategory": {
      "type": "string",
      "enum": ["intimate", "standard", "large"],
      "description": "Group size category"
    },
    "dietaryRestrictions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Dietary restrictions to accommodate"
    },
    "fastingRequired": {
      "type": "boolean",
      "description": "Whether religious fasting rules apply"
    },
    "cookingCapacity": {
      "type": "object",
      "properties": {
        "ovenSlots": { "type": "number" },
        "stovetopBurners": { "type": "number" },
        "canPrepAhead": { "type": "boolean" }
      }
    },
    "budgetLevel": {
      "type": "string",
      "enum": ["economical", "moderate", "generous"],
      "description": "Budget level for ingredients"
    }
  },
  "required": ["groupSize", "groupCategory", "dietaryRestrictions"]
}
</output>