# Attempt ledger — 06-tanzania

Cross-round memory for the judge. **Read this before attributing a failure**; **append one entry at
the end of every round.** One line per attempt. This is how a fresh-context round knows a rung is
already exhausted and it must CLIMB instead of re-trying the same rung.

Format per line:
`R<round> · step <N> · <L0|L1|L2|L3> · <file#symbol> · verify=<PASS|FAIL|INTERRUPTED> · <evidence>`

## step 1 — the dump (attachments → offer, nothing built)
- R4 · step 1 · L1 · user-thing/thing/instruct.md (no research during setup; complete per-part spaces; page-repair-is-write) · verify=PASS · committed
- R5 · step 1 · L1 · user-thing/thing/instruct.md (one final offer, not a progress placeholder; parallel Promise.all attachment reads) · verify=PASS · committed
- R6 · step 1 · L1 · user-thing/thing/instruct.md (delegate results are PLAIN TEXT, not objects) + prompt-contract.test.ts · verify=PASS · committed
- R8 · step 1 · — · (no change) · verify=PASS · attachment flow stable across a fresh replay
- R9 · step 1 · — · (no change) · verify=PASS · stable

## step 2 — the bare "yes" → per-leg specialist spaces + DB + app, no research
The recurring hard one: THING builds FEWER specialists than the trip has legs, systematically
dropping the MINOR legs (Cairo layover, Dar es Salaam transit) and keeping the salient ones
(Serengeti/safari, Zanzibar), sometimes adding a generic catch-all.
- R7 · step 2 · L1 · user-thing/thing/instruct.md (itinerary/destination scope prose) · verify=INTERRUPTED · **OVERFIT to the travel domain** (human de-overfit it to a domain-neutral "brevity is not a merge" rule; anti-overfitting rule in judge.md/scenario-spec.md hardened to forbid domain framing, not just literal tokens)
- R8 · step 2 · L1 · user-thing/thing/instruct.md (domain-neutral "scope-inventory rule": complete inventory of independently-owned scopes; brevity is not a merge; an overview supplements, never replaces) + org/docs/system-spaces/README.md · verify=FAIL (2 clean reruns) · both persona replays still dropped Cairo AND Dar; a direct-authoring probe built all four → primitive is NOT the gap
- **Escalation note (human, after R8):** two L1 rounds of the RIGHT prose have not moved it, and the
  SAME two legs drop every time → this is SYSTEMATIC, not variance, and NOT an L1 problem. The
  granularity is decided by how many parts THING enumerates before delegating (the architect builds
  one space per seed — `system-architect/.../01-design.md` designs ONE agent per call). A free-form
  judgment that must come out the same every time wants a **DETERMINISTIC L2 structure**: an
  enumerate-then-`forEach` tasklist that lists the legs as discrete items and builds one specialist
  per item, so "how many specialists" stops being a per-run dice roll. Next round: CLIMB to L2; do
  not write the instruction a fourth time.
- R9 · step 2 · **L2** · NEW `user-thing/tasklists/organize_material/` (01-inventory → 02-build_specialist `forEach` → 03-build_app) + instruct.md delegates to it + `libs/core/src/spaces/system-spaces-dag.test.ts` + org/docs · verify=**INCONCLUSIVE (ran out of context / runner timeout)** · **THE L2 FIX IS CORRECT AND WORKS FOR COVERAGE** — verify-rerun-3 built ALL FOUR legs (Cairo, Dar, safari, Zanzibar). It did NOT complete because the automator's app build then exceeded the runner's **10-min per-turn timeout**; the judge then hit "Prompt is too long" after 3 heavy reruns before writing a report. Inventory rule was iterated to its final form: READ the attached source docs before classifying scopes (earlier tries over-fragmented into facts, or missed legs).
- **Direction (human, after R9):** DO NOT re-attribute and DO NOT rebuild `organize_material` — the L2
  structure is right and already in the tree. The only reason step 2 didn't verify was the harness
  per-turn timeout, now RAISED to 20 min (`sdk/org/scenarios/harness/lib/thing.mjs#dispatchAndWait`).
  Round 10's job is narrow: rebuild, run ONE clean `--through 2` verify, confirm all four leg
  specialists build ONCE each (no duplicate/overview catch-all) and the app builds, then judge step 2
  PASS and continue to step 3+. Keep reruns to a MINIMUM (the build turn's trace is context-heavy —
  don't burn context on repeats). If inventory still over/under-produces, tune ONLY 01-inventory's
  rule; the structure stays.
- R10 · step 2 · **L1 (architect action contract)** · `system-architect/agents/architect/instruct.md` (−12 net) + `libs/core/src/spaces/{prompt-contract,system-spaces-dag}.test.ts` · verify=**INCONCLUSIVE (ran out of context again)** · **DEEPER ROOT CAUSE found, past the L2 structure:** `organize_material`'s fan-out is WORKING — it enumerated all legs and fired all 5 architect delegates in parallel. But the architect's **action prompt manually CONTINUES after a tasklist result the runtime already auto-captures**; under the parallel `forEach`, that invalid cross-turn continuation clobbers sibling results → only the LAST architect build registers (some reruns instead over-produced 10 spaces w/ duplicate/overview junk — same bug, other face). R10 started the fix (delete the manual continuation, rely on the action runtime's envelope return) + updated both tests, but died of `Prompt is too long` before verifying.
- **Context fix (human, after R10):** rounds 9 AND 10 both died of context exhaustion because
  `step-NN.json` was an ~80KB full dump (every DB row + every yield's args) that the judge re-read
  across reruns. FIXED: `run-yaml.mjs` now writes a ~10× smaller COMPACT `step-NN.json` (observables:
  space names+count, table row-counts, delegate/yield kinds, errors, reply) and spills the raw dump to
  `step-NN.full.json`; judge.md tells the judge to read compact by default. This should let a round
  finish.
- **Direction (human, after R10):** the architect action-contract fix is IN THE TREE (unverified). DO
  NOT re-attribute — the diagnosis is solid (parallel architect results clobbered by the redundant
  manual continuation). Rebuild, run ONE clean `--through 2` verify, and confirm ALL legs build ONCE
  (no clobber → all present; no over-production → no duplicate/overview spaces). ⚠️ HIGH BLAST RADIUS:
  the architect action contract drives EVERY space build product-wide — confirm the two updated tests
  pass and that a SINGLE (non-parallel) `build_specialist` still returns its result. If verify is
  green, step 2 is finally done → continue to step 3.
- R11 · step 2 · (traced, no runtime edit) · verify=**INCONCLUSIVE (context death again)** · with R10's fix in the tree, step 2 swung the OTHER way: **17 fragmented spaces** ("Cairo Stopover 1", "Cairo Stopover 1 Arrival", "Cairo Stopover 2", "Arusha Pre-Safari", "Arusha Post-Safari", "Suricata Booking" + "Suricata Circuit", …) — the organizer RE-ENTERS / over-fragments. The judge was reaching for an **L3 action-runtime** change and died before writing it (no `src` runtime edit landed — the tree is still just the L2 tasklist + architect prompt + tests).
- **ROOT CAUSE (human, after R11 — high confidence, verified by reading the prompt):** it is NOT an
  L3 runtime bug. `user-thing/agents/thing/instruct.md` has **TWO overlapping build paths for supplied
  material that both fire**: (1) path 3 (~L394–417) "build each space DIRECTLY" via an inline per-part
  `delegate('system-architect','architect','synthesize_and_run')` loop, and (2) path 4a (~L458)
  `tasklist('organize_material')` which ALREADY does inventory → per-scope specialist → app — AND THEN
  (~L465–472) STILL says "build the spaces too… Build the parts first, then the app", duplicating what
  organize_material just did. So THING runs organize_material AND the manual per-part build → double
  build → re-entry / 17 fragments (and, when the manual path wins a race, the R10 clobber symptom).
- **Direction (human, after R11):** make `organize_material` the SINGLE supplied-material build path.
  In `thing/instruct.md`: on the "supplied material + yes" path, call `organize_material` and REPORT
  its result — DELETE the competing manual per-part build guidance (the L465–472 "build the parts
  first, then the app" prose, and steer path 3's inline `synthesize_and_run` loop to NON-supplied
  single-space requests only). This is **L1**, one file, a deletion — do NOT touch the action runtime
  (L3). Then RE-EXAMINE whether R10's architect continuation-removal is still needed with a single
  path (it may have been chasing a symptom of the double-build; keep it only if a single-path verify
  still shows a genuine parallel-clobber). Context death is fixed (compact `step-NN.json`), so ONE
  clean `--through 2` verify should finish: expect ONE specialist per leg (Cairo, Dar, Zanzibar/Stone
  Town, Serengeti/Ngorongoro safari), no duplicates, app served.
- R12 · step 2 · L1 · `user-thing/thing#path-3-and-path-4-consent` + `organize_material/01-inventory` · verify=FAIL · organizer-only replay invoked the tasklist but attempt 3 over-fragmented into 11 specialists and attempt 4 still created 8 off-partition specialists with `appManifest.built=false`; next round must diagnose tasklist inventory/app handoff rather than restoring duplicate direct calls.
- **Direction (human, after R12) — the SINGLE-PATH ROUTING FIX IS GOOD, keep it.** Two remaining issues, both INSIDE `organize_material` (the judge's own next-diagnosis is right: L2 reliability, not restoring THING's direct calls):
  1. **Re-entry / recovered-typecheck noise (fix FIRST — concrete):** all three nodes (`01-inventory`, `02-build_specialist`, `03-build_app`) say "Emit these statements across turns" and reference earlier-statement variables (`result`→`built`→resolve; `app`→`data`→resolve). **Variables DO NOT persist between statements** (each statement is its own module), so those cross-statement refs throw the "unavailable `result`" recovered-typecheck errors the report noted, and drive retries/instability (a likely contributor to the "repeated organize_material + two automator delegates" re-entry). REWRITE each node body to ONE self-contained statement that chains the delegate and `currentTask.resolve(...)` inline — exactly the "one-statement action contract" the architect node already uses. This is fixing the NODE code (L2), not the runtime (not L3). Then confirm the trace shows organize_material invoked ONCE and automator delegated exactly ONCE.
  2. **Inventory partition axis (the judgment core):** `01-inventory` keeps choosing CROSS-CUTTING facets (budget, logistics, travel-documents, a single photo/memo) as their own specialists instead of the user's real-world SEGMENTS. Make the partition axis explicit: one specialist per distinct real-world SEGMENT that owns its own body of facts (here: each place/stage of the trip); a cross-cutting facet (a budget, a documents checklist, one loose photo/memo, a travelers list) is APP DATA, never its own specialist. Domain-neutral, no leg/place literals. If this cannot be made reliable in prose because the partition is inherently model-judgment, DOCUMENT the missing primitive per the ladder before reaching for L3.
  Context death is fixed (compact evidence) and the routing is single-path, so a clean `--through 2` should now be judgeable: expect ONE specialist per segment, no cross-cutting-facet specialists, organizer runs once, app served with real rows.
- R13 · step 2 · L2 · `user-thing/tasklists/organize_material/{01-inventory,02-build_specialist,03-build_app}` · verify=FAIL · moved attachment reads into inventory prelude and made both delegate nodes self-contained; fresh replay created source-backed rows without web research but still made 9 overlapping spaces (including duplicate Ngorongoro Tariffs Reference) and left `appManifest.built=false`; trace next post-tasklist continuation (`organize_material_result` undefined) and automator finalization—do not restore direct path or retry L1 partition prose.
- R13 · step 2 · **L2** · `organize_material/{01-inventory,02-build_specialist,03-build_app}` rewritten to one-statement / prelude form + DAG test · verify=FAIL · **the one-statement NODE rewrite WORKED** (cross-statement `result`/`built`/`app` errors GONE, typecheck+DAG+build all pass) but step 2 still fails: THING RE-ENTERS (9 overlapping/duplicate specialists, automator delegated TWICE, `built:false`).
- **ROOT of the re-entry (human, after R13 — high confidence):** the report's own evidence names it — a recovered **`Cannot find name 'organize_material_result'` AFTER the tasklist resolves**. That is THE SAME cross-statement bug, now in **THING's CALLER code**: THING does `const organize_material_result = await tasklist('organize_material', …)` in one statement and references it in the NEXT — variables don't persist → typecheck error → THING RETRIES the whole call → second `organize_material` run → second automator → races app to `built:false` and doubles the specialists (9 ≈ 2 runs). One fix very likely clears ALL THREE symptoms (re-entry, double-build/over-count, unbuilt app).
- **Direction (human, after R13):** in `user-thing/agents/thing/instruct.md`, model the accepted-offer call as ONE self-contained statement: await `tasklist('organize_material', …)` and produce the user's closing reply IN THE SAME STATEMENT (the result does not survive to the next). Make explicit: do NOT capture the tasklist result into a variable to use in a later statement; consume it inline (or, if the tasklist already reports to the user, just `await` it and give a one-line close in the same statement). Then verify the trace shows organize_material invoked ONCE and automator delegated ONCE. AFTER re-entry is gone, re-check the two follow-ons only if they persist: (a) partition still over-produces → tune `01-inventory` (segments, not cross-cutting facets); (b) `built:false` with a populated manifest → trace the automator's finalize (it may have been a casualty of the double-run). Do NOT restore direct build paths; do NOT add routing prose (exhausted).
- R14 · step 2 · L2 · `user-thing/thing#accepted-supplied-material-offer` + `organize_material/01-inventory` · verify=FAIL · inline organizer result removed the original retry (attempt-2: 4 architect + 1 automator) and the inventory reached stage-shaped scopes, but fresh attempt-3 still ran two organizer tasklists (10 architect + 2 automator), leaving 7 overlapping spaces, zero pages, and `built:false`; next round trace an L3 runtime completion/dedup boundary, not more prompt prose.
- R14 · step 2 · **L1** (THING inline envelope consumption) + `01-inventory` partition-axis · verify=FAIL (2 reruns) · **TWO big wins:** (a) the caller-side inline-consume fix removed the first re-entry — attempt-2 ran organize_material ONCE, 4 specialists, automator ONCE; (b) the partition-axis fix WORKED — attempt-3's inventory enumerated the RIGHT segments (Cairo, pre-safari, safari, Zanzibar, return). But attempt-3 still re-entered a SECOND time (2 organize_material runs, `built:false`). Judge hypothesized an L3 "no single-execution boundary" and stopped honestly.
- **ROOT of the SECOND re-entry (human, after R14 — pinned from the trace, NOT L3):** it is THING SELF-VERIFYING the build after the tasklist. The exact recovered error: `Type 'void' must have a '[Symbol.iterator]()'` on the statement `const [dbEntries, spacesEntries] = await inspect([dbDir.entries, spacesDir.entries]);` — THING calls `inspect()` (returns void) to "confirm tables and spaces exist", destructures the void, spirals on the recovered error, and re-emits `organize_material`. NOT a missing runtime boundary — THING should TRUST the tasklist envelope, not re-inspect the filesystem.
- **Direction (human, after R14):** L1 in `user-thing/agents/thing/instruct.md`. After `await tasklist('organize_material', …)`, THING TRUSTS the returned envelope (`r.ok` / `r.data`), composes ONE closing reply from it, and ENDS the turn. Make explicit: do NOT verify the build by inspecting the project (no `inspect()`/`listProjectDir` "to confirm tables/spaces exist") — the tasklist already built and registered everything and its envelope is the source of truth; re-inspecting is both wrong (`inspect` returns void → the `[Symbol.iterator]` throw) and spirals into re-running the whole workflow. General principle: after a workflow tasklist returns its envelope, an orchestrator reports from the envelope and stops — it does not re-derive success by reading the filesystem. This is NOT L3 — do not add a runtime single-execution guard. After this, re-check `built:false`: it is most likely the second (racing) automator run and should clear once the workflow runs once; if it persists with a single run, trace the automator finalize separately.
- R15 · step 2 · L1 + L2 · `user-thing/thing#accepted-supplied-material-offer`, `organize_material/03-build_app` · verify=FAIL · L1 envelope-trust fix eliminated re-entry (one organizer), but direct automator still returned survey-only success with zero app artifacts; L2 app-architect action routed the build into the deterministic DAG yet its tasklist returned early before `create_project`/write nodes (no tables/pages). Next round: trace why the nested action tasklist has an immediate completed `build_app` child; do not undo organizer single-path or direct-delegate removal.
- R15 · step 2 · **L1 (envelope/no-self-inspect)** + rerouted 03-build_app → `app-architect#build_app` · verify=FAIL · **THE SELF-VERIFY FIX WORKED** — organizer now runs EXACTLY ONCE (both re-entries gone), and the partition is right (4 appropriately-scoped specialists). BUT the app still doesn't build: `appTables:{}`, `pageCount:0`, `built:false`.
- **ROOT of built:false (human, after R15 — WRONG PIPELINE):** R15 rerouted `03-build_app` to `system-appbuilder/app-architect#build_app`, but that is the **standalone-CATALOG** app pipeline (THING's path 4b — "a NEW, shareable app UNRELATED to the current project's data"); it builds a catalog template, not the current project, so it authored ZERO project tables/pages. The **LIVE-PROJECT** app is built by the **AUTOMATOR** (path 4a): `delegate('system-appbuilder','automator', { query })` — it holds `db:schema/db:write/pages:write/api:write/hooks:write` and `writeProjectTable`/`writePage`/`writeApi`, and republishes live. Earlier rounds' automator path DID create tables (cost_items, itinerary_legs, …) — it just didn't write PAGES / mark built (survey-only).
- **Direction (human, after R15):** in `organize_material/03-build_app.md`, REVERT the delegate target to the **automator** (path 4a, live-project), NOT `app-architect#build_app` (catalog). Keep the single-statement `currentTask.resolve(await delegate('system-appbuilder','automator', { query, attachmentIds }).then(app => ({ ok: app.ok===true, detail: … })))` shape. The query must ask the automator to build the FULL live-project app for the supplied material — tables AND the pages to view them — so the manifest ends with pages>0 and built:true (earlier automator runs made tables but pageCount:0). If the automator still stops at data-only, trace WHETHER the query asks for pages and whether `built` is set by a separate finalize the nested delegate skips. Everything else in step 2 is now correct (single run, right 4 specialists) — this is the LAST piece: a served app with real rows+pages.
- R16 · step 2 · L2 · `user-thing/organize_material/03-build_app` + `system-spaces-dag.test.ts` + `org/docs/system-spaces/README.md` · verify=FAIL · corrected catalog `app-architect#build_app` to live-project `automator`; fresh replay seeded four tables (27 rows) and wrote four pages/APIs, proving the handoff, but inventory still emitted four forbidden cross-cutting facets instead of independently-owned segments. The app manifest’s `built:false` is a cache-hit signal after the second automator pass; pages are present. Next round: do not retry partition prose; implement/justify an enforceable structured partition contract.
